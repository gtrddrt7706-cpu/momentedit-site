// 정점을 하나로 정하고, 울어도 된다고 말해 준다 [PEAK_ONE]
//
//   node scripts/apply-peak-one.mjs [--write]
//
// ★왜 — 2026-09-11 사장님: *"최선이야 ? 심리학 분석해서 개선있다면개선해봐"*
//   조사를 둘 돌렸다(감정 설계 · 부모 심리와 증언). 넷이 남았고, 그중 «지금 고칠 수 있는» 셋만 여기서 한다.
//
// ★★[PEAK_ONE — 정점은 대비의 산물이다]
//   감정 정점 여섯이 전부 최대치면 서로를 지운다(고립 효과는 «배경이 평평할 때만» 작동한다).
//   그리고 여러 회차로 이어지는 경험에서는 «정점을 하나 더 얹는 것»보다 «골을 하나 없애는 것»이
//   전체 평가를 더 올린다(Miron-Shatz 2009). 그래서 더하지 않고 «뺐다».
//   정점으로 정한 것: 어머님 「저는 그날 이 걱정을 내려놓았습니다」.
//     ①스물아홉 해 이어진 걱정이 ②이 예식 안에서 ③끝나는 것을 하객이 «실시간으로» 본다.
//     대본에서 시간 폭이 가장 긴 문장이고, 유일하게 «상태가 바뀌는» 문장이다.
//
//   그래서 바로 앞 순서인 아버님의 겹친 결말 하나를 눌렀다:
//     「하윤이 손을 스물아홉 해 잡고 걸었습니다 / 그게 참 좋았습니다 /
//      ★오늘부터 이 손은 셋이 잡습니다 / 잘 살아라」 — 결말이 둘 겹쳐 있었다.
//     뺀 것은 「셋이 잡습니다」다. ①비유라 이 대본에서 혼자 톤이 뜬다(사장님이 싫다고 한 아침드라마 결이다)
//     ②70초 뒤 어머님 정점과 같은 «스물아홉 해» 소재를 먼저 써 버린다
//     ③신랑 헌정의 「오늘부터 둘이 갑니다」와 「오늘부터 + 숫자」로 구조가 겹친다.
//     남은 「잘 살아라」가 착지를 이미 하고 있어 잃는 것이 없다.
//
// ★★[CRY_OK — 25명 방에는 익명이 없다]
//   200명 홀에서는 우는 사람이 군중에 묻히지만, 25명 방에서는 전부가 본다.
//   그래서 눈물이 «기쁨»이라고 이름 붙여 주지 않으면 「누가 힘든가 보다」로 읽히고,
//   옆 사람이 달래러 움직이면서 장면이 깨진다. 허가는 «편지 직전»에 한 번만 준다.
//   ★한 번만인 이유 — 두 번 말하면 지시가 되고, 울지 않는 사람이 «울어야 하나» 하고 눈치를 본다.
//   ★문장 — 「듣다가 울컥하셔도 괜찮습니다. 오늘은 그래도 되는 날입니다.」
//     감정을 «명명»하지 않고 «허용»만 한다. 「눈물」이라 부르는 순간 아직 안 운 사람에게 지시가 된다.
//   ★자리 — LETTER 세 변형(parent·each·both) 전부. 코스마다 어느 것이 뜨는지 다르고,
//     셋은 서로 배타라 중복 재생되지 않는다(ritual-cue.js letter() 는 하나만 고른다).
//
// ★★[ALGOE_TRAIT — 칭찬은 «네가 나에게 준 것»이 아니라 «너는 이런 사람이더라»]
//   Algoe 2016: 감사를 표현할 때 «수혜자의 이득»(내가 걱정을 놓았다)만 말하면 관계 효과가 낮고,
//   «상대의 됨됨이»(한 번 더 묻는 사람이구나)를 이름 붙이면 높다.
//   어머님은 목격담 뒤에 곧바로 자기 이득으로 넘어가고 있었다. 사이에 한 줄을 넣어 됨됨이를 부른다.
//   ★「싶었습니다」로 쓴 것은 열 줄 위 「이 아이는 평생 이러겠구나 싶어서요」와 안에서 운을 맞추기 위해서다.
//     같은 말투로 무서워했던 사람이 같은 말투로 안심한다.
//
// ★★[LAND — 착지는 설명이 아니라 관찰이다 (LETTER_LAND 원칙 그대로)]
//   어머님 끝 「엄마는 이제 네 걱정을 안 한다 / 네 옆에 사람이 생겼으니까」는
//   정점(「걱정을 내려놓았습니다」)을 곧바로 «다시 말하고» 그 이유까지 설명한다. 정점이 셋으로 닳는다.
//   → 「엄마 이제 밤에 잘 잔다」 하나로 바꾼다. 같은 내용의 «관찰형»이고, 11음절이라 앞줄보다 짧다.
//   ★사장님 지시로 살린 「낳아주셔서, 키워주셔서, 참아주셔서」(신부 편지)는 손대지 않았다.
//
// ★★[안 한 것 — 편지 2개·덕담 2개 사이를 나레이션으로 벌리는 것]
//   조사는 「연속 낭독 사이에 5~7초뿐」이라 했는데, 실측하면 그 사이에 나레이션은 «0초»다.
//   둘 다 한 라이브 블록 안에서 마이크가 손으로 건너간다(PARAM.read.partnerHandoffAt · 덕담은 디렉터 전달).
//   그 물리적 전달이 이미 10~15초짜리 간격이고, 나레이션을 끼우면 말이 늦은 부모를 «자르는» 사고가 난다.
//   구조를 바꾸는 일이라 사장님 결정 사항으로 남긴다 — 여기서는 하지 않는다.
//
// ★★[안 한 것 — 신부 서약 끝 「너는 나에게 먼저 말해」]
//   신랑 「괜찮다고 해도 한 번은 더 물을게」와 짝을 이루는 VOW_ECHO 장치다(merge-guard 에 「한쪽만
//   고치지 말 것」이라 적혀 있다). 겹쳐 보이지만 의도된 대구이고, 정점과 40분 떨어져 있어 상쇄하지 않는다.
//
// ★★[쓰지 않은 조사 결과 — 무효 판정]
//   「남을 대신해 말하면 대담해진다」(재현 실패) · 「간접 칭찬이 직접 칭찬보다 낫다」(Zhao 2022 와 상충) ·
//   「92%가 제3자를 신뢰한다」(마케팅 블로그 출처). 셋은 쓰지 않았다.
//
// ★비용 — 나레이션 3클립(우성)이 다시 녹음 대상이 된다. 배역 2클립은 어차피 재녹음 대기 중이라 추가 비용 0.
//
// ★종료 코드 0 다 맞음 · 1 자리를 못 찾음
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const WRITE = process.argv.includes('--write');
const CAST = path.join(ROOT, 'docs/plans/식순연구/배역_예시_대사.txt');
const DATA = path.join(ROOT, 'assets/ritual-data.js');

const CRY = '듣다가 울컥하셔도 괜찮습니다. 오늘은 그래도 되는 날입니다. ';
const TAIL = '이제, 그 목소리가 이어집니다.';

const NEW = [
  ['12', `서준아, 하윤아.
이거 쓰는 데 한 달 걸렸습니다.
그래도 길게는 하지 않겠습니다.
작년 겨울에 우리 집 보일러가 고장 났습니다.
날이 추워서 하윤이한테만 말했는데, 그 주 토요일에 서준이가 왔습니다.
두 시간을 만지더니 고쳐 놓고, 밥도 안 먹고 갔습니다.
저는 그날 저 사람이 어떤 사람인지 알았습니다.
말로 하는 사람이 있고, 그냥 하는 사람이 있습니다.
서준이는 뒤쪽입니다.
하윤아, 너는 앞쪽이지.
살다 보면 서로 미운 날이 온다.
그때 먼저 말을 거는 건 아마 네가 할 거다.
지는 게 아니다. 아버지는 그걸 알기까지 오래 걸렸다.
하윤이 손을 스물아홉 해 잡고 걸었습니다.
그게 참 좋았습니다.
잘 살아라.`],
  ['13', `하윤아, 서준아.
오늘 할 말을 적어 왔습니다.
그러지 않으면 못 할 것 같아서요.
하윤이가 고등학교 삼학년 때 일입니다.
아침에 얼굴이 하얘서 왜 그러느냐고 물었더니 괜찮다고 했어요.
학교에 보내 놓고 마음이 놓이지 않아, 점심때 찾아갔습니다.
하윤이는 이미 양호실에 누워 있었어요.
왜 말을 하지 않았느냐고 하니, 엄마가 걱정할까 봐 그랬답니다.
그때 저는 좀 무서웠습니다.
이 아이는 평생 이러겠구나 싶어서요.
서준아.
지난여름에 둘이 우리 집에 왔을 때요.
하윤이가 괜찮다고 하는데, 네가 한 번 더 물었지요.
한 번 더 묻는 사람이구나 싶었습니다.
저는 그날 이 걱정을 내려놓았습니다.
하윤아.
엄마 이제 밤에 잘 잔다.`],
];

// ── 1. 배역 대사
const lines = fs.readFileSync(CAST, 'utf8').split('\n');
const at = new Map();
lines.forEach((l, i) => { const m = /^\[(\d+)\]/.exec(l); if (m) at.set(m[1], i); });
const syl = (s) => (s.match(/[가-힣]/g) || []).length;
let bad = 0;
const plan = [];
for (const [no, text] of NEW) {
  const h = at.get(no);
  if (h === undefined) { console.log(`✗ [${no}] 머리를 못 찾았다`); bad++; continue; }
  let e = h + 1;
  while (e < lines.length && lines[e].trim()) e++;
  const neu = text.split('\n').map((s) => s.trim()).filter(Boolean);
  const old = lines.slice(h + 1, e);
  plan.push({ no, h, e, neu });
  /* ★[LAND_VOCATIVE] 착지 검사는 «마지막 줄 < 앞 줄»인데, 앞 줄이 호명(「하윤아.」)이면
     3음절짜리를 상대로 재게 돼 멀쩡한 착지가 ★긺으로 뜬다. 호명은 문장이 아니라 «부름»이고
     그 뒤가 곧 착지다. 그래서 호명은 건너뛰고 그 앞의 «진짜 문장»과 비교한다.
     ★조건을 좁게 잡았다 — 6음절 미만 + 「아./야.」로 끝나는 줄만. 그래야 짧은 평서문이
     호명으로 오인돼 검사를 빠져나가지 않는다. */
  const isCall = (s) => syl(s) < 6 && /[아야]\.$/.test(s.trim());
  let pi = neu.length - 2;
  while (pi > 0 && isCall(neu[pi])) pi--;
  const last = syl(neu[neu.length - 1]), prev = syl(neu[pi] ?? '');
  const long = neu.filter((s) => syl(s) > 32).length;
  const sec = (neu.reduce((a, s) => a + syl(s), 0) / 406 * 60).toFixed(0);
  const who = lines[h].split('·')[1]?.trim() ?? '';
  console.log(`  [${no}] ${who.padEnd(5)} ${old.length}→${neu.length}문장 · 약 ${sec}초 · 끝 ${String(last).padStart(2)}(앞 ${String(prev).padStart(2)}) ${last < prev ? '착지 ok' : '★긺'} · 32음절 넘는 줄 ${long}`);
}

// ── 2. 나레이션 — 편지 여는 말 3종에 «울어도 된다»를 한 줄 넣는다
let data = fs.readFileSync(DATA, 'utf8');
const hits = [...data.matchAll(/nar:"([^"]*이제, 그 목소리가 이어집니다\.)"/g)];
console.log(`\n  편지 여는 말 ${hits.length}곳 (parent·each·both 셋이어야 한다)`);
if (hits.length !== 3) { console.log('✗ 3곳이 아니다 — 아무것도 쓰지 않는다.'); bad++; }
if (data.includes(CRY.trim())) { console.log('✗ 이미 들어 있다 — 두 번 넣지 않는다.'); bad++; }
for (const h of hits) console.log(`    …${h[1].slice(-46)}`);

if (bad) { console.log('\n맞지 않는 것이 있다 — 아무것도 쓰지 않는다.'); process.exit(1); }
if (!WRITE) { console.log('\n(안 씀 · --write 로 실제 반영)'); process.exit(0); }

const out = [];
let i = 0;
for (const p of plan.sort((a, b) => a.h - b.h)) { out.push(...lines.slice(i, p.h + 1), ...p.neu); i = p.e; }
out.push(...lines.slice(i));
fs.writeFileSync(CAST, out.join('\n'));

data = data.replace(/(nar:"[^"]*?)(이제, 그 목소리가 이어집니다\.")/g, `$1${CRY}$2`);
const after = (data.match(new RegExp(CRY.trim().replace(/[.*+?^${}()|[\]\\]/g, '\\$&'), 'g')) || []).length;
if (after !== 3) { console.log(`✗ 넣고 보니 ${after}곳 — 되돌린다.`); process.exit(1); }
fs.writeFileSync(DATA, data);
console.log(`\n반영함 · 배역 ${plan.length}클립 · 편지 여는 말 ${after}종 (${TAIL.slice(0, 6)}… 앞에 삽입)`);
