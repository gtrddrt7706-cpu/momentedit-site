# check-copy.py 묶음 — 전 회차 완료판

## ★기준
- **그쪽 `dc48c2dc` 위에 얹은 판입니다.**
- ★**7·8회차가 늘었습니다.** 7회차 = 하객맞이 여덟(제가 빠뜨렸던 구간) · 8회차 = 편지·덕담 일곱을
  같은 자로 재기(43번 두 줄만 고쳤고 편지 여섯은 한 글자도 안 고쳤습니다).
- ★`전수표.py` 를 새로 넣었습니다. **91클립을 식순 순서대로 세우고 «미착수»가 하나라도 있으면
  종료코드 1** 입니다. 회차를 «진단서가 나눠 놓은 구간»에서 가져왔다가 하객맞이 여덟을
  통째로 빠뜨린 사고에서 나왔습니다. 지금 미착수 0 · 사유 있는 제외 8.
- ★**검사가 2분 넘게 걸리던 것을 0.05초로 줄였습니다.** 배타 조합이 38,880가지가 되면서
  전수 열거가 불가능해졌습니다. 열거 대신 «묶음마다 최악인 칸을 고르는» 셈으로 바꿨고,
  조합이 적은 묶음에서 옛 방식과 전수 대조해 **같은 답**임을 확인했습니다(근사 아님).
- `selftest-check.py` 는 **그쪽이 보내 주신 판을 그대로 씁니다**(`[CASE_TIED_TO_BODY]` 포함).
  제 판에 있던 케이스가 이미 전부 들어 있어 더할 것이 없었습니다.
- `본문_여섯편.txt` 도 그쪽 판으로 바꿨습니다. 생성기 `build-body-six.py` 를 함께 넣어 뒀습니다.
  **손 사본을 만들지 않습니다.**
- ★`40_toast-toast #0` 을 「이제, 축배를 들겠습니다」 → **「축배를 들겠습니다」** 로 되돌렸습니다.
  그쪽 게이트가 기록형 코스에서 성혼 선언 바로 뒤라는 것을 잡아 주셨습니다.
  제 검사는 «한 예식 안에서 들리는 것»을 세지 «어느 클립 다음에 어느 클립이 붙는가»를 못 봅니다 —
  그 자는 큐 엔진에만 있습니다.

## 돌리는 법
```
python3 check-copy.py round0.json ... round6.json 본문_여섯편.txt
python3 selftest-check.py                      # 검사기 자신을 검사 (11/11)
python3 build-copy-pick.py round*.json 판정.html
```
지금: **문장 176 · 빨강 0 · 노랑 0 · 자가시험 11/11**

## 오늘 얹은 변경
### 오탐 교정 열두 건 (9~20 · 전부 근거 주석 포함)
9 어미를 «금지»→«예산» · 10 종결어미 덩어리를 낱말로 셈 · 11 전문 겹침이 배타를 무시
12 보어를 주어로 셈 · 13 「한 분도」를 수량으로 셈 · 14·15 「큰 박수」 예약 범위
16 주제격 뒤 진짜 주어 · 17 「한 장」은 관용 · 18 어형 변화를 예우 삭제로 셈
19 「한 분씩」은 관용 · 20 부사절 안 추상어를 주어로 셈

### 구조 변경 넷
- **검사 2 → «섞임 재기»** (한 꼴 금지는 다른 한 꼴로 몰아간다 · 38%/50%)
- **검사 19 → «자리»에서 «글자»로** (게이트는 글자 수를 세지 자리를 안 본다)
- **검사 20 [IN_OUT_PAIR] 신설** (41↔77 나이프 어긋남)
- **닳은 낱말 → 낱말별 명시 예산**

### 「넘긴 것」 칸
문안으로 못 고치는 자리를 빨강에서 빼되 **지우지 않고** 따로 찍습니다.
지우면 다음 사람이 «검사가 초록이니 없는 문제»로 읽습니다. 지금 넷.

## ★번호가 겹칩니다
진행 `06·07·21·22` 와 배역 `06·07·19·21·22` 가 다른 셈판인데 번호가 같습니다.
그냥 두었더니 «22와 23이 같이 나가는 예식»이라는 있을 수 없는 조합이 만들어졌습니다.
→ 여기서는 배역 쪽에 `06w`·`07w`·`19c`·`21c`·`22c` 를 붙였습니다. 실제 키로 바꿔 주십시오.

## 배타 갈래를 반드시 적어 주십시오
`excl` 이 «한 예식에 하나만 나가는 묶음»입니다. 틀리면 검사가 통째로 틀립니다.
- 안 적으면 → 있을 수 없는 조합을 겹침으로 셉니다
- **짝으로 잘못 묶으면 → 실제로 나갈 조합을 통째로 못 봅니다** (입장에서 실제로 났던 일)

## 항목을 새로 넣으면 자가시험도 함께
「그 항목이 빨개지는 고장」이 없는 항목은 있으나 마나입니다.
오늘 이 자가시험이 자기 자신의 고장을 하나 잡았습니다 — 회차 목록을 손으로 적어 둬서
4회차를 건드리는 시험 셋이 조용히 헛돌았습니다.
