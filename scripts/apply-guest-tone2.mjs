/* ★★[GUEST_TONE2 2026-09-14 사장님 *"편히 드시면 이것도 빼자"* · *"이 대사 전부 한번 비슷하게 검토하고"*]
 *
 * 앞 판([GUEST_TONE])에 이어 넷을 더 고친다. 80줄을 같은 눈으로 다시 보고 고른 것이다.
 * ★감정 구간(서약·편지·덕담·헌정)은 손대지 않았다 — 게이트에 결정이 잠겨 있고
 *   ([THREE_BEAT]·[PEAK_ONE]·[CROSS_ECHO]·[BLESS_SCENE]·[KEEP_AS_IS]) 사장님이 받으신 판이다.
 *   진행·안내 나레이션에서만 골랐고, 억지로 개수를 늘리지 않았다.
 * ★세 벌 전부에 친다([COPY_THREE]) — ritual-data · order-preview · 배역_예시_대사.
 *   앞 판에서 배역 쪽 한 벌을 빠뜨려 대장이 옛말을 물고 갔다. 같은 실수를 두 번 하지 않는다.
 */
import fs from 'node:fs';
const ROOT = new URL('..', import.meta.url).pathname;
const P = (f) => ROOT + f;
const now = new Map();
const cur = (f) => now.has(f) ? now.get(f) : fs.readFileSync(P(f), 'utf8');
let bad = 0;
function sub(f, old, neo, want = 1) {
  const s = cur(f); const n = s.split(old).length - 1;
  if (n !== want) { console.log(`  ✗ ${f}: "${old.slice(0, 26)}…" ${n}건 (${want} 기대)`); bad++; return; }
  now.set(f, s.split(old).join(neo)); console.log(`  ✓ ${f}: ${want}건`);
}
const D = 'assets/ritual-data.js', O = 'order-preview.html', C = 'docs/plans/식순연구/배역_예시_대사.txt';

console.log('\n① 「편히 드시면」 — 사장님: *"편히 드시면 이것도 빼자"*');
/* 다과가 있다는 «사실»은 앞줄이 이미 말한다. 뒷줄까지 먹기를 권하면 그 사실이 «권유»가 된다.
   십 분 전 안내가 할 일은 앉히는 것 하나뿐이다. ★「편히 드시면」을 되살리지 말 것. */
sub(D, '자리에 앉으셔서 편히 드시면 됩니다.', '자리에 앉아 주시면 됩니다.', 2);
sub(O, '자리에 앉으셔서 편히 드시면 됩니다.', '자리에 앉아 주시면 됩니다.', 2);
sub(C, '자리에 앉으셔서 편히 드시면 됩니다.', '자리에 앉아 주시면 됩니다.', 1);

console.log('\n② 「그 편지는 두 분이 가져갑니다」 — 갈래에 따라 «틀린 말»이 된다');
/* 이 대본에서 「두 분」은 신랑·신부다. 그런데 이 한 문장이 편지 세 갈래 «전부»의 닫는 말이다:
     부모님께 → 받은 사람은 부모님이다. 「두 분이 가져갑니다」면 준 사람이 도로 가져간다.
     서로에게 → 이때만 맞는다.      두 통 → 두 편지의 임자가 다르다.
   ★[NO_VERDICT] 가 정한 «사실만 말한다»는 그대로 지킨다 — 감상을 결론짓지 않는다.
     바꾸는 것은 «누가»뿐이고, 세 갈래에서 모두 참인 말로 둔다. */
sub(D, '그 편지는 두 분이 가져갑니다.', '그 편지는 받으신 분이 간직하십니다.', 1);
sub(O, '그 편지는 두 분이 가져갑니다.', '그 편지는 받으신 분이 간직하십니다.', 2);

console.log('\n③ 「아직 만나지 못한 자리도」 — 자리를 «만난다»고 했다');
/* 만나는 것은 사람이고 찾아가는 것이 자리다.
   ★같은 장면의 앞 클립(61_narr-round-open)이 이미 「자리마다 차례로 찾아뵙습니다」로 옳게 쓴다.
     그 말에 맞추면 두 클립이 한 장면으로 이어진다. */
sub(D, '아직 만나지 못한 자리도 차례로 찾아뵙습니다.', '아직 들르지 못한 자리도 차례로 찾아뵙습니다.', 1);

console.log('\n④ 「오늘 오신 분들 얼굴을 한 분씩 다 알고 있습니다」 — 신부 첫인사');
/* 말하려는 것은 «스물다섯 명이라 다 아는 사이»다. 그런데 「얼굴을 … 알고 있습니다」는
   사람을 «파악하고 있다»로 들린다. 어른들 앞에서 신부가 할 말의 결이 아니다.
   ★「한 분 한 분」은 바로 다음 줄이 쓴다 — 여기서 겹쳐 쓰면 그 줄의 힘이 죽는다. */
/* ★이 줄은 배역_예시_대사.txt 에만 있다 — 07_welcome-bride 는 한 벌짜리 클립이다([COPY_THREE]).
     ★ritual-data 에도 「오늘 오신 분들 얼굴을 한 분씩 «보면서»」가 있는데 그건 «다른 문장»이다
       (마지막 인사). 앞자락이 같다고 같이 치면 엉뚱한 데를 고친다 — 첫 판이 그랬다. */
sub(C, '오늘 오신 분들 얼굴을 한 분씩 다 알고 있습니다.', '오늘 오신 분들은 저희가 다 아는 분들입니다.', 1);

if (bad) { console.log(`\n★${bad}자리가 어긋나 아무것도 쓰지 않았다.`); process.exit(2); }
for (const [f, s] of now) fs.writeFileSync(P(f), s);
console.log(`\n[GUEST_TONE2] ${now.size}개 파일 반영.`);
