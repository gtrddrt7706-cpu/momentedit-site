/* ★★[LETTER_WHOLE_TAKE 2026-09-19] 문장 단위 창고가 «해당되지 않는» 클립.
 *
 * 어른께 드리는 편지(parents-letter)는 처음부터 «통낭독»이다 — 사장님이 45문장을 한 호흡으로
 * 받아서 통째로 주신다. 그래서 assemble-parents-letter.mjs 도 이 한 클립만 따로 있다.
 *
 * ★왜 창고에서 빼나 — 안 빼면 매번 «다시 받아야 한다»고 말한다
 *   창고(sent-lib)는 문장마다 파일이 하나씩 있어야 «찼다»고 센다. 통낭독은 파일이 하나다.
 *   그대로 두면 대본을 고칠 때마다 45줄 전체가 «낡음»으로 뜨고, 다시받기 생성기가
 *   이미 받은 것을 또 받으라고 내민다. 실제로 2026-09-19 에 그렇게 나왔다(32 낡음 · 3 없음).
 *
 * ★그리고 더 나쁜 것 — 옛 판 조각이 남아 있으면 섞인다
 *   43문장 시절 문장 flac 42개가 남아 있었다. 그 뒤 두 문장이 들어가 번호가 밀렸으니
 *   같은 번호가 다른 문장을 가리킨다. [CUT_AMBIG] 가 막는 그 사고의 씨앗이라 파일도 지웠다.
 *
 * ★되살리려면 — 이 클립을 문장 단위로 받기로 «바꾸는» 것이 먼저다.
 *   그때 이 목록에서 빼고, assemble-parents-letter.mjs 도 함께 본다. 한쪽만 바꾸면 갈린다.
 */
export const WHOLE_TAKE = new Set(['parents-letter']);
export const isWholeTake = (c) => WHOLE_TAKE.has(c.file);
