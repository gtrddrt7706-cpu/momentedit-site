# 작업서 · 결제 시스템 적용 전 명세 (검토용)

> 작성 2026-06-30 · 목적: **실제 코드 작업 전에 검토·승인**하기 위한 구현 명세. 돈이 걸린 작업이라 파일별 변경·데이터모델·웹훅·테스트·수용기준·롤백을 모두 명시.
> 상위 전략은 `PLAN_결제_기획.md` §12(확정본). 이 문서는 "그걸 코드로 어떻게 만들지"의 실행 설계.
> **승인 전엔 코드 건드리지 않음.** (현재 사전구축분: 카드 confirm `98_pay_card.gs`/`meCardPay` · 플래그 OFF)

---

## 0. 안전 원칙 (전 작업 공통 · 불변)
- **플래그 OFF 기본** — `PAY_CARD_ENABLED!=='true'`면 결제 UI·SDK·웹훅 모두 비활성. 켜기 전 라이브 영향 0.
- **계좌이체(현행) 무영향** — 기존 입금신호→관리자확인 흐름·금액·단계 로직 일절 변경 금지. 결제 "수단"만 추가.
- **금액은 서버가 재계산** — 클라이언트가 보낸 금액 신뢰 금지. `_journeyAmounts` 단일 출처와 일치해야만 기록.
- **멱등** — 같은 단계 중복 확인 금지(이미 '확인'이면 차단). LockService로 직렬화.
- **기록은 기존 함수 재사용** — `adminConfirmPayment/Mid/Balance`를 호출해 기록(컬럼·단계전이·고객알림 일관). 결제수단이 카드든 가상계좌든 동일.

---

## 1. 범위

### 1-1. 이번 작업(In scope)
- A. **가상계좌(무통장) 발급 + 입금 웹훅 자동확인** ← 고액 핵심(연구 §12 반영)
- B. **결제수단 차등 노출** — 소액(예약금·계약금)=카드/간편결제, 고액(중도금·잔금)=가상계좌
- C. **디자인 마감** — `── 또는 ──` 디바이더 · 카드/가상계좌 경로 현금영수증 분기 · 성공 시 ✓ 접힘
- D. 묶음결제(중도금+잔금 합계) 결제 지원
- E. (선택) 카드 부분취소/환불 API
- F. (선택) 링크페이 결제링크 발송

### 1-2. 이번 작업 아님(Out of scope)
- 토스 라이브 가동(통신판매업 신고 후) · 실제 PG 계약 · 에스크로 인증마크 게시(별도)

---

## 2. 데이터 모델 변경

### 2-1. 새 시트 `가상계좌` (VA 발급·입금 추적)
| 컬럼 | 설명 |
|---|---|
| 발급시각 | VA 발급 시각(KST) |
| orderId | 우리 주문ID(`ME...`) · 웹훅 매칭 키 |
| 개인코드 | 고객 |
| 단계 | 계약금/중도금/잔금 |
| 금액 | 서버 기대금액 |
| paymentKey | 토스 결제키 |
| VA은행 | 입금은행 |
| VA번호 | 가상계좌번호 |
| 만료 | 입금 기한 |
| 상태 | 발급 / 입금완료 / 만료 |

> 고객 식별을 **웹훅 payload에 의존하지 않음** — 발급 시 orderId↔개인코드·단계를 이 시트에 저장하고, 웹훅은 orderId로 조회해 매칭(스푸핑 차단).

### 2-2. 기존 `카드결제로그`(이미 있음) — 가상계좌 결과도 함께 적재(결과/메모로 구분).
### 2-3. Customers 시트 — **컬럼 추가 없음**(상태는 기존 `입금상태/중도금상태/잔금상태`='확인' 그대로 사용). 헤더 드리프트 0.

---

## 3. 작업 항목별 상세

### A. 가상계좌 발급 + 입금 웹훅
**A-1. 발급(프론트→서버 기록)**
- 프론트 `meCardPay.pay(milestone, amount, '가상계좌')` → 토스 SDK `requestPayment('가상계좌', {...})`.
- 토스가 VA 발급(상태 `WAITING_FOR_DEPOSIT`) → successUrl 복귀.
- 복귀 시 `cardPayConfig`가 아니라 **`vaIssued`** 호출: `{token, milestone, paymentKey, orderId, amount, bank, accountNumber, dueDate}` → 서버가 금액검증 후 `가상계좌` 시트에 '발급' 적재 → 화면에 "입금 대기" 카드(계좌번호·기한) 표시.

**A-2. 입금 웹훅(서버, 자동확인)**
- 신규 공개 엔드포인트 `doPost action='vaWebhook'`(또는 별도 webhook URL).
- 수신 시 **payload를 신뢰하지 않고**, `paymentKey`로 **토스 GET `/v1/payments/{paymentKey}` 재조회**(시크릿키) → `status==='DONE'` && `totalAmount===기대금액` 확인.
- `가상계좌` 시트에서 orderId로 행 조회 → 개인코드·단계 확보 → **이미 '확인'이면 멱등 종료** → 아니면 `adminConfirmX(code)` 호출로 기록 + 상태 '입금완료'.
- 웹훅 보안: ① 토스 재조회로 진위 확인(필수) ② 선택 `WEBHOOK_SECRET` 경로 토큰. LockService.
- **만료 처리**: 일1회 트리거가 `가상계좌` '발급' 중 만료 지난 행 → '만료'로 정리(미입금).

**검증**: 발급→가짜입금(샌드박스)→웹훅→해당 단계 '확인' + 마이페이지 반영 + 중복 웹훅 멱등.

### B. 결제수단 차등 노출 (`meCardPay`)
- 단계별 노출 정책(서버 `cardPayConfig`가 단계별 `methods` 배열 반환):
  - 계약금(소액): `['카드','간편결제']` 우선(간편결제 상단) · 가상계좌·계좌이체 보조
  - 중도금·잔금(고액): `['가상계좌']` 기본 · 계좌이체(0%) 보조 · 카드는 "할부 원하면" 접힘 안에
- 프론트는 정책대로 버튼 구성(없는 수단은 미표시).
- **검증**: 단계별로 맞는 버튼만 노출 · OFF면 전무.

### C. 디자인 마감
- `── 또는 ──` 헤어라인 디바이더(기존 톤).
- **현금영수증 입력**: 계좌이체·가상계좌 경로에만 노출, 카드 경로엔 숨김.
- 성공 시 카드 `collapsedCard`로 ✓ 접힘("결제 완료 · 카드/가상계좌").
- **검증**: render-check 구문 통과 + OFF 시 DOM 변화 0.

### D. 묶음결제(중도금+잔금 합계)
- `handleCardConfirm`/`vaWebhook`에 **복수 단계 동시확인** 옵션: `milestones:['중도금','잔금']` → 합계 검증 → `adminConfirmMidBalance(code)` 재사용.
- **검증**: 합계 금액 일치 시 두 단계 동시 '확인'.

### E. (선택) 카드 환불 API
- `adminCancelCard(code, milestone, reason)` → 토스 `/v1/payments/{paymentKey}/cancel` → 상태 되돌림(관리자 전용·adminCall). 노쇼·청약철회 대비.
- **검증**: 샌드박스 부분취소 → 상태 복원.

### F. (선택) 링크페이
- 분납 알림(95_notify)에 결제링크 첨부 발송. 도입 여부 미정(§7 질문).

---

## 4. API · 웹훅 명세(요약)
| action | 입력 | 처리 | 출력 |
|---|---|---|---|
| `cardPayConfig` | token,(milestone) | 활성·clientKey·단계별 methods·금액 | {enabled,clientKey,methods,amount} |
| `cardConfirm` | token,milestone,paymentKey,orderId,amount | 금액검증→토스 confirm→기록 | {ok,recorded} |
| `vaIssued` | token,milestone,paymentKey,orderId,amount,bank,acct,due | 금액검증→VA시트 '발급' | {ok} |
| `vaWebhook` | (토스)paymentKey,orderId,status | 토스 재조회 검증→기록 | {ok} |
| `adminCancelCard`(선택) | code,milestone | 토스 취소 | {ok} |

---

## 5. 보안 체크리스트
- [ ] 모든 고객 액션 `resolveSession(token)` 필수
- [ ] 금액 = `_journeyAmounts` 서버값과 일치(불일치 기록 거부)
- [ ] 웹훅은 payload 불신 → **토스 재조회로 status/amount 검증**
- [ ] 멱등(이미 '확인' 차단) + LockService
- [ ] 시크릿(`TOSS_SECRET_KEY`)은 ScriptProperty만 · 프론트엔 clientKey만
- [ ] 플래그 OFF 시 전 경로 비활성
- [ ] 로그에 카드번호·민감정보 미기록(paymentKey 일부만)

---

## 6. 테스트 계획(샌드박스 · 수용기준)
| # | 시나리오 | 기대 |
|---|---|---|
| 1 | 카드 결제 성공 | 단계 '확인' + ✓ 접힘 + 고객알림 |
| 2 | 카드 취소(me_pay=0) | 카드 유지·재시도 가능 |
| 3 | 카드 승인 실패 | 안내 + 계좌이체 대체 노출 |
| 4 | 금액 위변조(임의 변경) | 기록 거부 |
| 5 | 카드 이중 confirm | 두번째 already(이중청구 X) |
| 6 | 가상계좌 발급 | "입금 대기" + 계좌번호 표시 |
| 7 | 가상계좌 입금 웹훅 | 자동 '확인' 반영 |
| 8 | 웹훅 중복 수신 | 멱등(중복기록 X) |
| 9 | 가상계좌 만료 | '만료' 정리·미확인 |
| 10 | 묶음(중도금+잔금) | 합계 일치 시 동시 '확인' |
| 11 | OFF 상태 | 버튼·SDK·웹훅 전무 |

**수용기준**: 1~11 전부 통과 + gas-lint/render-check 통과 + 계좌이체 기존 흐름 회귀 없음.

---

## 7. 미결정 사항(작업 전 결정 필요 · 질문)
1. **소액 카드 허용 범위**: 예약금(10만)·계약금만 카드/간편 허용? 고액(중도금·잔금)은 카드 막고 가상계좌만? (연구 권장=고액은 가상계좌, 카드는 "원하면")
2. **에스크로/구매안전 인증마크**: PG 옵션으로 켜고 하단 게시? (의무 아님 · 신뢰장치)
3. **링크페이(F) 도입 여부**: 분납 알림에 결제링크 첨부?
4. **환불 API(E)**: 이번에 포함? (노쇼·청약철회 자동화)
5. **가상계좌 입금기한**: 며칠로? (예: 발급 후 3일)
6. **웹훅 호스팅**: GAS doPost(`vaWebhook`) vs Vercel 함수 — GAS 권장(기록 일원화). OK?

---

## 8. 롤아웃 · 롤백
- **롤아웃**: 코드 머지(플래그 OFF) → 샌드박스 키로 E2E → 통과 시 OFF 유지 → 오픈 후 라이브키+ON.
- **롤백**: `PAY_CARD_ENABLED` 삭제/OFF → 즉시 전체 비활성(계좌이체만). 코드 되돌릴 필요 없음.

---

## 9. 작업 순서(승인 후)
1. (B,C) 차등노출 + 디자인 마감 — 키 없이 가능 · OFF
2. (A) 가상계좌 발급+웹훅 — 키 없이 코드까지 · 샌드박스 검증은 키 후
3. (D) 묶음결제
4. (E,F) 환불·링크페이(선택)
5. 샌드박스 E2E(테스트키) → 수용기준 통과 → OFF 유지

---

## 10. 작업 전 체크(이 작업서 승인 항목)
- [ ] §7 미결정 6개 답변
- [ ] 범위 A~F 중 이번에 할 것 확정
- [ ] 데이터모델(`가상계좌` 시트) OK
- [ ] 보안 체크리스트 동의
- [ ] "플래그 OFF로 미리 다 만들고, E2E는 키 후" 방식 동의

---

## 부록 · 개선검토 로그

### Pass 1 — 큰 틀(아키텍처·전략) · 2026-06-30
- **BP-1 우선순위 재정렬**: 빅뱅 금지. 단계 = ①가상계좌+웹훅 → ②카드/간편 → ③묶음/환불. (가치·리스크상 가상계좌 최우선)
- **BP-2 회계·정산 가시화(추가)**: 직접 계좌이체(즉시·전액) vs PG(D+N·수수료차감) 두 스트림 → 관리자 화면/로그에 "직접입금 / PG정산대기 / PG정산완료" 구분 표기.
- **BP-3 현금영수증 이중발급 방지(추가)**: PG 가상계좌 자동발급분은 `weeklyReceiptAudit`·발급 큐에서 **제외**(결제수단=PG 플래그로 식별). 우리 수동발급은 직접 계좌이체분만.
- **BP-4 콘텐츠 동기화(추가)**: 카드 가동 시 "계좌이체만·카드 불가" 문구를 계약서·약관·`api/_kb.js`·AI 상담사 facts에서 일괄 갱신(코드와 세트).
- **BP-5 환불=위약금 연동**: 환불 API는 전액 void가 아니라 기존 환불예상(기수령−위약금) 로직에 연결된 **부분취소**.
- **BP-6 PG 폴백**: 결제 호출부를 PG-추상화 → 토스 심사 반려 시 나이스페이먼츠로 교체 가능하게. 폴백 메모 유지.

### Pass 2 — 고객 입장 디테일 · 2026-06-30
- **CX-1 2갈래 단순화**: 손님 화면은 ①"계좌로 보내기"(가상계좌=자동확인을 기본·'가상계좌' 용어 최소화) ②"카드·간편결제" 두 갈래만. 4수단 나열 금지.
- **CX-2 금액 근거 한 줄**: 예약금 차감·묶음 합계 시 "왜 이 금액인지" 한 줄 표기.
- **CX-3 가상계좌 장점 안내**: "이름 안 적어도 자동으로 확인돼요"(입금자명 불일치 지연 제거).
- **CX-4 결제 후 안심**: 즉시 성공 피드백 + 상태 즉시 갱신. 가상계좌="입금하시면 자동 확인 · 따로 알려주실 필요 없어요".
- **CX-5 입금대기 카드 UX**: 계좌번호·은행·금액·기한 또렷이 + 번호 복사 + 자동확인 안내 + 기한 임박 리마인드.
- **CX-6 막다른 길 방지**: 카드 실패/취소/한도초과 시 "계좌로 보내기" 대체 경로 항상 노출 + 실패 문구에 다음 행동.
- **CX-7 앱 복귀 끊김 방지**: 복귀 즉시 "결제 확인 중…" 로딩→결과(세션 유지).
- **CX-8 할부 안내**: 카드 경로 "무이자 할부 2~6개월 가능(카드사별)" 한 줄(우리 부담 없음 명시).
- **CX-9 현금영수증 혼동 제거**: 카드 경로 현금영수증 칸 숨김 · 가상계좌 "현금영수증 자동 발급" 안내.
- **CX-10 고액 신뢰 신호**: "안전하게 처리돼요"(토스 보안·에스크로 마크·사업자정보) 절제되게.
