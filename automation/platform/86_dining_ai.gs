/**
 * Moment Edit · 86_dining_ai.gs — [폐기 2026-06-16]
 * ──────────────────────────────────────────────────────────────────────────
 * 다이닝 자연어→필터 추출은 애프터 웨딩 컨시어지(Vercel /api/after-concierge)로 일원화됨.
 * 옛 GAS 경유 추출(handleDiningMatch · diningMatch 액션)은 프런트에서 호출하지 않아 제거.
 * (남겨두면 음식 enum을 세 곳에서 동기화해야 해 드리프트 위험 → 단일 출처로 단순화)
 *
 * 이 파일은 GAS 프로젝트의 파일 슬롯만 유지(코드 없음). 다음 정리 때 삭제해도 무방.
 */
