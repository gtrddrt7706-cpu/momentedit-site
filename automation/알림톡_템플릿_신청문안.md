# 알림톡 템플릿 신청 문안 (솔라피 등록용) — v2 (2026-06-12 단계별 심리·전환 최적화 · 2026-09-25 T20~T22 추가)

---

## ★ 지금 할 일 (2026-09-25 · notifySetupCheck 실측) ★

`95_notify` 파일의 `notifySetupCheck` 결과(2026-09-25 18:56): **켜진 고객 알림 19종 · 알림톡 템플릿 있음 15 · 없음 4.**

- **있음 15** (건드릴 필요 없음): T01 상담확정 · T02 하루전 · T03 시간변경 · T04 시착동의서 · T05 계약서 · T08 중도금(2) · T09 잔금(2) · T10 결과물 · T13 임시고정만료 · T14 변경적용 · T15 변경보류 · T17 상담완료 · T18 계약금확인
- **없음 4 → 이번에 신청할 템플릿 4개** (문안은 아래 「1.」에 있음)

| 번호 | 알림 | 언제 나가나 | 버튼 |
|---|---|---|---|
| **T19** | `cust.couponIssued` 후기 감사 선물 | 관리자가 커피 쿠폰(바코드)을 처음 발급할 때 | 마이페이지 열기 |
| **T20** | `cust.resultOriginal` 원본 도착 | 관리자가 원본 링크를 올려 «원본전달»이 될 때 | 보정 컷 고르기 |
| **T21** | `cust.resultRetouch` 보정본 도착 | 보정본을 올려 «컨펌대기»가 될 때 · 수정 반영 후 다시 올릴 때 | 보정본 확인 |
| **T22** | `cust.refundAcctReq` 환불 계좌 요청 | 관리자가 취소 처리했는데 받은 돈이 있고 환불 계좌가 비어 있을 때 | 환불 계좌 남기기 |

### ✅ 2026-09-25 저녁 진행 결과 [SOLAPI_WEBHOOK_ON]
- **T19~T22 4개 등록 · 검수 진행 중**(검수자 참고 의견 · 대체발송 안함 · 버튼 외부 브라우저 끔).
- **기존 13개 실발송 확인** — `95_notify` 의 `testKakaoAll` → 솔라피 접수 13 · 실패 0 · 관리자 폰 도착 13통.
- **전달결과 웹훅 등록** — 그전엔 «현재 0개»였다(전달 실패 → 이메일 대체가 한 번도 안 돌았다). 개발 → Webhooks → 새로운 웹훅 생성 · EVENT «메시지 리포트» · 수신 URL = `https://momentedit.kr/api/solapi-report`(베르셀 중계 · 처음엔 GAS /exec 에 바로 걸었다가 302 답 때문에 솔라피 «웹훅 실패 알림»이 와서 바꿨다) · Secret 비움. `testKakaoT17` 뒤 시트 «알림톡추적» F열 «완료»로 확인.
- 남은 것: 승인 문자가 오면 `importKakaoTemplates` 한 번.

### 신청 순서 (솔라피 콘솔)
1. 알림톡 → 템플릿 등록. 공통 설정은 아래 「등록 공통 설정」 표 그대로.
2. **템플릿 이름은 반드시 번호로 시작** — `T19 후기 감사 선물` · `T20 원본 도착` · `T21 보정본 도착` · `T22 환불 계좌 요청`.
   승인 뒤 불러오기(`importKakaoTemplates`)가 **이름의 T 번호로** 어느 알림인지 찾는다. 번호가 없으면 승인나도 안 이어진다.
3. 본문을 코드블록 그대로 붙여넣는다(`#{…}` 변수는 자동 인식). **변수 이름을 바꾸지 말 것** — 코드가 그 이름으로 값을 넣는다.
4. 버튼: 웹링크(WL) 1개 · 모바일/PC 둘 다 `https://momentedit.kr/mypage.html` · 이름은 위 표.
5. 검수 요청.

### 승인난 뒤 (코드 수정 없음 · 1분)
0. **전제 — 2026-09-25 판 `95_notify.gs` 가 GAS 에 붙어 있어야 한다**(붙여넣기 + 새 버전 재배포 · 확인은 `99_deployCheck` 파일 → `deployCheck`).
   그 전 판의 불러오기는 T20~T22 번호를 모르고, **이어진 매핑을 통째로 덮어쓴다.**
1. `95_notify` 파일 열고 → `importKakaoTemplates` 실행 — 솔라피에서 승인 템플릿을 불러와 **더한다.** 이미 이어진 15건은 그대로 둔다(2026-09-25 부터 덮어쓰지 않음 · TPL_KEEP).
   검수 중인 것은 건너뛴다 — 하나씩 승인날 때마다 다시 실행해도 된다.
2. 같은 `95_notify` 파일 → `notifySetupCheck` 실행 → 「있음 19 · 없음 0」이면 끝.

### 승인 전까지는
이 4종은 **알림톡 대신 고객 이메일로** 나간다. 알림톡을 못 보낸 사실은 고객 상세 처리이력 한 줄 + 관리자 메일(하루 한 통)로 드러난다(TPL_SILENT).
이메일도 없는 고객이면 그 고객 이름으로 따로 관리자 메일이 간다 — 그때는 직접 연락이 필요하다.

---

## 지난 기록 (2026-06-12 ~ 06-27)

### ✅ 완료된 것 (건드릴 필요 없음)
- 카카오톡 채널 '모먼트에디트'(@momentedit) 개설 + **비즈니스 채널 심사 통과**(2026-06-12)
- 솔라피 가입(구글 gtrddrt7706) · 발신번호 010-7349-7706 등록(번호인증 만료 2026-12-11 → 그때 재인증 1회)
- API Key 발급 · GAS `R3n9Mr` `95_notify.gs` 반영 + 재배포 · **전 알림 SMS 실발송 중**
- **솔라피 채널 연동 완료(2026-06-12)** → `SOLAPI_PF_ID = KA01PF260612100003783HfQEob2cqJX` 스크립트 속성 저장 완료
- 일일 발송 한도 **200건 승인됨**
- 스크립트 속성: `NOTIFY_ENABLED` `SOLAPI_API_KEY` `SOLAPI_API_SECRET` `SOLAPI_SENDER` `ADMIN_PHONE` `SOLAPI_PF_ID` 전부 입력됨

### 📌 검수 결과 (2026-06-27 확인 · 06-15 재신청분)
- 🟢 **통과 6종**: T04 시착동의서 · T05 계약서 · T08 중도금 · T09 잔금 · T10 결과물 · T15 변경보류 → 승인 ID 매핑하면 바로 알림톡 전환
- 🔴 **반려 5종(보완 재신청)**: T01 상담확정 · T02 하루전 · T03 시간변경 · T13 임시고정만료 · T17 상담완료
  - **공통 반려 사유**: "수신 대상을 명확히 확인하기 어려움 → 어떤 액션으로 발송되는지 **고정값으로 추가**(신청하신·요청하신·예약하신·계약하신 등)". T03은 추가로 **다발성 발송 근거** 한 줄도 요구.
  - **해결**: 각 본문 맨 아래에 아래 1·T03·T13·T17 블록에 넣어둔 `본 메시지는 … 발송되는 정보성 메시지입니다.` 고정문구를 그대로 붙여 재신청. 변수 안 늘어 코드 수정 불필요.
- ⚪ **T16 보관 만료임박**: 06-23부터 카톡 끔(메일 전용 = `archiveExpiring` off:true). **재신청 불필요**(방치/삭제 OK).

### ⏳ 추가 등록 2종 (06-23 이후 코드에 생긴 알림 · 콘솔에 없음)
- **T14 예식일 변경 적용**(`cust.changeConfirmed`): 06-23 발송 켬. 문안 아래 "1. T14"에 있음 → **콘솔 등록만**(코드 수정 불필요).
- **T18 계약금 입금→제작/촬영**(`cust.depositToProduction`): 06-23 신규. 문안 아래 "1. T18"에 있음. `95_notify.gs`에 `#{안내}` 변수 추가 완료(재배포 필요).

### ⏳ 매핑(승인 후)
1. 검수 승인되면 템플릿 ID(KA01TP…)를 "2. 승인 후 매핑" JSON에 채워 스크립트 속성 `KAKAO_TEMPLATES` 저장 → `95_notify` 파일 열고 `notifyTestCustomerByCode('테스트코드')` 1회 → 알림톡 수신 확인.
- ※ 매핑은 코드 수정·재배포 불필요(속성만). 승인된 것부터 부분 매핑 가능(나머지는 SMS 유지).

### 메모
- **[TODO·사업장 확정되면]** T01(상담 확정)·T02(하루 전)에 상담 장소 안내 줄 추가(현재는 장소 데이터가 없어 보류) — 템플릿 수정 재등록+재검수 필요
- 솔라피 잔액 210원 → **1만원 충전 권장**
- API Secret이 작업 채팅에 노출된 적 있음 → 마음 쓰이면 재생성+속성 갱신(세트로)
- 법인카드 교체·070 전환 등 '나중에' 항목은 → 루트 **`나중에할일_체크리스트.md`로 통합**(2026-06-12)

---

> **v2 문안 원칙**: 본문·변수는 `automation/platform/95_notify.gs`(문자 대체 문구)와 1:1.
> 본문을 한 글자라도 바꾸면 승인 템플릿과 코드가 어긋나니, 수정은 코드와 함께.
> 카카오 심사 기준(정보성)에 맞춰 광고 문구·가짜 희소성 없이, 각 단계의 고객 심리에 맞는 사실 기반 문장만 사용.

## 등록 공통 설정 (모든 템플릿 동일)

| 항목 | 값 |
|---|---|
| 채널 | @momentedit |
| 카테고리 | 기타(999999) 그대로 OK (심사에 영향 없음) |
| 템플릿 유형 | **기본형** |
| 부가정보·이미지 | 비움 |
| 보안 템플릿 | 사용 안 함(OFF) |
| 변수 | 본문 붙여넣으면 `#{…}`가 자동 인식됨('변수 추가' 버튼 불필요) |
| 버튼 | 있는 템플릿만 **웹링크(WL)** 1개 · 모바일/PC URL 둘 다 `https://momentedit.kr/mypage.html` |

---

## 1. 등록 템플릿 (콘솔에 그대로 복붙)

### T01 · 상담 확정 — `cust.consultConfirmed`
심리: 결정 직후 안심+단독 예우(한 타임 한 팀을 사실로 전달) → 노쇼 억제·기대 상승. #{유형}=상담/촬영(스냅) 자동
```
#{이름}님, 모먼트에디트 #{유형} 일정이 확정되었습니다.

· 일시: #{일시}

이 시간은 두 분만을 위해 비워둡니다. 일정 변경이 필요하시면 마이페이지에서 변경하실 수 있어요. 편안한 마음으로 오시면 됩니다.

본 메시지는 모먼트에디트에 일정을 예약하신 고객님께 확정 안내로 발송되는 정보성 메시지입니다.
```
버튼: `마이페이지 열기`
※ 2026-06-27 반려 보완: 맨 아래 '본 메시지는…' 고정문구 추가(수신 대상 명확화).

### T02 · 상담 하루 전 — `cust.consultDayBefore`
심리: 부담 제거+환대 → 출석률·첫인상. #{유형}=상담/촬영 자동(스냅은 신랑 정장 직접 준비라 '준비물 없음' 단정 제거)
```
#{이름}님, 내일 모먼트에디트 #{유형}이 예정되어 있어요.

· 일시: #{일시}

두 분은 편안히 오시면 됩니다. 맞이할 준비는 저희가 해두겠습니다. 내일 뵙겠습니다.

본 메시지는 모먼트에디트에 일정을 예약하신 고객님께 하루 전 안내로 발송되는 정보성 메시지입니다.
```
버튼: 없음
※ 2026-06-27 반려 보완: 맨 아래 '본 메시지는…' 고정문구 추가.

### T03 · 상담 시간 변경 제안 — `cust.timeProposed`
심리: 주도권 돌려주기(수락/다른 시간)+빠른 응답의 이득 명시 → 일정 확정률. #{유형}=상담/촬영 자동
```
#{이름}님, 모먼트에디트에서 #{유형} 시간 변경을 제안드렸어요.

· 제안 일시: #{일시}

마이페이지에서 수락하시거나 더 편한 시간을 선택하실 수 있어요. 확인해 주시면 그 시간으로 바로 확정해 드릴게요.

본 메시지는 모먼트에디트에 일정을 예약하신 고객님께, 예약하신 시간 진행이 어려워 변경 제안이 필요할 경우 발송되는 정보성 메시지입니다.
```
버튼: `마이페이지에서 확인`
※ 2026-06-27 반려 보완: 수신 대상 + 다발성 발송 근거를 한 문장 고정문구로 추가(반려문이 둘 다 요구).

### T04 · 시착 동의서 도착 — `cust.fittingRequest`
심리: '비용?' 불안 선제 차단(기본 2벌 포함을 먼저)+절차 투명성 → 서명 거부감 완화. ※2026-06-13 v4 정책(시착 2벌·5만원) 반영
```
#{이름}님, 드레스 시착 동의서가 도착했어요.

드레스 시착은 기본 2벌까지 포함되어 있어요. 진행 기준을 담은 동의서를 마이페이지에서 확인하신 뒤 서명해 주세요. 서명 후 시착이 진행됩니다.
```
버튼: `동의서 확인·서명`

### T05 · 계약서 도착 — `cust.contractArrived`
심리: 전환의 순간. 일관성 신호("안내드린 그대로" — 스냅은 상담 없이 발송될 수 있어 중립)+차분한 검토 권유, 기한은 사실로 명확히 → 서명 전환
```
#{이름}님, 모먼트에디트 이용계약서가 도착했어요.

안내드린 내용 그대로 담았습니다. 마이페이지에서 차분히 살펴보신 뒤 서명해 주세요.

· 서명 기한: 도착 후 72시간 (기한이 지나면 계약서는 자동 파기됩니다)
```
버튼: `계약서 확인·서명`

### T08 · 중도금 안내 — `cust.midPre` · `cust.midDue` 공용
심리: 돈 얘기는 예측 가능하게+절차 단순화(버튼 한 번) → 이행률, 압박감 최소
```
#{이름}님, 중도금 일정을 안내드립니다. (예식 D-#{디데이})

금액과 입금 계좌는 마이페이지에 정리해 두었어요. 입금하실 때 입금자명을 남겨주시면 확인이 더 빨라요. 궁금한 점은 언제든 문의 주세요.
```
버튼: `금액·계좌 확인`

### T09 · 잔금 안내 — `cust.balancePre` · `cust.balanceDue` 공용
심리: 결제를 '완성 단계'로 리프레이밍(준비 마무리 중 안심 한 줄). #{행사}=예식/촬영(스냅) 자동
```
#{이름}님, 잔금 일정을 안내드립니다. (#{행사} D-#{디데이})

남은 준비는 저희가 차근차근 마무리하고 있습니다. 금액과 입금 계좌는 마이페이지에 정리해 두었어요. 입금자명을 남겨주시면 확인이 더 빨라요.
```
버튼: `금액·계좌 확인`

### T10 · 결과물 전달 — `cust.resultDelivered`
심리: 여정의 정점(peak). 감정 중심+"언제까지"를 구체 숫자로(계약서 12조③ 6개월) → 막연함 제거
```
#{이름}님, 두 분의 시간이 담긴 결과물이 준비되었습니다.

마이페이지에서 바로 확인하실 수 있어요. 결과물은 전달일부터 6개월 동안 보관돼요. 그 안에 다운로드하시고, 오래 두고 보실 곳에도 꼭 옮겨 보관해 주세요.
```
버튼: `결과물 확인`

### T13 · 임시고정 만료 임박 — `cust.holdExpiring`
심리: 손실 회피(사실 그대로의 기한·자동 해제)+유지 방법 명확 → 상담 확정 전환
```
#{이름}님, 예식일 임시고정 만료가 #{남은일}일 남았어요.

· 일시: #{일시}

기간이 지나면 자리는 자동으로 풀립니다. 상담을 확정하시면 이 자리가 그대로 유지돼요.

본 메시지는 모먼트에디트에 예식일 임시고정을 요청하신 고객님께 만료 안내로 발송되는 정보성 메시지입니다.
```
버튼: `상담 확정하러 가기`
※ 2026-06-27 반려 보완: 맨 아래 '본 메시지는…' 고정문구 추가.

### T15 · 예식일 변경 보류 — `cust.changeDeclined`
심리: 거절을 '함께 다른 답 찾기'로(사과+사유 투명+대안 경로)
```
#{이름}님, 죄송하게도 요청하신 예식일 변경을 진행하지 못했습니다.

· 사유: #{사유}

마이페이지에서 다른 일정으로 다시 요청하실 수 있어요. 가능한 방향을 함께 찾아보겠습니다.
```
버튼: `다시 요청하기`

### T16 · 결과물 보관 만료 임박 — `cust.archiveExpiring`
심리: 잊힌 일 리마인드+사실 기반 손실 회피(계약서 12조③: 만료 후 삭제 가능)
```
#{이름}님, 결과물 보관 기간이 곧 끝나요.

· 보관 만료일: #{만료일}

만료 후에는 파일이 삭제될 수 있어요. 마이페이지에서 미리 다운로드해 보관해 주세요.
```
버튼: `지금 다운로드`

### T17 · 상담 완료 · 계약 진행 안내 — `cust.consultDone` (2026-06-12 신설)
심리: 상담 직후 감사(따뜻한 마무리)+다음 행동 단일 명확화 — 이 알림이 없으면 상담완료 후 고객이 뭘 해야 하는지 몰라 여정 정체
```
#{이름}님, 진행하신 상담이 완료되었습니다. 함께해 주셔서 감사합니다.

다음 단계는 마이페이지에서 예식일과 기본 정보를 입력해 주시면 진행되며, 확인 후 이용계약서를 보내드립니다.

본 메시지는 상담을 완료하신 고객님께 발송되는 정보성 안내입니다.
```
버튼: `다음 단계 입력`
※ 2026-06-29 재반려 보완: 첫 문장을 수신자 액션 고정값('진행하신 상담이 완료되었습니다')으로 명확화 · 요청형('요청해 주세요')→정보형('입력해 주시면 진행되며')으로 수정 · 버튼 '계약 진행하기'→'다음 단계 입력'.

### T18 · 계약금 입금 확인·다음 단계 — `cust.depositToProduction` (2026-06-23 신설)
심리: 결제 직후 안심+다음 행동 단일 명확화. #{안내}=시그(제작 정보 입력)/스냅(촬영 준비) 코드 자동 분기
```
#{이름}님, 계약금 입금이 확인되었습니다.

#{안내}

본 메시지는 모먼트에디트와 계약을 진행하신 고객님께 입금 확인 후 다음 단계 안내로 발송되는 정보성 메시지입니다.
```
버튼: `마이페이지 열기`

### T19 · 후기 감사 선물 — `cust.couponIssued` (2026-08-18 신설 · CPN_NOTIFY · ★2026-09-25 신청 대상)
심리: 약속의 마무리. 화면은 «준비되면 위쪽에 바코드가 떠요»라고만 말해 두고 끝나서,
두 분이 마이페이지를 다시 열어보지 않으면 받은 줄을 모른다. 그 문장을 완성하는 한 통.
★바코드 이미지는 문자로 보내지 않는다 — 마이페이지 안에서만 연다(유출·오전달 방지).
```
#{이름}님, 후기 감사합니다.

마음을 담아 #{선물}을 마이페이지에 올려 두었어요.
바코드를 매장에서 보여주시면 됩니다.

· 사용기한: #{기한}

본 메시지는 모먼트에디트 후기를 남겨주신 고객님께 감사 선물 안내로 발송되는 정보성 메시지입니다.
```
버튼: `마이페이지 열기`
※ `#{기한}` 은 관리자 화면이 쿠폰을 발급할 때 **반드시** 받는다(날짜 형식이 아니면 발급 버튼이 막힌다) — 빈 값으로 나가는 길이 없다.
※ `#{선물}` 기본값은 「스타벅스 커피 2잔」(관리자가 바꿀 수 있다).

### T20 · 원본 도착 — `cust.resultOriginal` (★2026-09-25 신청 대상 · RESULT_NOTIFY_STEPS)
심리: 기다리던 사진의 첫 도착. 다음 행동은 하나(보정할 컷 고르기)만 — 마이페이지 결과물 카드의 문장(「보정할 컷을 골라 주세요」)과 같다.
```
#{이름}님, 원본 사진이 도착했습니다.

마이페이지에서 원본을 확인하시고, 보정할 컷을 골라 주세요.

본 메시지는 모먼트에디트와 계약하신 고객님께 원본 사진 도착 안내로 발송되는 정보성 메시지입니다.
```
버튼: `보정 컷 고르기`
※ 관리자가 원본 링크를 처음 올려 결과물 상태가 «원본전달»로 바뀔 때 1회(링크만 고쳐 다시 저장하면 안 나간다).

### T21 · 보정본 도착 — `cust.resultRetouch` (★2026-09-25 신청 대상 · RESULT_NOTIFY_STEPS)
심리: 마무리 직전. 「마음에 드시면 확인」 — 마이페이지 확인 카드의 문장과 같아 알림과 화면이 같은 말을 한다.
```
#{이름}님, 보정본이 도착했습니다.

마이페이지에서 받아보시고, 마음에 드시면 확인을 눌러 마무리해 주세요.

본 메시지는 모먼트에디트와 계약하신 고객님께 보정본 도착 안내로 발송되는 정보성 메시지입니다.
```
버튼: `보정본 확인`
※ 보정본을 올려 «컨펌대기»가 될 때, 그리고 고객 수정 요청을 반영해 다시 올릴 때도 나간다 — 두 경우 모두 맞는 문장으로 썼다.
※ 고정문구를 «보정 컷을 고르신 고객님께»로 쓰지 않았다 — 고객이 고르기 전에 관리자가 보정본을 올리는 경우도 있다(mypage 결과물 카드 주석).

### T22 · 환불 계좌 요청 — `cust.refundAcctReq` (★2026-09-25 신청 대상 · REFUND_ACCT_REQ)
심리: 취소 뒤에도 받은 돈은 돌려드린다는 사실 + 할 일 하나(계좌 남기기). 마이페이지 「환불 계좌 안내」 카드로 이어진다.
```
#{이름}님, 예약이 취소되었어요.

환불 계좌를 남겨주시면 입력하신 계좌로 환불해 드릴게요. 마이페이지에서 남겨주세요.

본 메시지는 모먼트에디트에 예약하신 고객님께, 예약이 취소되어 환불 계좌가 필요한 경우 발송되는 정보성 메시지입니다.
```
버튼: `환불 계좌 남기기`
※ 관리자 쪽 취소(상담 취소·이메일 취소·강제 취소)에서만 나간다. 고객이 마이페이지에서 스스로 취소할 때는 계좌를 먼저 받으므로 안 나간다.
※ 받은 돈(예약금 또는 계약금)이 없으면 안 나간다 — 돌려드릴 것이 없다.

### T14 · 예식일 변경 적용 — `cust.changeConfirmed` (2026-06-23 발송 켬 · 등록 필요)
심리: 요청 수용 확인+후속 정리까지 해뒀다는 케어 신호
```
#{이름}님, 요청하신 예식일 변경이 적용되었습니다.

· 새 일시: #{일시}

이후 일정과 안내는 모두 새 날짜 기준으로 정리해 두었어요. 마이페이지에서 확인하실 수 있습니다.

본 메시지는 모먼트에디트에 예식일 변경을 요청하신 고객님께 적용 안내로 발송되는 정보성 메시지입니다.
```
버튼: `마이페이지 열기`

---

### ❌ 발송 안 함 4종 — 등록하지 마세요 (2026-06-12 사용자 결정)

> ※ T14 예식일 변경 적용은 2026-06-23 발송 켬 → 위 "1. T14"로 이동(등록 대상). T16 보관 만료임박도 06-23 카톡 끔(메일 전용)이라 등록 불필요.

"없으면 진행이 막히는 알림"만 남기기로 결정 → 아래 4종은 코드에서 발송 차단(`95_notify.gs` NOTIFY_EVENTS의 `off: true`).
복구하려면: ① 해당 줄에서 `off: true,` 삭제 + 재배포 ② 아래 문안으로 템플릿 등록 ③ KAKAO_TEMPLATES에 키 추가.

### T06 · 입금 확인 — `cust.paymentConfirmed`
심리: 결제 직후 후회 방지(즉시 확인+감사+다짐) → 취소 방지·만족
```
#{이름}님, #{항목} 입금이 확인되었습니다. 감사합니다.

받은 마음 그대로 정성껏 준비하겠습니다. 자세한 결제 내역은 마이페이지에서 언제든 확인하실 수 있어요.
```
버튼: `결제 내역 보기`

### T07 · 현금영수증 발행 — `cust.cashReceiptIssued`
심리: 사무 처리의 정확함=신뢰 굳히기(꾸밈 없이 정확하게)
```
#{이름}님, #{항목} 현금영수증이 발행되었습니다.

· 금액: #{금액}원

승인번호와 발행 내역은 마이페이지에서 확인하실 수 있어요.
```
버튼: `마이페이지 열기`

### T11 · 임시고정 승인 — `cust.holdGranted`
심리: '찜 성공' 안도+기한·다음 행동을 한 번에
```
#{이름}님, 예식일 임시고정이 승인되었습니다.

· 일시: #{일시}

14일 동안 이 자리는 두 분을 위해 비워둡니다. 상담에서 확정하시면 그대로 이어져요.
```
버튼: `마이페이지 열기`

### T12 · 임시고정 해제 — `cust.holdReleased`
심리: 상실 순간의 관계 유지(문 열어두기·정중) → 재방문 여지
```
#{이름}님, 예식일 임시고정이 해제되었습니다.

· 일시: #{일시}

마음이 정해지시면 마이페이지에서 언제든 다시 요청하실 수 있어요. 두 분의 시간에 맞춰 기다리겠습니다.
```
버튼: `마이페이지 열기`

---

## 2. 승인 후 매핑 — `KAKAO_TEMPLATES`

**보통은 불러오기 한 번이면 끝난다** — `95_notify` 파일 열고 → `importKakaoTemplates` 실행.
솔라피에서 **승인된** 템플릿을 불러와 이름의 T 번호(T01~T22)로 알림에 잇고, 이미 이어진 매핑은 지우지 않는다(2026-09-25 부터 · TPL_KEEP).
공용 템플릿(T08·T09)은 번호 하나가 두 알림(pre·due)에 함께 이어진다.

★**Script Properties 의 `KAKAO_TEMPLATES` 를 손으로 고치지 말 것.** 따옴표 하나만 틀려도 JSON 을 못 읽어 **이어진 매핑 전부가 한꺼번에 끊긴다**
(코드가 못 읽은 값을 «매핑 0건»으로 읽는다). 하나만 넣거나 빼야 하면 `addKakaoTemplate(이벤트, 템플릿ID)` — 인자를 넘겨야 하니 GAS 편집기에서는 래퍼 함수가 필요하다(`addT17` 참고).

참고 — 켜진 고객 알림 전체 키(19개 · 발송 안 함 4종 T06·T07·T11·T12 와 메일 전용 T16 은 제외):
```json
{"cust.consultConfirmed":"KA01TP...","cust.consultDayBefore":"KA01TP...","cust.timeProposed":"KA01TP...","cust.fittingRequest":"KA01TP...","cust.contractArrived":"KA01TP...","cust.depositToProduction":"KA01TP...","cust.midPre":"KA01TP같은ID","cust.midDue":"KA01TP같은ID","cust.balancePre":"KA01TP같은ID2","cust.balanceDue":"KA01TP같은ID2","cust.resultDelivered":"KA01TP...","cust.consultDone":"KA01TP...","cust.holdExpiring":"KA01TP...","cust.changeConfirmed":"KA01TP...","cust.changeDeclined":"KA01TP...","cust.couponIssued":"KA01TP...","cust.resultOriginal":"KA01TP...","cust.resultRetouch":"KA01TP...","cust.refundAcctReq":"KA01TP..."}
```
빠진 알림은 알림톡 대신 **고객 이메일**로 나간다(문자는 2026-06-29 부터 안 쓴다) · 처리이력과 관리자 메일로 드러난다(TPL_SILENT).

---

## 3. 테스트 순서 (GAS 편집기에서)

1. `95_notify` 파일 열고 → `notifySetupCheck()` 실행 → 로그에서 설정 누락 · 템플릿이 빠진 알림(이름으로) 확인
2. `95_notify` 파일 → `notifyTestAdminSms()` 실행 → 디렉터 폰에 문자 오면 솔라피 연동 성공(수동 점검 전용)
3. 같은 `95_notify` 파일 → `testKakaoAll` 실행 → 이어진 템플릿을 ADMIN_PHONE 으로 1건씩 카톡 테스트(`notifyTestKakaoAll`)
4. 이후 실제 흐름(상담 확정 등)에서 자동 발송 — 실행 로그에 `[notify] 발송 OK`가 남음

※ 관리자 알림(입금신호·변경요청 등)은 **메일 전용**이다(2026-06-29 · 문자비 0). 템플릿 승인과 무관하다.
※ 발송 실패는 본 흐름(계약·입금 처리)을 절대 막지 않는다 — 고객 상세 처리이력에 한 줄이 남는다.
※ 발송 시각: 21시~익일 8시 사이 고객 알림은 자동 보류 후 아침 8시에 발송(야간 방해 금지 설계).
