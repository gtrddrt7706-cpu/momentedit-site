# 출처 — frontend-design

- **원천**: https://github.com/anthropics/skills (`main` · `skills/frontend-design/SKILL.md`)
- **받은 날**: 2026-09-25
- **라이선스**: ★**저장소에 라이선스 파일이 없었다**(실측 2026-09-25 — 루트 `LICENSE`·`LICENSE.md`·
  `NOTICE` 전부 404 · 스킬 폴더 안에도 없음 · README 에 licence/copyright 문구 0건).
  Anthropic 공개 저장소이고 impeccable README 가 «이 스킬에서 시작했다»고 밝히며 공개 참조하지만,
  **명시된 허가가 없다.** 저장소가 라이선스를 달면 이 줄을 고치고 `LICENSE` 를 동봉한다.
- **받은 방법**: `raw.githubusercontent.com` — ★경로는 `skills/frontend-design/SKILL.md` 다.
  `frontend-design/SKILL.md`(앞에 `skills/` 없이)는 404 다. 두 번 헤맨 자리라 적어 둔다.

## 이 스킬의 자리

impeccable 이 «여기서 시작했다»고 적은 원조다(impeccable README 9행).
겹치는 주제가 많아 **셋을 동시에 근거로 쓰지 않는다** — 순서는 `CLAUDE.md` 의
`[DESIGN_SKILLS_12]` 가 정한다. 이 스킬은 «새 화면의 미감 방향»에서만 쓴다.
