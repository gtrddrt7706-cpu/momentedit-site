# 실행 로드맵 — 디테일~큰틀 전부 (턴키)

**원칙:** *직접 수정(라이브 동작 변경)* 전, **셋팅·준비 가능한 건 전부 완료**해 둠. 아래 배치별로 "go" 주면 즉시 실행.
**참고 문서:** `REPORT_mypage_review.md` · `REPORT_admin_mypage_2차검토.md` · `REPORT_아키텍처_검토.md` · `PLAN_잔금_여정_설계.md` · `SETUP_clasp_배포자동화.md`

---

## §1. 이번에 준비 완료 (inert — 라이브 영향 0)
- ✅ `shared/tokens.css` — 디자인 토큰 정본(각 페이지 :root 통합용, 아직 미연결)
- ✅ `shared/core.js` — 공통 유틸(api·esc·qp·copy·token, `window.ME` 격리, 아직 미연결)
- ✅ `.clasp.json` · `.claspignore` — GAS 배포 자동화 스캐폴드(R3n9Mr만 push하도록 제외목록 포함)
- ✅ `SETUP_clasp_배포자동화.md` — 안전 셋업 가이드(pull→비교→push)
- ✅ 본 로드맵

> 위 파일들은 **아무 페이지도 아직 안 씀** → 지금 main에 있어도 라이브는 그대로.

---

## §2. 직접수정 대기 (배치별 — "go" 시 실행)

### 배치 A — 프런트 디테일·신뢰 (저위험 · Vercel 자동)
| 항목 | 내용 | 파일 |
|---|---|---|
| A1 | 핀치 줌 허용(`user-scalable=no` 제거) — mypage·admin 둘 다 | mypage.html·admin.html |
| A3 | 네트워크 끊김 시 캐시 화면 유지(로그아웃 방지) | mypage.html |
| A4 | 로그인 화면에 "상담 신청" 출구 링크 | mypage.html |
| A5 | 카톡 미설정 시 이메일 문의 폴백 | mypage.html |
| B4 | 관리자 큐·검색 행에 예식 D-day 배지 | admin.html |
| B5 | 관리자 입금 카드에 계약금/잔금 금액 분해 표시 | admin.html |
| — | 작은 회색 글씨 대비(이미 mypage 적용 → admin도 동일) | admin.html |
> 한 번에 반영 가능. (참고: REPORT_mypage_review·REPORT_admin_mypage_2차검토)

### 배치 B — 프런트 공통화 적용 (C3 wiring · 저위험)
- 각 페이지에 `shared/tokens.css`·`shared/core.js` 연결 + 인라인 중복 제거.
- *주의:* 한 페이지씩 전환·검증(토큰 값 동일 확인). 드리프트 제거 효과.

### 배치 C — 기능(백엔드+프런트 · R3n9Mr 재배포 필요)
| 항목 | 내용 | 파일 |
|---|---|---|
| **B1** | **관리자에 다이닝·식순 표시**(출시했는데 "준비중"으로 스테일) — adminDetail mirror에 dining/ritual 추가 + 카드 렌더 | admin.gs(재배포)·admin.html |
| **잔금** | 잔금 결제 마일스톤 전체(고객 카드·관리자 배지/확인·D-7 리마인드) | `PLAN_잔금_여정_설계.md` 그대로 |
| A2 | 개인코드 마이페이지 노출(숨김 의도 확인 후) | mypage.html |

### 배치 D — 구조 (큰틀)
| 항목 | 내용 | 누가 |
|---|---|---|
| **C1** | clasp 배포 자동화 — 스캐폴드 완료, **너가 1회** `clasp login`/pull/push | 너(가이드대로) |
| C2 | 청첩장 데이터 단일 출처(Customers↔Couples 중복·이중 경로 통합) — *설계 먼저* | 나(설계doc) → 구현 |
| C4 | 조회/트리거 필드 컬럼 승격(예식일 등) — 잔금 구현에 포함 | 잔금 배치에 흡수 |
| C5 | 경량 CI(node --check + clasp push) | 선택 |

---

## §3. 추천 실행 순서 (ROI·안전 순)
1. **배치 A**(프런트 디테일) — 즉시 체감·저위험. ["A 해줘"]
2. **C1 clasp 셋업** — 이후 모든 백엔드 반영이 쉬워짐(가이드대로 너가 1회).
3. **배치 C의 B1(관리자 다이닝·식순)** — 출시 기능이라 운영 공백 큼. (clasp 있으면 반영 쉬움)
4. **잔금 전체** — PLAN 승인 후 한 번에(고객+관리자+트리거).
5. **C2 단일출처** — 설계 doc → 구현.
6. **배치 B(공통화)·C5(CI)** — 여유 될 때.

---

## §4. "go" 방법
- `"배치 A 해줘"` / `"B1 해줘"` / `"잔금 구현"` / `"C2 설계"` 처럼 집어주면 그 배치만 실행·반영(필요 시 GAS 파일·재배포 안내 동봉).
- clasp(C1)은 준비 끝 — 가이드(`SETUP_clasp_배포자동화.md`)대로 한 번만 돌려보고 막히면 알려줘.

> 정리하면: **준비는 다 됐고**, 라이브를 바꾸는 "직접 수정"만 네 신호를 기다리는 상태예요.
