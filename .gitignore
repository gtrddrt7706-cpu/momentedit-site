# 작업용 엑셀 — 열 때마다 '변경'으로 떠서 git 추적 제외 (파일은 PC에 그대로 남음)
*.xlsx
*.xls
~$*

# OS / 편집기 / 빌드 잡파일
.DS_Store
Thumbs.db
desktop.ini
*.bak

# 점검 도구 임시 설치(puppeteer 등) — 정적 사이트라 평소 의존성 없음. 실수 커밋 방지.
node_modules/
package-lock.json
package.json
