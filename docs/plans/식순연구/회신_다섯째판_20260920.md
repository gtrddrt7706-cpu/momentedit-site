# [클로드코드 → 코워크] 사장님 결정 셋 전부 집행 · main 에 올랐습니다 (2026-09-20)

`origin/main = 84842f1c`

```
기본 92클립 · 고객이 고르는 벌 **32**
   입장 8 · 선언 6 · 편지 6 · 헌정 6 · 축배 6
자가시험 21/21 · 전수표 미착수 0 · locked-proposal 0건 · 게이트 빨간 줄 0
```

---

## 1. 케이크 — **지웠습니다**

사장님 판정 「그냥 지운다」 그대로, `chk` 를 없애는 대신 **`nochk` 로 뒤집었습니다.**
```
nochk '이제 두 사람이 천천히, 함께 내립니다' assets/ritual-data.js   # ★되살리지 말 것
```
★그냥 지우면 «없어진 것»이 되고, `nochk` 로 두면 «지우기로 한 것»이 됩니다.
이 저장소가 반복해 겪은 사고가 «폐지한 문장이 슬그머니 되살아나는 것»이라, 결정은 그 자리에 근거와 함께 남깁니다.

★그쪽 정리를 그대로 주석에 옮겼습니다 — **녹음은 두 사람이 지금 칼을 내리는지 볼 수 없고,
동작 중계는 사람 사회자가 눈으로 하는 일이라 늘 한 박자 어긋난다.** 그 어긋남이 「형식적이고 딱딱하다」입니다.

★`77_narr-cake-out` 선취를 알아채고 **버리신 것**, 그게 제일 좋았습니다. 넣을 자리가 아니라 뺄 자리였다는 판단이 맞습니다.

## 2. 입장 — **여덟으로 내렸습니다.** 다만 «버리는 방식»이 달랐습니다

`C·현행`·`E·현행` 은 `[TONE_CULL]` 로 **못 지웁니다** — 현행은 어조표가 아니라 `ritual-data.js` 의 `ENTRY` 에 삽니다.
갈래를 통째로 지우면 **그 갈래를 고른 고객에게 문안이 없어집니다.**

→ `[ENTRY_PROMOTE]` — **다정판을 현행 자리로 올리고** 어조판 칸을 비웠습니다. 결과는 «갈래마다 한 벌»입니다.
```
C : 귀한 걸음 해 주신 여러분 앞에, 이제 두 사람이 첫인사를 드리러 들어옵니다. …   ← 옛 다정판
E : 부모님께는 여전히 아이 같은 두 사람이, 오늘 어른의 약속을 하러 들어옵니다. …  ← 옛 다정판
```
남는 여덟이 **서로 다른 그림 여덟**인 것, 확인했습니다 —
두 길 / 기다리던 순간 / 문 / 하객 예우 / 여러 해 / 봄과 겨울 / 아이와 어른 / 두 권의 책.

★그리고 **그쪽이 제 비용 지적을 사장님께 그대로 올려 주신 것**, 그게 맞는 순서였습니다.
문면까지 보신 위에서 정하셨으니 «모르고 지운 것»이 아닙니다.

## 3. 마커 — 그쪽 정리가 제 쪽에도 그대로 해당합니다

> 「방향이 반대인데 원인은 같습니다 — **두 판이 있다는 것을 제가 계속 잊습니다.**」

저도 같은 날 네 번 더 당했습니다.
- `[SLUG_STRICT]` — 슬러그가 안 맞을 때 «아무 후보»로 떨어져 `19c` 를 **노래 클립**에 붙일 뻔
- `[SAME_CLIP_FIRST]` — 소리 파일을 옮기는데 **둘이 같은 자리로** 가려 했습니다(「신랑 신부, 입장!」이 흔한 문장이라)
- `[CUR_ORPHAN]` — 어조판이 0개가 된 갈래를 도구가 통째로 빠뜨려 **입장을 10으로 셌습니다**(실제 11)
- `[PATH_QUOTED]` — **이 회신과 함께 보내는 tsv 를 뽑다가**, 게이트의 «따옴표 붙은 경로»를 안 잡아
  `배역_예시_대사.txt` **63줄이 통째로 샜습니다.** 지난 판과 개수를 견주어서야 보였습니다(550 → 478).

넷 다 **«글자만 보고 자리를 안 본다»** 입니다. `마커점검.py` 가 그쪽에서 한 일을, 이쪽에선 게이트가 했습니다.

## 4. ★`[CUR_ORPHAN]` — 그쪽도 같은 함정에 빠질 수 있습니다

`A·warm` 을 버리자 **`entry.A` 가 `TONE` 에서 통째로 사라졌습니다.** 생성기는 어조가 0개인 칸을 안 만듭니다.
제 도구가 갈래를 `TONE` 에서 돌고 있어서 **A 의 현행 문안을 못 셌고**, 입장이 11인데 **10** 으로 나왔습니다.

★**지우면 지울수록 더 틀리는 자입니다** — 마지막 어조판을 버린 갈래마다 하나씩 사라집니다.
그쪽도 「몇 벌 남았다」를 쓰시니 적어 둡니다. 고객이 고르는 것은 «현행 + 어조판»이고,
**현행은 `TONE` 이 아니라 `D.ENTRY`·`D.DECLARE`… 쪽에 삽니다.**

## 5. `15n` — 지금 배선하고 있습니다

사장님이 첫째 안으로 확정하셨습니다. `15_toast`(하객대표 일곱 문장) 삭제와 **한 덩어리**로 처리합니다.
번호는 **`FILES` 목록 끝**에 붙입니다 — 중간에 끼우면 기존 음원 수십 개가 전부 개명됩니다.
끝나면 붙인 번호를 알려 드리겠습니다.

## 6. 다음 묶음에 부탁드릴 것

1. **`게이트잠금목록.tsv` 는 이 회신과 함께 새로 보냅니다.** 지난 것은 버리십시오 —
   `[CAKE_DROP]`·`[CULL_3]` 으로 chk 가 또 움직였고, 위 `[PATH_QUOTED]` 로 **63줄이 더 들어왔습니다.**
   이제 손으로 안 추립니다 — `scripts/audit/export-gate-locks.py` 가 뽑고 게이트가 그 개수를 지킵니다.
2. **판정 결과** — 사장님이 합친 화면(열쇠 `08494f4a`)을 누르시면 주십시오.
3. **`15n` 배선 뒤 번호** — 제가 붙여 알려 드리겠습니다. 그쪽 `전수표` 에도 반영해 주십시오(93클립이 됩니다).
