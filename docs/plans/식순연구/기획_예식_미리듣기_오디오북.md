# 기획 · 예식 미리듣기 — 엔진 하나 · 쓰임 셋 · v0.6

> 2026-08-01 · **v0.6 — 트랙 2 제작 파이프라인 완성(§4-A). 대본 → 붙여넣기 파일 → 타입캐스트 → 조립기 → mp3. 사람 몫은 목소리 고르기 + 감정 다듬기 + 다운로드 1회로 줄었다.**
> 2026-08-01 · v0.5 — 런북 §11-A·§11-B 구현 완료. 덕담이 길어지면 마무리 문안이 저절로 바뀐다(ALT_CLIP) · 대본 54클립 · 성우 대리 낭독은 상품에서 뺀다.
> v0.4 = 기획이 코드가 됐다. 엔진·콘솔·검사 3파일 main 반영(`7ba065b`). §6 결정 1·10·12 승인 확정.
> v0.3.2 = 현장 콘솔을 '최소 조작'으로 재설계. 디렉터 조작 20회 → 10회(§3-A).
> v0.3 = 배역 음성 채택 · v0.3.1 = 콘솔 사양 신설(git `b25dec1`) · v0.2 = `23fe58a` · v0.1 = `67c60d6`. 무엇을 왜 뒤집었는지는 §1·변경 이력.
> 이 문서가 이 기능의 **단일 기획서**다. 소스 실측·비용·결정을 전부 여기에 메모해 가며 고쳐 나간다.
>
> 발단 — 사용자 제안 verbatim:
> "이런걸 우리 코스마다 읽어볼 수 있게 예 부분을 만들면 고객 입장에서 이미지 트레이닝과 코스 정하기 이해가 쉬울 거 같은데, 기술적으론 진짜 오디오북처럼 주인공 각각의 음성이 나올 수 있게 하고, 더 기술적으론 코스를 커스텀 완성하면 그거에 맞춰진 오디오북이 완성되면 더 좋을 거 같은데"

---

## 0. 한 줄 결론 (v0.3.2)

**클립 시퀀서 엔진 1개를 만들고, 세 곳에 쓴다.**
①**당일 디렉터 콘솔**(운영 필수 · 이것이 본체) ②코스 기본형 미리듣기 ③커스텀 미리듣기.
콘텐츠는 3트랙 — 텍스트 장면 레이어(지금·0원) / 나레이션 54클립 / **배역 예시 대사 클립 ~17개** — 이고, **엔진은 오디오가 0개여도 텍스트 카드 모드로 작동**하므로 착수가 목소리 병목에 걸리지 않는다.

**현장 조작(v0.3.2)**: 메인 **[다음] 버튼 1개를 예식당 10회.** 나머지 10회는 자동 체인이 대신 눌러 준다. 볼륨 버튼은 0개.

---

## 1. 큰틀 검증 — 유지 · 역전 · 철회

### 유지 (검증 통과)

| 항목 | 판정 근거 |
|---|---|
| 텍스트 예시 대본 먼저 | 0원 · 런북 B·C 초안 겸함 · 사용자 즉각 반응("이거 좋다")으로 1차 검증 |
| 커스텀 = 파일 생성이 아니라 재생 순서 | 순서 축 13,632조합 사전 렌더 불가 · 클립 재사용으로 고객당 한계비용 0원 |
| 포지셔닝 가드(주어는 항상 예식·하루·코스) | CLAUDE.md 나레이션 차선 규칙(2026-07-16)과의 긴장 관리 — 불변 |
| **고객 실제 서약·편지 텍스트 TTS 절대 금지** | 비밀 정책 충돌 · 감정 가치 소진 · 민감 텍스트 외부 전송 — **불변. 배역 채택과 무관**(데모 대사는 가상 인물의 예시 문장이다) |

### 역전 1 (v0.2 · 유지) · 본체는 미리듣기가 아니라 **당일 콘솔**이다

- **실측: 당일 클립을 트는 수단이 저장소·문서 어디에도 없다.** `<audio>`/`new Audio` 사용처는 order-preview.html(샘플 1곡)·parents.html(어른 안내 1클립)뿐. 큐시트 30행은 "디렉터가 수동 '준비→발사'"라고만 적었고 **도구는 미정의.** 클립 파일명이 `01_`~`53_` 번호 체계인 것은 폴더 수동 재생을 암시하는 설계 흔적이다.
- 폴더 수동 재생으로 첫 예식을 치를 수는 있다(번호순 더블클릭). 그러나 그 방식엔 더킹(-6~-18dB 차등)·클립 간 1.5~2.5초 간격·N4~N6 브릿지 즉시 호출·고객 S 자동 로드가 전부 없다. **사고 복구 수단이 가장 필요한 순간에 도구가 가장 허술하다.**
- 판정: **콘솔이 첫 예식의 운영 필수품(본체)이고, 고객용 미리듣기는 같은 엔진의 자동 모드 스킨이다.**
- 폴더 수동 재생은 폐기가 아니라 **플랜 B(백업)로 강등**: 보조 기기에 번호 파일 폴더 상비.

### ★철회 · v0.2 '역전 2'(배역 AI 음성 제외 추천)를 **취소한다**

**2026-07-31 사용자 결정: 배역별 AI 음성 채택.** 아래는 철회 사유의 정직한 기록이다.

**1) 내 논거 하나가 사실이 아니었다 (실측 오류).**
v0.2 §1은 "실제 상품 구조는 나레이션만 녹음 · 사람 파트는 사람인데, 데모가 사람 파트까지 AI로 채우면 상품 구조를 데모가 거짓말하게 된다"고 썼다. **틀렸다.** 상품에 이미 당사자 목소리 트랙이 두 개 있다:

```javascript
// order-preview.html:416  기본값
guestVoice:'nar', entryVoice:'nar', ...
// order-preview.html:1501  ★'녹음·AI' 명시
return {n:'신랑·신부 입장', nar:eCouple?ENTRY[S.entry].self:ENTRY[S.entry].nar,
        self:eCouple?'두 분 목소리로 인사 (녹음·AI)':null, ...};
// order-preview.html:1595-1596  D-7 준비물
if(S.entryVoice==='couple') p.push(['D-7','입장 인사 녹음 파일 올리기(mp3/m4a)']);
if(S.guestVoice==='couple') p.push(['D-7','하객 맞이 안내 녹음 4개 올리기(mp3/m4a)']);
```

즉 `guestVoice='couple'`·`entryVoice='couple'`은 **하객 맞이 안내와 입장 멘트를 두 분 목소리로 바꾸는 판매 중인 옵션**이고, 코드가 그 수단으로 **AI 생성을 명시하고 있다.** 데모에서 당사자 목소리가 나오는 것은 상품 구조와 어긋나지 않는다 — 오히려 **이 두 옵션의 판매 도구**가 된다. 손님이 "두 분 목소리로 하면 이렇게 들리는구나"를 계약 전에 들어볼 수 있다.

**2) 내 대안(텍스트 카드만)은 목적을 못 이룬다.**
사용자 목적은 "흐름을 감으로 익히게 하기"인데, 사람 파트를 전부 화면 글자로 두면 클립 → 침묵 → 눈으로 읽기 → 클립이 반복된다. **미리'듣기'가 아니라 미리'읽기'가 되고, 흐름 체감이라는 목적 자체가 사라진다.** 시뮬레이션 실측상 본식의 86%가 사람·음악·침묵 구간이므로, 그 86%를 무음으로 두면 남는 건 14%뿐이다.

**3) 남는 리스크는 유효하되, 설계로 해소 가능하다.**
진짜 위험은 "AI가 읽는 것"이 아니라 **"AI가 신랑인 척 연기하는 것"**이다(오디오북 ↔ 드라마CD 차이). 사용자 원안의 표현도 "마치 오디오북처럼"이었다. → §2-B 안전장치 4개로 처리.

**4) 밈 필터 ×4 부담은 실재한다** → 순서로 해소: 나레이션 1종을 먼저 확정해 병목을 풀고, 배역 4종은 그 뒤에 태운다(§5).

---

## 2. 새 큰틀: 엔진 하나 · 쓰임 셋

```
[엔진] 클립 시퀀서
  입력: S(코스·옵션·순서) → curSeq() → 클립 목록
  재생: 순서 재생 · 클립 간 간격(1.5~2.5초) · BGM 트랙 + 자동 더킹
  폴백: 오디오 없는 클립은 텍스트 카드 + 무음 → 오디오가 0개여도 전체 작동

[쓰임 A · 본체] 당일 디렉터 콘솔 — 수동 모드 · 첫 예식 전 필수
  - 메인 [다음] 버튼 1개 · 예식당 10회 — 전 20큐 중 자동 10 / 수동 10(§3-A 전수 판정)
  - 자동 큐 체인: 앞 큐가 끝나는 순간 다음 큐 시점이 이미 정해진 구간은 코드가 잇는다
  - 보조 3개: [대기 클립]N4 · [음악만 멈춤] · [뒤로] — 사고 시에만(정상 0회)
  - BGM 더킹 자동(순간별 -6~-18dB 차등 · 큐시트 수치 이관) · **볼륨 버튼 0개**
  - 고객 S 자동 로드 → 예식마다 큐 프로그래밍 0분
    ★배역 예시 클립은 당일 콘솔에서 재생하지 않는다(미리듣기 전용 트랙)
    — 단, guestVoice/entryVoice='couple' 고객의 실제 업로드 음원은 당일 재생 대상이다

[쓰임 B] 코스 기본형 미리듣기 — 자동 모드 + 코스 기본 S
[쓰임 C] 커스텀 미리듣기 — 자동 모드 + 고객 ritualDraft.S
```

### 2-A. 배역 음성은 '코스별 완성 mp3'가 아니라 **블록 단위 클립 라이브러리**다

이것이 v0.1과의 결정적 차이다. 코스별 완성본 3개를 만들면 커스텀(쓰임 C)에 대응할 수 없다 — 순서 축만 13,632조합이다. **나레이션 54클립과 똑같이 블록 단위로 쪼개 두면 엔진이 고객 S대로 조합 재생한다.** 사용자 원안의 "코스를 커스텀 완성하면 그거에 맞춰진 오디오북이 완성"을 실제로 충족시키는 유일한 방법.

발화 블록 실측(`order-preview.html:1500-1552` `self` 필드 기준) — 배역 클립 필요분:

| 블록 | 화자 | 클립 | 비고 |
|---|---|---|---|
| `guest`(guestVoice=couple) | 신랑·신부 | 4 | 상품 옵션 존재 · D-7 업로드 4개와 1:1 |
| `entry`(entryVoice=couple) | 신랑·신부 | 1 | 상품 옵션 존재 |
| `welcome` | 신랑·신부 | 2 | 항상 두 분 직접(고정 · `S.welcome='self'`) |
| `vow` | 신랑·신부 | 2 | 감정 정점 · 연기 절제 |
| `bless` | 아버님·어머님 | 2 | |
| `letter` | 신랑 or 신부 | 2 | 부모님께 / 서로에게 대표 1씩 |
| `tribute` | 신랑·신부 | 1 | 짧은 감사말 |
| `toast` | 대표 하객 | 1 | 건배사 |
| `declare`(ask/chorus) | **하객 군중** | 2 | ★AI 단일 보이스로 군중 불가 → §6 결정 7 |
| veil·ring·ringwarm·valley·song | — | 0 | 발화 없음(동작·음악) |

**합계 약 17개 · 총 ~1,000음절 ≒ 3.4분.** 나레이션 54클립(13.0분)의 26%.
v0.1이 상정했던 "코스별 완성 오디오북 3종 ≒ 24분"보다 **7분의 1**이다. 블록 분해가 제작량도 줄인다.

### 2-B. 안전장치 4개 (언캐니·오해 방지)

1. **화면 라벨 상시 표기** — 배역 클립 재생 중 화면에 `🎙 예시 목소리 · 당일엔 신랑님이 직접 말해요`. 나레이션 클립 재생 중에는 뜨지 않는다(대비로 구분).
2. **진입 시 1회 고지** — 미리듣기 시작 화면에 한 줄: "사람이 말하는 부분은 예시 목소리로 들려드려요. 당일엔 두 분과 가족이 직접 말합니다."
3. **연기 강도 하향** — 배역 음성은 **낭독톤**으로 수록한다(오디오북). 감정 연기·울먹임·과호흡 표현 금지. 특히 `vow`·`letter`는 절제할수록 언캐니가 줄고, 실제 당일의 감정을 데모가 미리 소진하지 않는다.
4. **예시 대사는 가상 인물 것만** — 시뮬레이션 코스A의 준호·세영·이만수 계열 문장을 재사용. **고객이 쓴 실제 문장은 어떤 경우에도 TTS 하지 않는다**(§1 유지 항목 4).

### 2-C. ★스크립트 표시 = 배지 2종 (2026-07-31 사용자 지시)

> 사용자 지시 verbatim: "신랑신부 부모님이 직접 대사하는 곳 그 외에 선택 가능한 부분들은 스크립트에 따로 표시를 해서, 고객이 '아 지금은 임의로 해놓은 거구나, 전체적으로 이런 느낌의 흐름이구나' 이해를 돕기 위함"

미리듣기 화면은 **지금 흐르는 구간이 어떤 성격인지**를 항상 배지로 알린다. 축이 둘이다.

| 배지 | 붙는 곳 | 문구(안) | 데이터 원천 |
|---|---|---|---|
| **① 예시** | 배역 클립 재생 중 | `예시 목소리 · 당일엔 두 분이 직접 말해요`<br>`예시 목소리 · 당일엔 아버님이 직접 말씀하세요` | §2-A 발화 블록 표 |
| **② 선택** | 옵션 있는 블록 | `여기는 고를 수 있어요 · 넣기 / 빼기` | ★**`COURSES[].seq[].pick` 이미 존재** |
| (무배지) | 나레이션 클립 | — | 대비로 "이건 저희가 준비하는 부분"이 읽힘 |

**②의 데이터는 새로 쓸 게 없다.** `order-preview.html`의 코스 정의에 `pick` 문구가 이미 전 블록에 깔려 있다 — 실측 16종:

```
입장 멘트 느낌 6가지 중 선택 / 넣기 / 빼기 / 울컥하면 배우자가 대신 /
부모님께 / 서로에게 / 나레이션 / 하객 응답 / 가족 낭독 / 인사말 준비 /
꽃 헌정 / 큰절 / 포옹 / 한 분 대표 / 양가 각각 / 축배 / 케이크 / 둘 다 /
어머니 / 아버지 / 가까운 분 / 생략 / 가족만 / 전체 하객
```

미리듣기 스크립트가 이 값을 그대로 물려받으면 **코스 카드와 미리듣기가 같은 원천을 쓰므로 문구가 어긋날 수 없다**(`build-dubbing-script.mjs`의 동기 보증과 같은 원칙).

효과: 사용자 발단 메시지의 두 목적이 배지 하나씩을 가져간다 — **①이 "이미지 트레이닝"**(어디가 내 차례인지 몸으로 익힘)을, **②가 "코스 정하기 이해"**(어디를 바꿀 수 있는지 보임)를 담당한다.

표기 규칙: 배지는 **이모지 없이 텍스트로**(CLAUDE.md 이모지 최소 규칙) · 전각 줄표 금지 · 호칭은 "두 분"·"하객분들".

콘텐츠 3트랙:

- **트랙 1 · 텍스트 장면 레이어** — 지금 · 0원 · 내 몫. 나레이션 문안은 `ritual-data.js`에서 자동 추출(신규 `scripts/build-course-story.mjs`), 지문·장면만 저작. 코스 B·C 집필이 미작성 런북 B·C 초안을 겸한다.
- **트랙 2 · 나레이션 54클립** — 목소리 A/B 확정 후. 완성되는 대로 클립 단위로 꽂힌다.
- **트랙 3 · 배역 예시 클립 ~17개** — 나레이션 확정 후 착수. 미리듣기 전용.

---

## 3. 당일 신뢰성 원칙 (콘솔 요구사항 · `console.html`에 구현됨)

- **오프라인 우선**: 음원·코드 사전 로드. 리허설 때 **기내모드로 1회 완주** 테스트 통과해야 실전 투입.
- **자동재생 정책**: 브라우저 오디오는 첫 터치 후 해제 — 개식 전 디렉터가 콘솔을 한 번 터치하는 절차를 운영 순서에 명시.
- **백업 2중**: 보조 기기에 콘솔 + 번호 파일 폴더(플랜 B) 상비.
- **화면 꺼짐 방지**: Wake Lock.
- **트랙 분리**: 콘솔 로드 대상에서 트랙 3(배역 예시)을 제외해 당일 오발사를 원천 차단.

### 3-A. ★현장 콘솔 조작 사양 — 2트랙 분리 · 자동 큐 체인 · **디렉터 조작 10회** (v0.3.2)

**사용자 제안**: "BGM 순서대로 따로 · 나레이션 순서대로 따로 · 버튼 눌러 타이밍 맞춰 소리 줄이고 높이고".
**판정: 트랙 분리는 채택. 상시 수동 볼륨은 기각.**
**설계 원칙 (2026-07-31 사용자 지시 "최고 진행 품질 · 최소 조작"):**
> **사람이 *기다려야* 하는 지점만 버튼으로 남기고, *시간이 정해진* 것은 전부 자동 체인으로 묶는다.**
> 조작을 줄이는 방법은 버튼을 없애는 게 아니라 **한 번 누르면 여러 개가 따라오게 묶는 것**이다.

**① 2트랙 분리 = 맞다 (큐시트 실측이 뒷받침).**
큐시트 순간별 표 11행이 순간마다 **다른** BGM(무드·BPM·편성)을 요구하므로 BGM은 한 곡이 아니라 **장면별 재생목록**이다.
결정적 근거는 두 트랙의 **전환 시점이 어긋난다**는 것 — 큐시트 14행 "반지·링워밍 … 성혼선언 직전 **음악 페이드아웃 시작**"은
나레이션이 아직 반지 구간을 진행 중인데 음악만 먼저 움직이라는 지시다. 한 타임라인으로는 표현 불가 → **독립 2트랙이 강제된다.**

**② 상시 수동 볼륨 = 기각.**
- 큐시트 23행이 이미 "**더킹은 자동(사이드체인) · 수동 볼륨 편집 불필요**"로 못박음.
- 손 부족: 아래 ★실측대로 **버튼 조작이 이미 20회**. 클립마다 내리고 올리면 3배로 뛴다. 디렉터는 **촬영 겸임**이라(전대본 21행) 손이 비어야 카메라를 든다 — 72행이 실증: "태블릿을 내려놓고 두 사람의 발만 본다."
- 재현성: 더킹 페이드는 **다운 0.3~0.5초 / 복귀 0.8~1.5초**(큐시트 25행). 손으로 이 곡선을 53회 동일하게 그릴 수 없다. 한 번 어긋나면 말이 묻히거나 음악이 뚝 끊긴다.

**★실측 정정 (전대본 전수 재계수)**
전대본 §디렉터가 한 일 표의 "클립 발사 17회 + 수동 '다음' 4회"는 **중복 계상**이다. 수동 '다음'으로 발사된 `14`·`16`·`20`·`24`는 본식 13개 클립에 이미 포함돼 있다.
또 후반 3클립(`46`·`47`·`49`)이 표에서 누락됐다. **정확한 버튼 조작 = 20회** (식전 4 + 본식 13 + 후반 3). 비언어 유도 2·발화 4는 버튼이 아니므로 별개.

**③ 20회 중 10회는 사람이 누를 이유가 없다 — 전수 판정.**
기준은 하나다. **다음 큐를 낼 시점이 "사람을 기다려야" 정해지는가, 아니면 앞 큐가 끝나는 순간 이미 정해져 있는가.**

| 클립 | 시점 | 판정 | 근거(전대본) |
|---|---|---|---|
| `01` 하객 입장 | 문 열림 | **수동** | 개식 개시 |
| `02` `03` `04` | 13:50 / 13:55 / 13:59 | **자동** | **시각 고정.** 대본도 `03`·`04`엔 발사 표기를 안 붙였다 |
| `05` 입장 | 신부 준비 완료 | **수동** | 사람 대기 |
| `11` 첫인사 열기 | 두 사람이 중앙에서 하객 향해 돎 | **수동** | 76행 "속으로 셋을 센다" |
| `12` 첫인사 닫기 | 인사 끝 | **수동** | 46초·30초·80초 다 가능(100행) |
| `13` 서약 안내 | `12` 종료 | **자동** | 사이에 사람 동작 없음. 카드 꺼내기는 `13` 위에서 |
| `14` 서약 닫기 | 낭독 끝 | **수동** | **55초/75초 — "오늘 처음 안 숫자"**(131행) |
| `15` 반지 안내 | `14` 종료 | **자동** | 141행 "**이 구간은 말을 기다리지 않는다**" |
| `16` 반지 닫기 | 반지 착용 확인 | **수동** | 사람 동작 |
| `29` 성혼 선언 | `16`+페이드8초+침묵3초 | **자동** | 153~159행 **전부 시간 고정** |
| `26` 편지 안내 | `29` 종료 + 음악 정지 | **자동** | 179행. 봉투 여는 동작은 `26` 위에서 |
| `20` 편지 닫기 | 낭독 끝 | **수동** | 2분20초 + 오열 8초 + 회복 15초 |
| `23` 덕담 안내 | `20` 종료 | **자동** | 마이크 전달은 `23` 위에서 |
| `24` 덕담 닫기 | 절단 판단 | **수동** | 3분 40초 폭주(260행) |
| `25` 폐식 | `24` 종료 | **자동** | |
| `46` 촬영 안내 | `25`+30초 | **자동** | |
| `47` 배웅 안내 | 촬영 종료 | **수동** | 사람 대기 |
| `49` 마지막 인사 | 하객 대부분 퇴장 | **수동** | 사람 대기 |

**자동 10 · 수동 10.** 정확히 절반이 사라진다.
남은 10회는 전부 **사람 말이 끝났는지 / 사람이 움직였는지**를 판정하는 지점이라 **더 줄일 수 없다 — 이것이 물리적 하한이다.**

**④ 그래서 볼륨 버튼 4개는 필요 없다 — 전부 체인 안으로 들어간다.**

| v0.3.1이 제안한 버튼 | v0.3.2 판정 | 어디로 갔나 |
|---|---|---|
| 음악 끊기 | **삭제** | `16`→`29` 체인(페이드 8초 → 침묵 3초)에 흡수 |
| 음악 넣기 | **삭제** | `29` 클립 종료 트리거로 하드 인. 171행 |
| 롱 페이드아웃 | **삭제** | `49` 종료 후 90초 자동 하강. 308행 |
| 더킹 홀드 | **삭제** | 편지 구간은 애초에 음악 무음(179행)이라 복귀할 것이 없음 |

**⑤ 대신 진짜 필요한 보조 버튼 3개 — 전부 "사고용", 정상 진행에선 0회.**

| 버튼 | 왜 | 실증 |
|---|---|---|
| ★**[대기 클립]** = `N4` 감정 대기 즉시 발사 | 오열 8초. **"필요한 것은 버튼이 아니라 목소리다"**(203행) — 그 목소리가 이미 녹음 대상 54클립에 있는데 **배선이 없어 못 꺼냈다**(갭 1) | 이 버튼 하나가 디렉터 발화 4회 중 1회를 없앤다 |
| **[음악만 멈춤]** | 체인이 돌기 시작하면 못 멈추는 위험의 답. 마이크 하울링·외부 소음 등 | — |
| **[뒤로]** | 오발사 취소. 안전장치 | — |

+ **BGM 마스터 슬라이더 1개** — 예식장마다 PA 볼륨이 달라 물리적으로 필요하나 **개식 전 1회 맞추고 그 뒤 0회.**

> ### ★적정선 결론
> **메인 [다음] 버튼 1개 · 예식당 10회.** 보조 3개는 사고 시 1~2회. 슬라이더는 개식 전 1회.
> **정상 진행 총 10회 / 사고 포함 11~12회.** v0.3.1안(20 + 4~6 = 24~26회) 대비 **절반 이하.**
>
> **그리고 품질은 오히려 올라간다.** 8초 페이드 → 3초 침묵 → 하드 인을 사람이 세는 것보다 코드가 정확하기 때문이다. 전대본 157행이 그대로 그 얘기다 —
> *"이 3초가 이 예식에서 가장 정확하게 재현된 3초다 — 사람 사회자였다면 이 정적을 못 견디고 0.5초쯤 일찍 입을 열었을 것이다."*

**⑥ 체인 안전 규칙 3개 (자동화가 사고를 키우지 않게)**
1. **체인은 다음 '수동 지점'에서 반드시 선다.** 한 번 누르면 최대 다음 대기 지점까지만 간다. 절대 그 너머로 안 넘어간다.
2. **자동화 금지 구간 명시** — 입장(`05`→`11`) 사이 1분 42초는 **타이머 자동 금지**. 걸음 속도가 매번 다르다(70~76행).
3. **다음 큐를 화면에 크게** — 큐 이름 + 예상 길이 + 그 다음 큐. 디렉터가 화면을 오래 안 봐도 되게.

**⑦ 기계 — 소프트웨어는 자체 제작, 입력 장치만 기성품.**
QLab(공연 큐 표준 · macOS 전용 · 무료 티어 있음) 등 기성 큐 재생기는 **예식마다 큐를 손으로 다시 프로그래밍**해야 한다.
우리는 순서 축만 13,632조합이고 고객 S가 이미 저장돼 있으므로, **S를 읽어 큐를 자동 생성하는 것**이 자체 엔진의 존재 이유다. 이건 기성품으로 대체 불가.
입력 장치는 사는 게 맞다(화면 안 보고 조작 · 오조작 감소):

| 후보 | 가격(2026-07 실측) | 브라우저 연결 | 판정 |
|---|---|---|---|
| **없음(노트북 화면 클릭)** | 0원 | — | **1차 예식은 이걸로.** 버튼 4개(메인 1 + 보조 3)면 화면으로 충분 |
| ★**KORG nanoKONTROL2** (페이더 8·노브 8·버튼 24) | 국내 **98,000원** / 미국 $116.99 | **Web MIDI로 브라우저 직결**(중간 SW 없음). Chrome·Edge만 지원 — **콘솔은 Chrome 고정** | **2차부터 추천.** 페이더 1개=BGM 마스터, 큰 버튼 1개=[다음], 나머지 3개=보조. 버튼 24개 중 4개만 쓴다 = 오조작 여지가 오히려 큼 → **물리 라벨 테이프 필수** |
| Elgato Stream Deck MK.2(15키) / Stream Deck +(8키+다이얼 4) | 국내 최저 **207,500원** / +는 $159.99~ | 전용 SW가 **키보드 단축키**를 보냄 → 브라우저 창이 포커스를 잃으면 먹통 | 키에 라벨이 뜨는 건 장점이나 2배 값 + 포커스 리스크 |
| 블루투스 페이지터너 풋페달 | 6만원대 | 화살표 키 전송 → 바로 먹힘 | **발로 '다음 클립'.** 손이 마이크·카메라에 묶일 때 보조 |
| 소형 아날로그 믹서(Yamaha MG 등) | — | — | **오답.** 물리 분리하려면 출력 2계통을 예식장 PA에 따로 꽂아야 함(음향실이 거부하는 경우 많음). 브라우저 Web Audio에서 믹스해 **스테레오 1줄**로 내보내는 편이 현장에서 안전 |

**구매 시점**: 물리 컨트롤러를 먼저 사면 버튼 배치를 실전 없이 정해야 한다. 1차 예식을 화면으로 돌려 **어느 큐에서 손이 급했는지**를 본 뒤 사도 늦지 않다(당일배송 품목).

**⑧ 문서 충돌 — 큐시트 2행 개정 필요.**

| 큐시트 | 현재 문장 | v0.3.2 개정 방향 |
|---|---|---|
| 23행 | "더킹은 자동(사이드체인) · **수동 볼륨 편집 불필요**" | **그대로 맞다.** 후반 편집 전제로 쓴 문장이지만 현장 라이브에도 동일하게 성립 — 볼륨 버튼을 하나도 두지 않는 근거로 승격 |
| 30행 | "디렉터가 **다음 클립·음악을 수동 '준비→발사'**" | **정정.** 클립 20개 중 **자동 10 / 수동 10**이고, **음악은 전 구간 수동 조작 0회**(클립 큐에 종속). "클립 간 1.5~2.5초 침묵이 여유를 흡수"는 유지 |

→ §6 결정 10 승인 시 큐시트에 반영.

---

## 4. 소스 대장 (v0.3 갱신 · 실측 메모)

| 소스 | 경로 | 상태 (2026-07-31 실측) |
|---|---|---|
| **당일 재생 수단** | (없음) | **부재 확인** → 콘솔 신설 근거 |
| ★**당사자 목소리 옵션** | `order-preview.html:416,420-421,1102-1103,1217-1260,1500-1501,1595-1596` | **`guestVoice`·`entryVoice`의 `'couple'` 값 존재 · 1501행에 "녹음·AI" 명시** — v0.2 반대 논거②를 무효화한 실측 |
| 발화 블록 목록 | `order-preview.html:1500-1552` `self` 필드 | 배역 클립 ~17개 산출 근거(§2-A) |
| ★**선택 배지 원천** | `order-preview.html:513-593` `COURSES[].seq[].pick` | **16종 문구 이미 존재 · 전 블록 커버** → §2-C ② 배지는 신규 저작 0 |
| 클립 파일명 체계 | `01_`~`53_` 번호 | 폴더 수동 재생 암시 → 플랜 B로 계승 |
| BGM 저작권 | 큐시트 §저작권 | **현장 재생도 KOMCA 대상** → 구독 결정 = 첫 예식 전제 |
| ritualDraft 저장 형태 | `order-preview.html:1743` · `mypage.html:1430` | S 전체 저장(`_v===3`) → 엔진 입력 그대로 사용 |
| 샘플 재생 전례 | `order-preview.html:666` `NARR_SAMPLE` | `sample-voice.mp3`(272KB)·`sample-bgm.mp3`(3.9MB) |
| 클립 음원 배선 | `parents.html:314` | 슬롯 1/53 · `assets/audio/` 미생성 |
| N4~N6 브릿지 | `scripts/build-dubbing-script.mjs:104-106`만 | 실행 화면 배선 없음(시뮬레이션 갭 1) — 콘솔 **[대기 클립]** 버튼이 해소. 발동 5초(런북 §11-C) |
| 코스 A 전대본 | `시뮬레이션_코스A_전대본.md` | 완료(`c7196c7`) · **배역 예시 대사의 원본 소스** · 갭 4개 전부 처리 기록 부착(2026-07-31) |
| ★**코스 A 런북 v1.2** | `런북_코스A_담백.md` | **갭 2·3·4 해소 문서.** §3 폴백 **3단**(4순위 성우 대리 삭제 · v1.2) · §8 덕담 4단 · §10 디렉터 발화 · §11 문안 명세(**구현 완료**) |
| **54클립 대본** | `더빙_녹음_대본_최종.md`/`.txt` | 자동 생성 · ritual-data.js 동기 보증. **§11-A 반영 완료 — `blessEndLong`(G3-14L) 추가로 54클립 · 3,894음절 ≒ 13.0분** |
| 목소리 벤더 규칙 | `벤더_확정_결정지.md` §4·§5 | A/B 미완 = 트랙 2·3 공통 병목 |
| 연출 수치 | `더빙_연출_큐시트.md` | 더킹 dB·페이드·간격 → 콘솔 파라미터로 이관 |
| ★**타입캐스트 붙여넣기 파일** | `타입캐스트/{1~4}.txt` · `manifest.json` · `README.md` | **자동 생성(v0.6)** · `scripts/build-typecast-import.mjs` · 54클립 184문장 7,695자 |
| ★**클립 조립기** | `scripts/assemble-narration.mjs` | 문장 파일 → 여백 삽입 → concat → -16 LUFS/-1.5 dBTP → `NN_slug.mp3`. 순서 검증 `r ≥ 0.85` |

용량 참고: 54클립 + 배역 17클립 ≈ 16.4분 → 128kbps mp3 ≈ 15MB. 정적 호스팅 무부담.

---

## 4-A. ★트랙 2 제작 파이프라인 (v0.6 신설 · 2026-08-01)

v0.5까지 §5 순서표는 "A/B 확정 후 **나레이션 54클립 제작**"이라고만 적고 그 안을 비워 뒀다. 그 칸의 실체는 이랬다 — 54클립을 하나씩 TTS에 넣고, 문장 사이 여백을 손으로 넣고, 음량을 맞추고, 파일명을 규칙대로 바꾸는 일. **클립 54개 × 평균 3.4문장 = 184번의 수작업**이다. 그걸 이번에 기계로 옮겼다.

### 체인

```
assets/ritual-data.js          ← 문안 단일 원천 (여기만 고친다)
  ↓ node scripts/build-dubbing-script.mjs
docs/plans/식순연구/더빙_녹음_대본_최종.txt        54클립 · 3,894음절
  ↓ node scripts/build-typecast-import.mjs        ★신설
docs/plans/식순연구/타입캐스트/{1~4}.txt + manifest.json + README.md
  ↓ (타입캐스트: 붙여넣기 → 화자 감지 → 보이스 배정 → 감정 다듬기 → 문장별 분리 다운로드)
  ↓ node scripts/assemble-narration.mjs --in <다운로드 폴더>   ★신설
assets/audio/narration/NN_slug.mp3                54개 (+ parents-letter.mp3 사본)
```

앞뒤 두 스크립트가 신설분이다. 가운데 한 칸 — **목소리 고르기 + 감정 다듬기 + 다운로드** — 만 사람 몫으로 남았다.

### 왜 이렇게 짰나 · 설계 판단 3건

**① ONE_SENT_ONE_LINE — 한 문장 = 한 줄.**
타입캐스트 다운로드의 '문장별 분리'가 *문장* 기준인지 *줄* 기준인지 확인할 방법이 없다(공식 문서 페치가 이 환경에서 막힌다). 우회하거나 추측하는 대신 **한 문장을 한 줄로 두어 두 해석의 결과를 같게 만들었다.** 확인이 불가능한 사양에 의존하지 않고 문제 자체를 없앤 것.

그리고 이 선택은 부작용이 아니라 이득이었다. 대본 규격은 문장 사이 여백을 1.5~2.5초, 선언문 앞뒤 0.5/1.0초, 혼주 편지 소제목 앞뒤 1.5초로 규정하는데, 원래는 "편집 단계에서 처리"였다. 문장 단위로 파일이 떨어지면 **그 여백을 매니페스트대로 기계가 정확히 넣는다.** 편집 공정이 통째로 사라졌다.

**② ROLE_SPEAKER — 화자를 캐릭터 이름이 아니라 역할명으로.**
붙여넣기 파일의 화자는 `진행`·`안내`·`편지` 세 개다. 타입캐스트는 화자명이 캐릭터 이름이면 자동 배정하지만, **A/B가 안 끝났으므로 지금 이름을 박으면 그게 곧 기본값으로 굳는다.** 역할명이면 3단계 「보이스 배정」에서 사람이 고르고, 파트 단위로 바꿔 끼울 수 있다. 확정되면 `--voice 진행=대길,안내=김경화 앵커,편지=현주`로 이름을 박아 그 단계까지 없앤다.

역할 배분은 협의안 §5 그대로 — 진행 41클립 / 안내 12클립 / 편지 1클립. **총 2~3인을 넘기지 않는다**(목소리가 많아질수록 예식이 방송처럼 들린다).

**③ ORDER_VERIFY — 순서를 길이 상관으로 검증한다.**
같은 문장이 **14종 30번** 나온다. "신랑 신부, 입장!"이 6번(입장 연출 6종), 선언문이 4번(선언 톤 3종 + 가족선언), 배웅 문장 4개가 각 2번(온라인 하객 유무). 코스가 갈리는 자리라 필연이다. 타입캐스트가 내용 기반으로 파일명을 지으면 **사전순 정렬이 재생 순서와 어긋난다.**

그래서 조립기는 붙이기 전에 **파일 실측 길이 vs 대본 음절수 기반 예상 길이의 피어슨 상관 r**을 계산하고, `r < 0.85`면 편차 상위 5개를 찍고 멈춘다. 한 칸만 밀려도 상관이 급격히 무너지므로 조립 후가 아니라 조립 전에 잡힌다.

### 산출물 (2026-08-01 실측)

| 파일 | 내용 | 화자 | 클립 | 문장 | 글자 |
|---|---|---|---|---|---|
| `1_안내.txt` | 식전 안내 + 폐식 브릿지 | 안내 | 12 | 36 | 1,533 |
| `2_진행_전반.txt` | 입장 + 고정 진행 나레이션 | 진행 | 22 | 50 | 1,945 |
| `3_진행_후반.txt` | 편지 도입 · 선언 · 베일 · 링워밍 · 헌정 · 축배 | 진행 | 19 | 60 | 2,364 |
| `4_혼주편지.txt` | 어른께 드리는 안내 편지 (3분 통낭독) | 편지 | 1 | 38 | 1,853 |

합계 7,695자로 **20,000자 한도의 38%**다. 파트를 나눈 이유는 한도가 아니라 ①편집기 렉(협의안 §3-②) ②보이스 배정 단순화 ③파트별 A/B 교체다.

무음 포함 총 **약 18.4분**(순수 낭독 13.0분 + 무음 5.4분). 무음이 29%인 것은 낭비가 아니라 설계다 — 예식 나레이션은 말과 말 사이가 진행 그 자체다.

### 잡아낸 것 하나

여백 규칙을 파일명으로 판정하다가 **축배로 끝나는 클립 하나를 놓쳤다.** 대본 note는 "축배 뒤에는 폐식으로 넘어가는 온도 낙차가 커서 무음을 4~5초로 길게 준다"인데, 코드는 `toast-toast`라는 **파일명**만 보고 있었다. 실제로 축배로 끝나는 클립은 둘이다 — `G9-toast`(43)와 `G9-both`(45). 후자가 1.0초만 받고 있었다.

note가 말한 건 "축배 클립 뒤"가 아니라 **"축배 뒤"** — 환호가 터진 자리의 낙차를 받는 무음이다. 판정을 마지막 문장(`/위하여!$/`)으로 바꿨다. 같은 블록이지만 환호가 없는 `toast-cake`(44)는 기본값이 맞으므로 그대로 뒀다. **`[ORD_BADGE_AXIS]` 때와 같은 종류의 실수 — 내가 세운 원칙을 내 코드가 어긴 것.**

### 남은 사람 몫

목소리 고르기(§6 결정 대기 · 최대 병목) · 문장별 감정 다듬기 · 다운로드 1회. **미리듣기와 재생성은 무제한 무료이고 다운로드만 한도를 깎으므로, 전부 확정하기 전에는 받지 않는다**(협의안 §3-①).

★A/B는 이제 30분짜리다. `3_진행_후반.txt`를 붙여넣고 **클립 27(`G4-parent` 편지 도입)과 클립 34(`W2-b` 서약 문답)** 두 자리만 들어 보면 된다. 앞은 가장 조용한 대목, 뒤는 가장 형식적인 대목이라 이 둘을 견디는 목소리면 나머지 52클립은 다 된다.

---

## 5. 순서 (v0.3)

| 순번 | 일 | 담당 | 전제 |
|---|---|---|---|
| ✅ 완료 | **엔진 + 콘솔 본체 구현** — `assets/ritual-cue.js` · `console.html` · 검사 스크립트 (`7ba065b`) | 나 | 2026-07-31 |
| 지금 | 텍스트 장면 레이어: 코스 B(런북 초안 겸) → A 고객판 → C | 나 | §6 결정 1 ✅ |
| 지금 | 배역 예시 대사 17개 문안 집필(전대본에서 추출·축약) | 나 | §6 결정 1 ✅ |
| ✅ 완료 | **트랙 2 제작 파이프라인**(§4-A) — 붙여넣기 생성기 + 조립기. 184번의 수작업이 사라짐 | 나 | 2026-08-01 · v0.6 |
| 병행 | **나레이션 목소리 A/B 확정 — 트랙 2·3 공통 병목** ★이제 30분(파트 3 · 클립 27·34 두 자리) | 사용자 | 붙여넣기 파일 ✅ |
| 확정 후 | 54클립 제작 — **남은 건 감정 다듬기 + 다운로드 1회뿐**. 조립·여백·음량·파일명은 `assemble-narration.mjs`가 처리 | 사용자·나 | A/B 확정 |
| 그 다음 | **배역 4종 밈 필터 → 배역 17클립 제작** | 사용자 | 나레이션 완료 |
| 첫 예식 전 | BGM 구독 1곳 결정 · 콘솔 리허설(기내모드 완주 + 일부러 사고 3종 + **체인 오작동 시 [뒤로] 복구 1회**) | 사용자 | — |
| 첫 예식 후 | 미리듣기 공개(쓰임 B·C) | — | 운영 검증 |

비용: 트랙 1 = 0원. 엔진 = 개발만(운영비 0). 트랙 2 = 기존 계획 그대로. **트랙 3 = 신규 ~3.4분 · 보이스 4종 라이선스 범위 확인 필요**(벤더 결정지 §5 — 보이스별 과금인지 계정 단위인지). BGM 구독 신규 지출.

---

## 6. 결정 대기함 (v0.3)

| # | 결정 | 상태 | 한 줄 |
|---|---|---|---|
| 1 | **큰틀 승인** — 엔진 1 · 쓰임 3 · 콘솔 본체 | ✅ **2026-07-31 승인 · 구현 완료** | 사용자 확인 *"우선그럼지금 본체 만들고 그걸이용햐서 대모 들어보고, 고객코스선택 버전도 직접 들어볼수잇고 한거잖아?"* + *"추천대로하는데 근데 왜 cc로 넘겨 너가못해?"* → CC 전달이 아니라 **내가 직접 구현**. `assets/ritual-cue.js`(엔진) · `console.html`(쓰임 3가지 한 파일) · `scripts/check-ritual-cue.js`(17,280조합 검사) — `7ba065b` main 반영 |
| 2 | ~~배역 AI 음성 제외~~ → **배역별 AI 음성 4종 채택** | ✅ **2026-07-31 결정** | 사용자 선택. §1 철회 · §2-A 라이브러리 설계로 이행 |
| 3 | 기능 이름 "예식 미리듣기"(텍스트는 "미리 읽어보기") | ⬜ 대기 | 주어가 예식 — 포지셔닝 안전 |
| 4 | 오열·덕담 장면 포함(완곡) | ⬜ 대기 | 최대 불안에 장면으로 답함 |
| 5 | BGM 구독 1곳 — 첫 예식 전으로 승격 | ⬜ 대기 | 현장 재생도 KOMCA 대상 |
| 6 | 쓰임 B 공개 범위 | ⬜ 대기 | 코스 카드 + 완성 화면 먼저 |
| 7 | ★**하객 군중 소리**(`declare` ask/chorus 2클립) | ⬜ 신규 | AI 단일 보이스로 군중 불가. ①팀·지인 4~5명 실녹음 ②그 구간만 텍스트 카드 ③생략 — 추천은 ② |
| 8 | ★**배역 보이스 성별·연령 조합** | ⬜ 신규 | 신랑30대·신부20후·아버님60대·어머님50후를 기본형으로. 벤더 보이스 풀에 실재하는지 A/B 때 함께 확인 |
| 9 | ★**선택 배지를 눌러서 그 자리에서 바꿔볼 수 있게 할까** | ⬜ 신규 | 엔진이 S를 입력으로 받으므로 기술적으로 가능("빼면 어떻게 들려요?" → 즉시 반영 재생). 미리듣기가 곧 빌더가 됨. 다만 쓰임 B(계약 전)에서 열면 빌더와 역할이 겹침 → 추천은 **쓰임 C(커스텀)에서만 활성** |
| 10 | ★**현장 콘솔 조작 사양(§3-A)** — 2트랙 분리 + **자동 큐 체인** + 메인 [다음] 1개(10회) + 보조 3개(사고용) | ✅ **2026-07-31 승인("추천대로") · 구현 완료** | **디렉터 조작 20회 → 10회.** 볼륨 버튼 0개. 판정표 20큐가 코드와 일치함을 검사로 고정(`CUE_FIRE_RULE`). 큐시트 30행 정정은 남음 |
| 11 | ★**입력 장치를 살까 · 언제 살까** | ⬜ 신규 | 추천: **1차 예식은 0원(화면 버튼 4개), 2차부터 nanoKONTROL2 98,000원**. Web MIDI 제약상 콘솔은 **Chrome 고정** 전제 |
| 12 | ★**체인 정지 실패 시 최종 안전핀** | ✅ **2026-07-31 승인("추천대로") · 구현 완료** | 메인 버튼 **롱프레스 1.5초**로만 발동(누르는 동안 테두리가 차오름). 오누름으로 예식이 멈추지 않는다 |

---

## 7. 다음 액션

- **사용자**: ★**나레이션 A/B**(최대 병목) — 이제 붙여넣기 파일이 있으니 30분이다. `3_진행_후반.txt` → 클립 27·34 두 자리만. 그다음 `momentedit.kr/console.html` 직접 눌러 보기(폰 1대 · 클립이 0개라도 끝까지 돈다) → §6 나머지 몰아서.
- **나**: 트랙 2 파이프라인은 **끝냈다**(v0.6 · §4-A). 다음은 **배역 예시 대사 17개 문안** — 같은 붙여넣기 포맷(`5_배역.txt`)으로 내면 트랙 3도 같은 체인에 올라탄다. 그다음 코스 B 장면 레이어 → A 고객판 축약. 콘솔은 클립이 생기는 대로 폴더만 물리면 된다(코드 수정 없음).

## 변경 이력

- **v0.6 (2026-08-01 · 트랙 2 제작 파이프라인 완성)** — 사용자가 타입캐스트 「대본 가져오기」 화면을 보내며 물었다: *"붙여넣으면 애가 자동으로만들수있게 하는건가? 만들어줄래? 붙여넣을파일?"* 답은 **"그렇다, 그리고 그 앞뒤를 전부 자동화할 수 있다"**였다.
  **§4-A 신설** — `scripts/build-typecast-import.mjs`(붙여넣기 파일 4개 + `manifest.json` + 절차서 생성)와 `scripts/assemble-narration.mjs`(문장 파일 → 여백 → concat → 라우드니스 → `NN_slug.mp3`)를 새로 썼다. §5 순서표의 "나레이션 54클립 제작"은 **184번의 수작업**이었는데, 남은 사람 몫이 **목소리 고르기 + 감정 다듬기 + 다운로드 1회**로 줄었다.
  설계 판단 3건: **ONE_SENT_ONE_LINE**(확인 불가능한 '문장별 분리' 사양에 의존하지 않고, 한 문장=한 줄로 두 해석의 결과를 같게 만듦 · 부수적으로 여백 삽입까지 자동화됨) · **ROLE_SPEAKER**(A/B 미확정이므로 화자를 캐릭터명이 아닌 역할명 3종으로 · 확정 시 `--voice`로 자동 배정) · **ORDER_VERIFY**(같은 문장 14종 30번 → 사전순 정렬이 재생 순서와 어긋날 수 있어, 실측 길이 vs 음절수 예상 길이의 피어슨 상관 `r ≥ 0.85`로 조립 전에 차단).
  **자기 버그 1건 발견·수정** — 축배 뒤 4.5초 무음을 `toast-toast` **파일명**으로 판정해, 같은 문장으로 끝나는 `toast-both`(클립 45)가 1.0초만 받고 있었다. 대본 note가 말한 건 "축배 클립 뒤"가 아니라 "축배 뒤" — 환호가 터진 자리의 낙차다. 마지막 문장(`/위하여!$/`) 기준으로 교체. 환호가 없는 `toast-cake`(44)는 기본값 유지. `[ORD_BADGE_AXIS]`와 같은 종류(내가 세운 원칙을 내 코드가 어김).
  **§7 갱신** — A/B가 30분짜리가 됐다(파트 3 붙여넣기 → 클립 27·34 두 자리만 청취 · 미리듣기 무제한 무료).
- **v0.5 (2026-08-01 · §11 구현 완료 · 명세 1건 정정)** — 사용자 재지적 **"Cc에게 전하지 말고 너가직접하는게 좋지않아?"**. 남겨 뒀던 §11-A·§11-B를 **내가 끝냈다.**
  ①**ALT_CLIP** — 덕담이 3분을 넘으면 마무리 문안이 저절로 바뀐다. 설계 제1원칙은 **디렉터에게 고르게 하지 않는 것**: 어른의 말을 끊기로 막 결정한 사람에게 "어느 문안으로 할까요"까지 물으면 그 1초가 사고가 된다. 엔진은 `alt`(overSec·문안·근거)만 싣고 **판정하지 않는다**(엔진은 시간을 모른다) · 콘솔이 사람 구간 시작 시각을 재 발사 순간에 얕은 복사본으로 교체한다. 원본 큐는 건드리지 않아 뒤로가기·재발사에도 판정이 굳지 않는다.
  ②**§8 1단(예방)** — `blessMid`를 "말씀이 있습니다"에서 **"한 말씀 청하겠습니다"**로. 마이크를 받기 **전에** 길이가 정해지는 것이 가장 싸고 정중하다. 원천·빌더 인라인 사본 2곳을 함께 고쳐 `NAR_MIRROR` 통과.
  ③**대본 53 → 54클립**(3,894음절 ≒ 13.0분). 신규는 `G3-14L`로 붙였다 — `G3-15`로 넣으면 폐식이 밀려 "최우선 리테이크 8개" 등 기존 참조가 통째로 깨진다.
  ④**§11-B 정정 — 성우 대리 낭독을 상품에서 뺀다.** 명세는 "화면에서 '당일까지 비밀'과 배타 처리하라"였지만, ⓐ그 선택지는 `order-preview.html`에 **처음부터 없었고**(없던 걸 새로 만드는 일이었다) ⓑ성립하려면 **두 분이 쓴 실제 서약문을 외부 음성 서비스로 보내야** 하며 ⓒ그 녹음은 1·2순위에서 대부분 회복되어 **거의 쓰이지 않는다**. 배타 처리로 남기느니 제공하지 않는 편이 지킬 수 있는 약속이다. 대신 서약·편지 카드가 §3 폴백 1~3순위를 **같은 문장으로** 말하게 맞췄다(`FALLBACK_LADDER`).
  ⑤**부수 수확 `PHASE_CLIP`** — 앞이 사람 구간이면 클립이 흐르는 동안에도 화면에 초록 막대와 "사람의 시간"이 **남아 있었다**(`phase='clip'`이 선언만 되고 대입이 없었다). 하필 그 자리가 덕담을 끊고 마무리가 나가는 순간이라, 디렉터가 가장 확실히 알아야 할 "이제 마이크는 나레이션 것"이 거꾸로 떴다. 한 줄로 고쳤다.
  ⑥**검증** `automation/tests/console-alt-clip.mjs` — 가상 시계로 덕담을 3분 10초 끌어 **대조군(기본 문안) vs 실험군(교체 문안)** 을 실렌더 비교. 토스트·배지·390/1280 넘침 0까지 함께 본다.

- **v0.4 (2026-07-31 · 기획 → 코드)** — 사용자 반문 **"추천대로하는데 근데 왜 cc로 넘겨 너가못해?"** 로 분업 전제가 뒤집혔다. 사양서를 넘기는 대신 **내가 직접 구현**하고, 두 번째 결정 **"3쓰임 전부 대응"** 에 따라 한 파일이 현장·미리듣기·리허설을 전부 맡는다. 커밋 `7ba065b`.
  ①**`assets/ritual-cue.js`** — 큐를 만드는 유일한 자리. 화면은 계산하지 않는다. 핵심은 **CUE_FIRE_RULE**: 큐마다 '뒤에 사람 구간이 붙는가'만 적으면 수동/자동은 **계산된다**(앞 큐에 `live`가 있으면 손으로, 없으면 이어서). 규칙 하나가 §3-A 20큐 판정표를 번호까지 그대로 재현함을 실행으로 확인 — `damback+bless=on` → 수동 10 `01,05,11,12,14,16,20,24,47,49` / 자동 10.
  ②**`console.html`** — **오디오가 한 개도 없어도 텍스트 카드로 끝까지 진행된다**(현재 클립 0개라 이게 전제다). 화면 버튼 4개 + BGM 슬라이더 1개 → 1차 예식 **0원 운용**. 패닉은 §6 12번 추천대로 **롱프레스 1.5초**.
  ③**`scripts/check-ritual-cue.js`** — 5코스 × 확장축 **17,280조합 전수** 검사 14항목. merge-guard가 호출한다.
  ④**★실사고 1건 발견·수정** — 390 실렌더 스크린샷에서 **입장 화면에 "이 자리는 두 분이 직접 말하는 시간입니다"** 가 떠 있었다. 입장은 걷는 자리다. 원인은 `live.self` 하나가 '말하는 자리'와 '움직이는 자리'를 겸하고 있던 것. 이 표시의 목적이 사용자 지시대로 *"직접 대사하는 곳"* 을 알리는 것이므로 **의도가 정반대로 뒤집힌 상태**였다. `live.doing`(`say`·`move`·`sing`)으로 갈라 15개 사람 구간을 전수 재분류하고, self만 있고 doing이 없으면 검사가 막게 했다(**LIVE_DOING**).
  ⑤**미리듣기 시간 압축 2단** — 엔진이 사람 구간 10초·뒤처리 1200ms로 줄이고(원값은 `full*`에 보존), 화면이 클립 없는 자리를 문안 길이 기준으로 줄인다. **현장 모드는 원래 길이를 지킨다** — 리허설의 목적이 타이밍 체감이라서. 이 비대칭을 검사가 양방향으로 고정한다.
  ⑥검증: merge-guard `ALL MARKERS OK` · 큐 검사 14항목 전부 통과 · 390/1280 실렌더(가로 넘침 0 · 페이지 오류 0) · **미리듣기 무개입 완주 164초 15칸**.
- **v0.3.2a (2026-07-31 · 큰틀 최종 점검)** — §0~§2를 §3-A 확정안과 대조. **모순 1건 수정**: §2 `[쓰임 A]` 블록에 v0.3.1 문장 `수동 '다음' 4곳(서약·반지·편지·덕담) = 큰 버튼 하나`가 남아 있어 v0.3.2 모델(자동 10 / 수동 10 · 볼륨 버튼 0개)로 교체. §0 헤더 버전도 v0.3→v0.3.2. **코드 실측 재대조 6건 전부 통과** — `order-preview.html:416`(4필드 기본값) · `:413`(`welcome:'self'` 고정) · `:520`('나레이션 대행' 복원 금지 주석) · `pick` 고유 16종(§2-C) · 발화 블록 `self:` 11개 → 클립 합계 17(§2-A) · 대본 53클립·3,858음절. **점검자 자기정정**: 중간 집계에서 `self:` 18개로 세어 §2-A "약 17개"와 불일치를 의심했으나, 18은 파일 전역 문자열 수(ENTRY 6종 예문 + 렌더 참조 1)이고 §2-A가 인용한 범위 `1500-1552`는 정확히 11필드·클립 17개다 — **기획서가 맞고 중간 집계가 틀렸다.**
- **v0.3.2 (2026-07-31)** — 사용자 지시 **"최대한 높은 진행 품질 · 최소한의 손 · 현장 컨트롤을 최소 조작"** 에 맞춰 §3-A **전면 개정**. ①**설계 원칙 확립** — *사람이 기다려야 하는 지점만 버튼, 시간이 정해진 것은 전부 자동 체인*. 조작 감축은 버튼을 없애는 게 아니라 **한 번 누르면 여러 개가 따라오게 묶는 것** ②**★실측 정정** — v0.3.1의 "디렉터 조작 27회"는 **중복 계상**. 수동 '다음' 4회(`14`·`16`·`20`·`24`)가 본식 13클립에 이미 포함, 반대로 후반 3클립(`46`·`47`·`49`)은 누락 → **정확히 20회** ③**20클립 전수 판정표** 신설 → **자동 10 · 수동 10**, 남은 10회는 전부 "사람 말이 끝났는가/움직였는가" 판정 지점이라 **물리적 하한** ④**v0.3.1의 볼륨 버튼 4개를 전부 삭제** — 음악 끊기·넣기는 `16`→`29` 체인(페이드 8초→침묵 3초→하드 인)에 흡수, 롱페이드는 `49` 종료 후 90초 자동, 더킹 홀드는 편지 구간이 애초에 무음이라 복귀 대상 없음 ⑤대신 **보조 버튼 3개**(★[대기 클립]`N4`·[음악만 멈춤]·[뒤로]) — 전부 사고용, 정상 진행 0회 ⑥**체인 안전 규칙 3개**(다음 수동 지점에서 정지 · 입장 구간 타이머 자동 금지 · 다음 큐 크게 표시) ⑦큐시트 30행 정정 대상 명시 ⑧신규 결정 1건(12번 패닉 버튼). **결론: 정상 진행 10회 / 사고 포함 11~12회** — v0.3.1안(24~26회) 대비 절반 이하이면서 품질은 상승(전대본 157행).
- **v0.3.1 (2026-07-31)** — 사용자 질문("BGM·나레이션 따로 · 버튼으로 볼륨 조작 · 관련 기계")에 대응해 **§3-A 콘솔 조작 사양** 신설. ①**2트랙 분리 채택** — 근거는 큐시트 14행(반지 구간 진행 중 음악만 먼저 페이드아웃)이 한 타임라인으로 표현 불가라는 점 ②**상시 수동 볼륨 기각** — 큐시트 23·25행 + 디렉터 조작 27회 실측 ③**자동 불가 큐 4개**(선언 앞 침묵·선언 후 하드인·편지 홀드·폐식 롱페이드)에 **전용 버튼** 배치 ④입력 장치 실측 비교 — nanoKONTROL2 98,000원(Web MIDI 직결·Chrome/Edge만) vs Stream Deck 207,500원(단축키 방식·포커스 리스크), 아날로그 믹서는 오답 판정 ⑤큐시트 23행 개정 필요 명시 ⑥신규 결정 2건(10·11).
- **v0.3 (2026-07-31)** — 사용자 결정으로 **배역별 AI 음성 4종 채택**. ①v0.2 '역전 2' 철회 — 반대 논거② "상품 구조를 데모가 거짓말한다"가 **실측 오류**였음을 기록(`guestVoice`·`entryVoice`의 `'couple'` 옵션이 이미 "녹음·AI"로 당사자 목소리를 판매 중) ②배역 음성을 코스별 완성 mp3가 아닌 **블록 단위 클립 라이브러리 ~17개**로 설계 — 커스텀(쓰임 C) 대응 + 제작량 24분→3.4분 ③안전장치 4개(라벨·고지·낭독톤·가상 인물 한정) ④콘솔은 트랙 3 미로드 ⑤신규 결정 2건(하객 군중 소리 · 배역 보이스 조합) ⑥**사용자 지시로 §2-C 배지 2종 추가** — '예시' 배지(임의 대사 고지)와 '선택' 배지(고를 수 있는 부분 표시). ②의 원천이 `COURSES[].seq[].pick` 16종으로 **이미 존재**함을 실측 · 신규 결정 1건(배지에서 즉석 변경 허용 여부).
- v0.2 (2026-07-31) — 큰틀 재검증. 주종 역전(콘솔=본체) · 배역 음성 강등(→v0.3에서 철회) · BGM 구독 승격 · 무음 폴백. git `23fe58a`.
- v0.1 (2026-07-31) — 최초 제안(3단계). git `67c60d6`.
