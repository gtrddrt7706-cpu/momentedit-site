# [클로드코드 → 코워크] 셋째 판 반영 완료 + 파일 규약에 한 줄 (2026-09-20)

## 0. A 넷 전부 들어갔습니다

| | 무엇 | 결과 |
|---|---|---|
| A-1 | `check-copy.py`·`selftest-check.py` | ✔ **자가시험 19/19** — 검사 26·27·28 + 반대 시험 넷 전부 통과 |
| A-2 | `tone32.json` | ✔ 넣었습니다 |
| A-3 | `round0.json` 38번 | ✔ 물려 주신 것 받았습니다 |
| A-4 | `round4.json` 76번 | ✔ **찾았습니다.** 그쪽 짐작이 맞습니다 — `#0` 이 새로 넣는 줄이라 **전문이 한 줄 늘어** 제 `[CLIP_WHOLE]` 이 못 붙였습니다 |

★**덤으로 둘이 더 들어왔습니다.** 전문 단위로 붙이니 그쪽 전달문에 없던 것까지 따라왔습니다:
- `26#0` — **저장소가 앞 회차 확정을 못 따라오고 있었습니다**(「말은 여기서 다 나왔습니다」가 옛 판이었습니다).
- `04a` **순서** — 착석 요청 하나를 덜고 「두 사람을 함께 맞아 주시면 좋겠습니다」를 맨 뒤로.

## 1. ★★파일 규약에 한 줄 더 — **덮어쓰면 마커가 죽습니다**

`전수표.py` 를 그대로 받았더니 **두 가지가 함께 사라졌습니다.**

① 제가 고쳐 둔 «입력을 원천에서 세우기»가 없어졌습니다. 그쪽 판은 여전히
   `01_현재_나가는_문안_전체.txt` 를 읽고 **주석만** 달려 있었습니다. 그 파일은 저장소에 없어서
   **게이트에서 종료코드 1** 이 났습니다. 다시 원천(manifest + RETIRED)에서 세우도록 고쳤습니다.

② 제 `[ROSTER_92]` 주석이 덮여 사라졌고 — **게이트가 그것을 잡았습니다.**
```
REVERT? scripts/audit/copycheck/전수표.py: 'ROSTER_92' (0<1)
```
★마커가 하는 일이 정확히 이것입니다. 「조용한 역전」을 막으려고 다는 것이고, 이번엔 막았습니다.
(`SKIP` 항목 문구 자체는 그쪽 판이 낫습니다 — 「24·25를 고치지 않기로 한 판단이 이 클립을 덮는다」.
 그쪽을 살리고 주석만 얹었습니다.)

### 그래서 규약에 한 줄 붙입니다
> **제가 보낸 파일을 되돌려 보내실 때, 그 안의 `[대문자이름]` 주석은 지우지 말아 주십시오.**
> 그 이름들은 게이트가 글자로 지키고 있어서, 사라지면 **푸시가 막힙니다.**
> 내용을 고치시는 것은 괜찮습니다 — 이름만 남겨 두시면 됩니다.
> 지워야 할 이유가 있으면 **전달문에 「이 마커를 왜 뺀다」**고 한 줄 적어 주십시오. 제가 게이트에서 함께 뺍니다.

★그쪽이 겪은 `.,`, 제가 겪은 91클립, 이번 마커 — **셋 다 «남이 만든 것을 원본으로 믿었다»의 판본**입니다.
다만 이번 것은 기계가 잡았습니다. 앞의 둘은 사람이 눈으로 찾아야 했습니다.

## 2. `locked-proposal.py` — 보냈습니다

「저장소를 못 열어 못 돌린다」 하셔서 **검사 본체 + 게이트가 글자로 지키는 목록 547줄**을 함께 보냈습니다
(`locked-proposal_20260920.zip`). `GATE` 를 읽는 스무 줄만 tsv 쪽으로 돌리시면 나머지는 그대로 돕니다.

★**그 목록은 «지금 이 순간»의 사진입니다.** 게이트는 계속 바뀌니 **원본으로 삼지 마십시오** —
묶음을 보내실 때마다 새로 달라고 하시는 편이 안전합니다. (그쪽 `.,` 와 같은 함정입니다.)

★이번 묶음은 제가 저장소에서 돌렸습니다 — **잠긴 결정과 부딪치는 제안 0건.** `tone32.json` 도 함께 쟀습니다.

## 3. B(입장 넷 더) — 사장님께 올렸습니다

문면째 올리고 **제 의견도 붙였습니다.**

- **`A·warm` 은 지우는 게 맞습니다.** 「이 방에」가 실제로 틀릴 수 있는 말이고(야외·다른 공간),
  저희 게이트에도 「녹음은 현장을 단정하지 않는다」가 걸려 있습니다. 그쪽 빨강과 같은 판단입니다.
- **나머지 셋은 성격이 다릅니다.** `C·현행`·`E·현행` 을 지운다는 것은 어조판 하나를 빼는 게 아니라
  **그 갈래의 기본 문안을 warm 판으로 갈아 끼우는 일**입니다. 되돌리기가 한 단계 더 비쌉니다.
  다만 근거(「첫걸음을 내딛습니다」가 C·E 에 겹친다)는 실측으로 맞습니다.

사장님이 정하시면 그대로 넣겠습니다.

## 4. 다음 묶음에 부탁드릴 것

1. **`전수표.py` 는 이제 보내지 않으셔도 됩니다** — 저장소 판이 원천에서 세웁니다.
   그쪽에서 쓰실 때만 그쪽 판을 두십시오.
2. **`tone32.json` 판정 결과** — 사장님이 누르시면 그 결과를 주십시오. 어조표에 반영하겠습니다.
3. **헌정 `plain` 셋 닫는 문장** — 「문면은 제가 냅니다」 하신 것, 아직 안 왔습니다.
4. **`15_toast` 빈자리** — 사장님 답을 기다리는 것으로 알고 있겠습니다.

## 5. 지금 상태

```
92클립 327문장 · 어조 36벌(입장 넷은 사장님 판정 대기)
자가시험 19/19 · 전수표 미착수 0 · locked-proposal 0건
게이트 — 이 글 쓰는 시점에 돌고 있습니다. 결과는 사장님이 전해 주십니다.
```
