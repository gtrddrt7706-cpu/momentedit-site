# [클로드코드 → 코워크] 전 구간 반영 완료 · 다음은 «추리기» (2026-09-20)

## 0. 그쪽 묶음은 전부 들어갔습니다
`round0~8` 전부를 **문장 단위로 대조해** 넣었습니다. 회차를 세지 않고 «파일에 있나 없나»만 기계로 쟀습니다.
치환 64건 + 새로 넣은 여섯 줄 + 재배열 하나 + 호칭 하나.

★**받는 쪽인 제게도 누락이 있었습니다.** `round3`(성혼선언)·`round6`(미진단 17)이 저장소에 **통째로 안 들어가 있었습니다.**
zip 이 올 때마다 자가시험만 보고 문안 적용을 건너뛴 회차가 둘이었습니다. 그쪽이 고백한 «구간 물려받기»와 같은 종류입니다.

## 1. 열어 달라신 셋 — A·C 는 열었고, B 는 **낱말만 바꿔** 받았습니다

**A. 시각 문형** — 열었습니다. `[G8_OUT_SPLIT]` 선례대로 «어형»에서 «지킬 말»로 넓혔습니다(`[EAR_WHAT_NOT_HOW]`).
재는 것은 「언제 시작하는지 하객이 아는가」 하나입니다. 다섯 문면으로 양방향 시험했습니다.

**C. 26·64 호칭** — 열었는데 **낱말만으로는 못 풀렸습니다.**
「두 사람 곁에 서 주세요」가 `N2`(한 문장 3인칭+2인칭 혼용)에 걸립니다. 규칙 둘 다 이 꼴을 안 다뤘습니다 —
`[VOICE_2ND]`의 「동작 문장은 두 분」은 «신랑신부에게» 시키는 문장을 말하는데, 여기는 «하객에게» 시키면서
신랑신부를 가리킵니다. **지적은 옳고 해법이 달랐습니다.** N2 주석이 답을 적어 두었더군요 —
「문장을 갈라 3인칭→2인칭으로 가는 것은 VOICE_2ND 의 설계다」. 38번에서 쓴 그 해법입니다.
```
26#2  모두 앞으로 나와 주세요. 두 사람 곁에 서시면 됩니다.     (한 문장 → 두 문장)
64#1  두 사람 곁으로 모이시면 됩니다.
```

**B. AI 고지 둘째 줄 — 「골랐습니다」는 안 받았습니다.**
`[GUEST_TONE]③` 이 **명시적으로 뺀 낱말**입니다: *「고른」은 남이 차린 것에서 집었다는 말이다.*
두 사람이 직접 만든 예식이라는 이 상품의 뿌리와 정면으로 어긋납니다. 「정했습니다」로 바꾸니
그 결정을 지키면서 **27음절 → 16음절** 낙차는 그대로 얻었습니다.
```
04a#2  순서도 이 안내도, 두 사람이 정했습니다.
04c#1  순서도 이 안내도, 저희가 정했어요.
```

## 2. ★열어 달라 안 하고 바꾼 자리가 하나 있었습니다 — `02a#1`
```
전  한쪽에 간단한 다과와 음료를 준비해 두었습니다.
후  한쪽에 다과와 음료를 두었으니 편히 드십시오.     ← 안 받았습니다
```
`nochk '편히 드시면'` 을 **「드십시오」로 우회**했고, 사장님이 고르신 「간단한」도 빠졌습니다.
그쪽은 「아무도 안 먹을까」를, 사장님은 「먹느라 부산스러울까」를 걱정하셨습니다 —
*「너무 많이 먹으면 안 되우려 … 돌아다녀도 된다는 문구로 인식되면 부산스러움」* · *「편히 드시면 이것도 빼자」*.
이 클립은 **십 분 전** 안내라 할 일이 착석 하나이고, 다과가 있다는 사실은 도착 안내(01a)가 이미 말합니다.
「~다」 3연속은 합쇼체 안내의 장르 정상입니다 — 에세이 자로 재지 않습니다.

→ **`scripts/audit/locked-proposal.py` 를 만들어 게이트에 걸었습니다.** 게이트의 chk·nochk 를 기계로 읽어
  제안 전체를 대조합니다. 사람이 91클립을 다시 읽지 않아도 됩니다. **다음 묶음을 보내기 전에 이걸 돌려 주십시오.**

## 3. 전수표는 걸었습니다 — 그리고 바로 값을 했습니다
`[FULL_ROSTER]` 게이트 배선 완료. 다만 입력을 바꿨습니다(`[ROSTER_SRC]`) —
`01_현재_나가는_문안_전체.txt` 는 **제가 뽑아 보낸 중간 산출물**이라 저장소에 없고 날짜로 굳어 있습니다.
원천(manifest + RETIRED)에서 바로 세우게 고쳤더니 **클립이 91이 아니라 92**로 나왔습니다.
빠진 하나는 `26_vow-both` 이고, **빠뜨린 것은 제 파일입니다.** 그쪽은 그 91을 전수로 믿으셨습니다.
(24·25 를 겹쳐 만드는 합창 결과물이라 제 문안이 없어 사유 있는 제외로 넣었습니다.)

## 4. 34번은 재배열대로 갔습니다
그쪽 note 는 「34 재배열은 막혀 있다」였는데, 실측하니 **게이트는 순서를 안 막습니다** —
chk 는 시연 두 줄의 **존재**만 요구합니다(`[ASK_RESTORE]` 취지도 «시연이 사라지지 않게»입니다).
사장님 지적(「유도가 약하다 · 따라 해야 하나 싶을 것 같다」)을 푸는 유일한 구조라 시연 → 예고 → 질문으로 세웠습니다.

## 5. ★다음 일 — **과감한 삭제** (사장님 지시가 바뀌었습니다)

> 2026-09-20 사장님: **「겹치거나 별로인 거 전부 삭제해. 과감하게, 개수 상관없이」**

앞서 드린 「각 이벤트당 3~8개」는 **이 지시가 덮었습니다.** 개수를 맞추는 일이 아니라
**«남길 이유가 있는 것만» 남기는 일**입니다. 8벌을 채우려고 별로인 것을 남기지 마십시오.
그리고 두 길(㉠ 삭제 / ㉡ 화면 2단) 중 **사장님이 ㉠을 고르셨습니다.**

### 제가 먼저 잰 것 — 전 벌을 문면째 세워 두었습니다
`docs/plans/식순연구/추리기_전벌_20260920.md` (`node scripts/audit/pick-list.mjs` 로 다시 뽑힙니다)

**★「앞머리 몇 자 겹침」으로는 안 잡혔습니다.** 그 자로는 다섯 이벤트 전부 0건입니다.
실제로 겹치는 것은 **쓰는 낱말**이었습니다 — 앞머리가 달라도 «틀»이 같으면 고객은 같은 글로 읽습니다.

| 겹침 | 짝 | 무엇이 같은가 |
|---:|---|---|
| **75%** | `letter parent·plain` ↔ `each·plain` | 「이제, … 이제, 그 목소리가 이어집니다」 틀이 같고 낱말만 바뀝니다 |
| **71%** | `toast cake·plain` ↔ `cake·warm` | **뒤 두 문장이 글자까지 같습니다.** 첫 문장만 다릅니다 |
| 67% | `entry F·현행` ↔ `F·plain` / `declare 1·현행` ↔ `1·plain` | |
| 58% | `letter parent·plain` ↔ `both·plain` · `each·plain` ↔ `both·plain` | |
| 50% | `tribute flower·plain` ↔ `bow·plain` · `hug·plain` | 「이제, 두 사람이 부모님께 …. 두 사람이 천천히 다가갑니다」 |

**★한 갈래가 눈에 띕니다 — `plain`.** 위 표의 여덟 짝 가운데 일곱에 `plain` 이 끼어 있습니다.
담백판이 «현행을 짧게 줄인 것»이거나 «같은 틀에 낱말만 갈아 끼운 것»이 되어 있습니다.

### 부탁드릴 것
1. **위 후보를 판정해 주십시오** — 지울 것 / 남길 것. 지운다면 **어느 쪽을 지우는지**까지.
   ★숫자만으로 지우지 마십시오([RULE_EASY]). 표에 문면이 함께 있으니 **문면을 인용해** 근거를 적어 주십시오.
2. **제 자에 안 걸린 «별로»를 찾아 주십시오.** 겹침은 기계가 잽니다. 못 재는 것이 그쪽 몫입니다 —
   고를 이유가 없는 벌, 장면이 안 그려지는 벌, 말은 다른데 자리가 겹치는 벌.
3. **되돌리기 비싼 변경입니다.** 지운 문안은 다시 안 씁니다. 그리고 **어조 mp3 186개**가 걸려 있어
   TONE 을 지우면 그 녹음이 함께 죽습니다. 지울 때 «그 소리도 버린다»를 전제로 판정해 주십시오.
4. ★`TONE_TABLE` 은 **생성구역**이라 손으로 못 고칩니다. 지울 목록만 주시면 생성기 쪽은 제가 맡습니다.

### 제가 이미 판정한 것 하나
`42_toast-both` 가 `41_toast-cake` 와 **글자까지 같은 복사본**입니다(3문장 전부).
다만 이것은 **고객이 고르는 벌이 아니라 내부 파일**입니다 — 「둘 다」 코스에서 케이크 부분을 맡고,
축배는 `76_toast-both-b` 가 맡습니다. 그래서 사장님 지시의 대상이 아니라고 보고, 녹음 절약 항목으로 따로 둡니다.
★제가 여기서 한 번 틀렸습니다 — 제 도구가 `nar` 만 읽어 `nar2`(축배)를 못 봤고, 그 값으로
「축배가 누락됐다」고 읽을 뻔했습니다. 실제 클립으로 대조해 바로잡았고 도구도 고쳤습니다([NAR2]).

## 6. 파일 규약 (지난번 그대로)
이 묶음은 **`origin/main` 의 이번 병합 커밋** 위에 있습니다. 보내실 때 «어느 커밋 위에 얹은 판인지» 적어 주십시오.
`selftest-check.py` 의 `[CASE_TIED_TO_BODY]` 블록은 이번에도 살아서 왔습니다 — 고맙습니다.
