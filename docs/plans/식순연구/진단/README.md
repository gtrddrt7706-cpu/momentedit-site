# 나레이션 AI냄새 진단 넷 [DIAG_FIRST]

`humanize-korean` 판정 체계(71패턴 taxonomy)로 식순 전 구간을 진단한 문서다.
합 1,413줄.

| 파일 | 구간 |
|---|---|
| `진단_1_식전입장첫인사.md` | 식전 안내 · 입장 · 첫인사 |
| `진단_2_서약반지선언.md` | 혼인 서약 · 반지 · 성혼 선언 |
| `진단_3_편지덕담헌정.md` | ★편지 낭독 · 부모님 덕담 · 헌정 |
| `진단_4_축배폐식지시.md` | 축배 · 케이크 · 폐식 · 지시 큐 |

## ★왜 여기로 옮겼나 (2026-09-20)

**원래 `_workspace/flow/` 에 있었는데 그 폴더는 `.gitignore` 에 있다**(38행).
즉 이 넷은 **저장소에 없었고, 만든 세션의 로컬에만 있었다.**
다른 세션·코워크·미래의 나는 이 문서가 있는 줄도 모른다.

실제 사고 — 2026-09-20 에 편지 여섯의 「억지감동」이 무엇인지 처음부터 다시 세었다.
결론은 `진단_3` 의 **P4 C-1 기계적 병렬**과 같은 것이었다. 반나절을 버렸다.
`_workspace/` 가 「캡처 비교용 작업 산출물」 부류라 함께 묻힌 것인데,
**이 넷은 임시 파일이 아니라 결과물이다.**

★교훈 — 다시 읽힐 문서를 ignore 된 폴더에 두지 않는다.
  ignore 폴더는 «지워도 되는 것»이라는 뜻이고, 검색도 안 된다.

## 쓰는 법

**밖에서 다시 진단하기 전에 여기부터 읽는다.** 그 다음에 낡았는지 확인한다 —
문서가 인용한 문장이 지금도 살아 있는지 세어 보면 된다(진단_3 은 17개 중 13개가 살아 있었다).

원본 텍스트는 `_workspace/flow/*.txt` 에 있으나 그쪽도 ignore 다.
지금 문안의 원천은 `docs/plans/식순연구/배역_예시_대사.txt` 와
`docs/plans/식순연구/타입캐스트/manifest.json`(생성물) 이다.
