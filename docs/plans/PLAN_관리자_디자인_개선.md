# PLAN · 관리자 페이지 디자인 개선 (운영 효율 · 위계 · 안전)

> 작성: 2026-07-26 (코워크 · 3렌즈 병렬 1차 점검: 운영 효율 / 정보 위계·시각 / 안전·실수 방지)
> **역할: 기획·점검 = 코워크 / 구현 = 클로드 코드.** 마이페이지 개선(PLAN_마이페이지_디자인_개선.md)과 같은 프로세스.
> 디자인 헌장(§0 7원칙)·H1 토큰·H2 피드백 매핑·§6-B 보이스 가이드를 관리자에도 준용한다.
> 이 문서가 단일 진실원. 상태: `제안`/`확정`/`반영`/`보류`/`기각`.

## 0. 한 줄 진단

골격(인라인 큐·CARD_STAGE_ORDER·위험 액션 격리)은 이미 우수하다. 남은 문제는 네 갈래 —
①긴급 신호가 약하고 반복 손입력이 남았고(효율) ②금전 확인 모달에 금액이 없고 입금 확인·환불의 되돌리기가 없으며(안전)
③자동 알림 미예고+사후 토스트 모순으로 이중 알림 위험(알림) ④AI 직원실이 별개 디자인 시스템으로 갈라져 있다(일관성).

## 1. 배치 AA — 프론트 퀵윈 (admin.html만 · 재배포 불필요)

| # | 항목 | 내용 | 근거 | 상태 |
|---|---|---|---|---|
| AA1 | 긴급 큐 시각 세기 | qrow.red 알파 0.025→0.06+ · 좌측 색 바 3px · "🔴N 급함"을 헤더 구석 11.5px에서 처리할 일 제목 옆 배지로 승격 | CSS 68 · 배지 1090(renderQueue sect-h) | 반영(1차 스프린트 PR① #261) |
| AA2 | 완료성 동사 사전 | "결과물 전달"(1730) vs "결과물 전달 완료 처리"(1739) 이중 라벨 통일 + 처리/전달/완료/착수 동사 규칙 1개 | 1730·1739 · 버튼사전 1070 | 반영(1차 스프린트 PR② #263) |
| AA3 | 파괴 클래스 통일 | 취소=btn-ghost vs 노쇼=btn-danger 혼용 → 파괴 계열 한 옷(danger 아웃라인) | 취소 1425·1426·1427·1428(btn-ghost) · 노쇼 1430~1432 · 미계약 1525(btn-danger). 임시고정 거절·해제 1435~1436은 가역이라 danger 제외(AA9 원칙 정합) | 반영(1차 스프린트 PR① #261) |
| AA4 | 백오피스 톤 정리 | 합쇼체 6+곳(1759·412·439·590·629·753)·전각 줄표(1734·1759) → 해요체·'·' (관리자용도 규칙 정합) | 합쇼체 412·439·590·629·1759 등 · 전각 줄표는 고객·관리자 노출 문자열만(주석 제외) | 반영(1차 스프린트 PR② #263) |
| AA5 | 결과물 링크 붙여넣기 | 모달에 "붙여넣기" 버튼(clipboard.readText · https면 자동 채움) — 하루 최다 손입력 제거 | 2101~2105(rOrig·rReto·rVideo 필드) | 반영(1차 스프린트 PR③ #264) |
| AA6 | 최근 본 고객 칩 | openDetail 시 localStorage 기록 → 홈 검색창 아래 최근 5명 칩. 폰 재방문 재검색 제거 | openDetail 1289 · 검색창 295 | 반영(1차 스프린트 PR③ #264) |
| AA7 | 자동 갱신 | visibilitychange 시 + 60~90초 조용한 폴링으로 loadHome 재호출(진행바 없이) — 폰 두고 온 사이 새 입금신호 반영 | loadHome 1039 · _view 상태 | 반영(1차 스프린트 PR③ #264) |
| AA8 | 벌수 이중 저장 제거 | 큐 인라인 저장 후 여는 상담완료 모달 onYes의 adminSetFittingCount 재호출 건너뛰기 | 인라인 저장 1113 → doConsultDone 1885 · onYes 재호출 1902 | 반영(1차 스프린트 PR④ #266) |
| AA9 | 쿠폰 회수 과잉 완화 | 저위험에 danger 모달 → 일반 모달 | doRevokeCoupon 1991 | 반영(1차 스프린트 PR① #261) |
| AA10 | 일반 큐 aging 승격 | 방치 임계(7일+) 건을 "오래 방치" 미니 그룹으로 상단 승격(단계순 1차 정렬 유지) — 프론트 재그룹 가능 여부 점검 후, GAS 정렬 수정이 필요하면 AC로 이동 | G:827 정렬 · **2차 판정: `_wait`가 queue 객체에 그대로 실려 프론트 도달(admin.gs 반환 `queue:{urgent,normal}` 무가공) → 프론트 재그룹 가능** | 반영(1차 스프린트 PR④ #266) |

## 2. 배치 AB — 안전·알림 (모달 표시 중심 · 프론트 위주, 일부 서버 값 필요 시 점검 후 분리)

| # | 항목 | 내용 | 근거 | 상태 |
|---|---|---|---|---|
| AB1 | 금전 확인 모달 금액 표시 | 입금/중도금/잔금/묶음 확인 모달에 해당 마일스톤 금액·입금자명 표시(큐 sub에 이미 있는 값을 상세 모달로 통일). 버튼 라벨에 금액 병기 검토 | doConfirmPay 1910·doConfirmBalance 1911·doConfirmMidBal 1912·doConfirmMid 1913 · 큐 sub(G:747·751·755·684) · 상세 1530~1551 | 반영(1차 스프린트 PR⑤ #268) |
| AB2 | 환불 모달 견적 표시 | 환불 완료(인라인 포함) 확인 화면에 refundQuote(예상 환불액·공제 내역) 표시 | doMarkRefundedInline 1873 · doMarkRefunded 1877 · 상세 렌더 1556~1557 | 반영(1차 스프린트 PR⑤ #268) |
| AB3 | **자동 알림 예고+토스트 모순 해소** | ①자동 발송 액션(계약서 발송·상담완료·입금/중도금/잔금 확인·결과물 등록/전달) 모달에 "확인 시 고객에게 카톡이 자동으로 가요" 1줄 ②afterAction의 "카톡 알림 잊지 마세요" 토스트를 "자동 발송됨"으로 수정 — **점검 필수: 이벤트별 실제 자동 발송 여부를 95_notify 기준으로 전수 대조 후 문구 분기**(수동 필요 이벤트만 리마인드 유지) | 1845~1846 · 95_notify | 반영(1차 스프린트 PR⑥ #272) |
| AB4 | 결과물 등록 발송 예고 | 링크 등록 모달에 "원본·보정본 저장 시 고객에게 도착 알림이 바로 가요" 명시 — 임시 저장 의도의 조기 발송 사고 방지 | 2101 모달 · **2차: resultOriginal/resultRetouch는 상태 전이 1회만 발송(RESULT_NOTIFY_STEPS `_nfOrig`/`_nfReto`) → 문구는 "처음 등록하면"으로 정확히** | 반영(1차 스프린트 PR⑤ #268) |

## 3. 배치 AC — GAS 포함 중작업 (묶어서 재배포 1회)

| # | 항목 | 내용 | 추천 | 상태 |
|---|---|---|---|---|
| AC1 | 입금 확인 취소 액션 | 오처리 복구 경로 전무(ROLLBACK_KEEP_PAID가 보존) → adminUndoConfirm류 전용 멱등 액션+처리이력 | 진행 추천(금전 실위험 1위) | 반영(2차 스프린트 PR① #275) |
| AC2 | 환불 완료 취소 | adminMarkRefunded 역방향 부재 → 취소 경로 | 진행 추천 | 반영(2차 스프린트 PR② #276) |
| AC3 | 강제변경 안전 게이트 | 지워질 컬럼·동의기록 dry-run 미리보기 + 체크박스 게이트("이후 데이터 초기화에 동의") · 드롭다운 기본값 비선택화 | 진행 추천 | 반영(2차 스프린트 PR③ #278) · **후속 수정 필요(6차 검증 AC3-BUG · 미리보기가 캘린더 이벤트 실삭제)** |
| AC4 | 월 사업현황 | aiMorningReport에 "이번 달 계약 N·매출 ₩X·전달 N" 1줄 + 상단바 매출 패널(AI비용 패널 패턴 재사용 · 계약총액 월합계 신규 집계) | 진행 추천(0클릭 아침 도착) | 반영(2차 스프린트 PR④ #279) |
| AC5 | 알림 자동/수동 정책 정리 | AB3 점검에서 드러나는 "수동 필요" 이벤트를 자동 발송으로 승격할지, 템플릿 부재분은 "카톡 문구 복사" 버튼으로 할지 | 점검 결과 따라 | 반영(2차 스프린트 PR⑤ #280) |

## 4. 배치 AD — 시스템 중작업 (점진)

| # | 항목 | 내용 | 추천 | 상태 |
|---|---|---|---|---|
| AD1 | 카드 요약 수치 분리 | cardPayment(1532)·cardResult(1551·1717) 요약 문장의 금액·D-day를 .kv 우측 열로(헌장 4) — 줄당 <b> 다수 경쟁 해소(헌장 1) | 진행 추천 | 제안 |
| AD2 | 진사 단일화+상태 칩화 | .qbtn 전부 진사(77) → 결정적 1건만 진사·나머지 골드 아웃라인(헌장 5) · 입금/계약/결과물 상태 <b>텍스트를 STAGE_COLOR 계열 미니 칩으로 표준화 | 진행 추천 | 제안 |
| AD3 | AI 직원실 시스템 통합 | 400~925줄 인라인 hex 96종·네이티브 alert 19/confirm 10/prompt 3를 CSS 변수·btn-*·openModal로 흡수(H2 매핑) · 다크 감광은 이때 검토 | 후순위 | 보류 |
| AD4 | 토큰 수렴 | font-size 20종·hex 96종 → 신규 코드부터 H1 스케일 강제(전면 교체 금지) | 원칙 채택 | 확정(원칙) |

## 5. 잘 되어 있는 것 (수정 금지 목록)

인라인 큐 설계(반복 4작업 2클릭)·큐→상세 자동 스크롤·CARD_STAGE_ORDER·STAGE_COLOR 중앙 사전·버튼 공식 5종 절제·
강제변경 격리(접힘+빨강+사유 필수+이중확인)·btn-danger 아웃라인·미수금 가드(needForce)·전 금전 액션 멱등·슬롯 재검증(더블부킹 0)·
서명 계약 보호·SILENT_FAIL_ALERT·netFail/authFail 구분+자동 재시도·처리이력 감사 로그·topBusy/mBusy·현금영수증 모달(금액 표시 모범)·
벌수 필수화·모바일 단일 컬럼(16px 입력·44px 타깃·SPA 뒤로가기)·14일 토큰·비번 해시·전기기 무효화·목록 민감정보 미노출.

## 6. 점검 이력

- **1차 · 2026-07-26 (코워크)** — 3렌즈 병렬(운영 효율/위계·시각/안전). 대표 작업 4개 클릭 실측(승인·입금·전달=2클릭, 결과물 등록=3클릭+1전환), 헌장 7원칙 대조(4·5 위반, 6은 마이페이지보다 엄격), 위험 액션 18종 전수(과소 4·과잉 1), 알림 예고 전수(자동 발송 8종 미예고+토스트 모순). 실측: font-size 20종·인라인 hex 96종·alert/confirm/prompt 32회(AI 직원실).
- **2차 · 교차 점검(클로드 코드) · 2026-07-26** — AA1~AA10·AB1~AB4 줄번호를 현재 main(89592c4) admin.html 기준 전수 재확인, 어긋난 근거 열 갱신(위 표). blocker 0건.
  - **AA10 경로 판정 = 프론트 가능**: `adminHome`이 `queue:{urgent:urgent, normal:normal}`를 가공 없이 반환해 `_wait`(대기 시작 YMD)·`_stage`·`_urgent`가 프론트까지 그대로 도달. GAS 수정 없이 재그룹 가능 → AC 이관 없이 PR④에 포함.
  - **AB3 자동 발송 전수 재확인**(admin.html NH 11키 → 액션 함수 → notifyKakao 이벤트 → 95_notify NOTIFY_EVENTS): 코워크 1차 대조표와 **결론 일치**. 발송 게이트는 `off` 플래그 하나뿐(95_notify:105 `if (meta.off) return false`)이고 `need`는 관리자 수신 분기(151)에서만 쓰인다는 점을 코드로 확정. 자동 발송 O 8종(contractArrived·fittingRequest·depositToProduction·resultOriginal·resultRetouch·resultDelivered·consultDone·changeConfirmed·changeDeclined) / 자동 발송 X 3종(중도금·잔금·묶음 → 전부 `cust.paymentConfirmed`, 95_notify:82 `off:true` · 2026-06-12 사용자 결정).
  - 1차 대조표에 없던 뉘앙스 3건: ①`adminConfirmPayment`는 직접 호출이 아니라 `_confirmDepositCore`(admin.gs:1347)로 위임해 발송(결과 동일) ②`cust.changeConfirmed`는 `need:false`지만 `off`가 없어 발송됨(게이트가 `off`만 보므로 결론 동일) ③`adminSetResultLinks`의 원본·보정본 알림은 **상태 전이 1회만** 발송(링크 수정 재저장은 재발송 없음) → AB4 문구에 반영.
  - `off:true` 전수 = paymentConfirmed·cashReceiptIssued·holdGranted·holdReleased·archiveExpiring(5종). 관리자 액션이 걸리는 건 paymentConfirmed 계열 3종뿐. **코드로 켜지 않고**(사용자 결정 사안) 결정 대기함에 등재 후 문구만 정직화.

- **5차 · 2차 스프린트 반영(클로드 코드) · 2026-07-26** — 배치 AC 5건 + AB1 보완 1건 반영. `admin.gs`·`96_ai_cost.gs` 변경 포함이라 **`R3n9Mr` 새 버전 재배포 1회 필요**.
  - PR① #275 AC1 / PR② #276 AC2 / PR③ #278 AC3 / PR④ #279 AC4 / PR⑤ #280 AC5 + AB1 보완.
  - 신규 서버 함수 5종(`adminUndoConfirmPayment`·`adminUndoConfirmPreview`·`adminUndoRefunded`·`adminForceStagePreview`·`adminNotifyText`)과 읽기 전용 집계 `monthBusinessData`는 CLAUDE.md 실행 함수 위치표에 등재.
  - 판단 기록 3건: ①**되돌리기는 전용 경로로** — `_clearForwardData`의 ROLLBACK_KEEP_PAID는 '확인된 수납 보존'이 목적이라 되돌리기에 재사용하면 안 된다. ②**미리보기와 실행은 같은 함수로** — AC3는 실행 경로(`_clearForwardData`)를 report 모드로 부른다. 미리보기 전용 코드를 따로 쓰면 시간이 지나며 어긋나 '안전장치처럼 보이는 함정'이 된다. ③**금액·문구는 서버 단일 원천** — AB1 보완은 `_journeyAmounts`, AC5는 `_nfCustomerMsg`를 그대로 읽는다. 프론트에 복제하면 두 곳이 갈라진다.
  - 알림 발송 설정(`95_notify`의 `off`)은 이번에도 건드리지 않았다. AC5는 자동 발송을 켜는 대신 '문구 복사'로 우회했고, 재개 여부는 결정 대기함에 그대로 남아 있다.

- **6차 · 2차 스프린트 독립 검증(코워크) · 2026-07-26** — 병합된 main(8e3c139)에서 AC1~AC5·AB1보완을 코드로 재검증. 통과 항목과 **실버그 1건**.

  **통과** — merge-guard `ALL MARKERS OK` · 신규 함수 7종 전량 존재 · 변경 .gs가 `admin.gs`·`96_ai_cost.gs` 2개뿐임을 `git log --name-only`로 확인(70_journey·95_notify·80_production·consultation-booking 무변경) · `95_notify:82`의 `cust.paymentConfirmed` `off:true` 유지 · AC5 `adminNotifyText`가 `_nfCustomerMsg` 템플릿을 그대로 반환(알림을 켜지 않음) · AC4 `monthBusinessData` 매출이 계약총액이 아니라 '확인'된 실입금 합 · AB1보완 프론트(admin.html:2045~2047)가 `d.milestoneAmounts`만 읽고 산식을 복제하지 않음 · AC1 차단 A~F 6종 코드 확인 · AC1 멱등 2중(`cur !== '확인'` continue + `!plan.length` early return) · AC1 dry-run이 `_adminLock()` 앞에서 return하므로 쓰기 0 · 4개 .gs + admin.html 인라인 스크립트 구문 검사 통과.
  - A 차단의 키 대칭 확인: `_undoSpec`의 `payKey`(스냅 '예약금' / 시그 '계약금' / 중도금·잔금 동명)가 `98_pay_card.gs:229~237`의 `_payMarkCard(code, ledgerKey)` 호출 키와 전부 일치.
  - 잔금 되돌리기의 `잔금확정금액` 삭제는 대칭적으로 옳음: 기록처 3곳(`admin.gs:1415`·`70_journey:1321`·`70_journey:1412) 전부 잔금 확인 시점에만 쓴다.

  **★AC3-BUG · 강제변경 미리보기가 캘린더 이벤트를 실제로 지운다(수정 필요)**
  - 위치: `admin.gs:1972` — `_clearForwardData` 안의 `if (consentKeys.indexOf('가예약') !== -1 && rec.가예약 && ...) _holdCalDelete(rec.가예약);`
  - 원인: AC3는 "미리보기와 실행이 같은 함수를 쓴다"는 옳은 원칙으로 `adminForceStagePreview`(2013)가 같은 `_clearForwardData`를 report 모드로 부른다. 그런데 이 함수는 순수 계산이 아니다. 시트 쓰기는 없지만(`upd`를 반환만 함) **구글 캘린더 이벤트 삭제라는 외부 부작용이 하나 섞여 있다.** `_holdCalDelete`는 `ev.deleteEvent()`(70_journey:1545)로 실제 삭제한다.
  - 재현: 임시고정이 승인된(`동의기록.가예약.eventId` 존재) 고객 상세 → 고급 → 단계 드롭다운에서 '신청접수' 선택. `admin.html:1919`가 `change` 이벤트에서 바로 미리보기를 호출하므로, **동의 체크박스도 누르기 전에 · 모달을 닫아도 캘린더 슬롯이 이미 풀린다.** 시트의 `동의기록.가예약`은 그대로 남아 관리자·고객 화면엔 임시고정이 살아 있는 것으로 보이고, `eventId`는 지워진 이벤트를 가리킨다(시트-캘린더 불일치).
  - 범위: `ti < flow.indexOf(상담확정/촬영확정)`이라 목표가 '신청접수'일 때만(예외 단계는 `ti < 0`으로 상단 return). 좁지만, 하필 "임시고정을 잘못 승인했나 되돌려볼까" 하고 드롭다운을 열어보는 상황과 정확히 겹친다.
  - 고칠 방향(권고): 부작용을 공유 함수에서 빼 **실행 경로로 옮긴다.** `_clearForwardData`는 무엇을 지울지 계산·보고만 하고(`consentKeys`에 '가예약'이 담겼는지를 report나 반환값으로 알린다), `_holdCalDelete` 호출은 `adminForceStage`가 `touchCustomer` 직후에 한다. 차선책은 `if (!report && ...)` 가드지만, "report가 곧 미리보기"라는 암묵 결합이라 다음 사람이 또 밟는다. 원칙은 유지: 미리보기와 실행은 계속 같은 계산 함수를 쓰되, **쓰기·외부 호출은 실행 경로에만 둔다.**
  - 재발 방지: merge-guard에 음수 마커 1줄 — `_clearForwardData` 본문 안에 `_holdCalDelete(`가 0곳이어야 한다.

  **뉘앙스 1건(블로커 아님)** — 번들 되돌리기는 버튼이 2~3번. 계약금 확인이 수납묶음으로 중도금·잔금까지 확정한 건은 `undoPay:계약금`이 계약금만 되돌리고, 중도금·잔금은 각각의 '확인 취소' 버튼으로 따로 되돌려야 한다(서버 `_undoConfirmCore`는 `milestone='중도금잔금'` 콤보를 이미 지원하는데 `admin.html`에 그 버튼이 없다). 번들 경로가 `중도금확인일시`·`잔금확인일시`를 함께 찍으므로(admin.gs:1409~1411) E 차단에 걸리지는 않아 복구는 가능하다. 개선안: 확인 취소 모달에 "이 건은 한 번에 확정된 건이라 중도금·잔금도 따로 되돌려야 해요" 한 줄, 또는 `undoPay:중도금잔금` 버튼 1개 추가.

- **4차 · 2차 스프린트 0단계 교차 점검(클로드 코드) · 2026-07-26** — 배치 AC 착수 전 "되돌리기가 되돌리면 안 되는 것까지 되돌리는" 위험을 코드로 확정.

  **(1) 입금 확인이 실제로 바꾸는 것** — `_confirmDepositCore`(admin.gs:1299~1352). 코워크 6항목 전부 확인 + 1건 추가.
  | | 바뀌는 것 | 비고 |
  |---|---|---|
  | ① | `입금상태` = '확인' | |
  | ② | `동의기록.영수증기준일.예약금` = now | 현금영수증 5일 기한 기산점. **재확인 시 항상 덮어씀** · 계약금 확인 시각을 담는 유일한 값(별도 '입금확인일시' 컬럼 없음) |
  | ③ | 번들 시 `중도금상태`·`잔금상태` = '확인' + 각 `확인일시`(같은 `_bNow`) | 대상은 `동의기록.수납묶음.keys` 스냅샷 우선, 없으면 '완료신호'인 것만 |
  | ④ | 번들 잔금 시 `동의기록.잔금확정금액` 스냅샷 | **이미 값이 있으면 유지**(덮어쓰지 않음) |
  | ⑤ | `setCustomerStage(code,'paid')` | 역행 금지·최고수위 보호가 있어 **이미 더 진행된 고객에겐 no-op**(단계가 안 바뀜) |
  | ⑥ | `notifyKakao('cust.depositToProduction')` | 이미 나간 카톡 · 되돌릴 수 없음 |
  | ⑦ | **처리이력 1줄**(`_recordHandler` '입금 확인 → 입금완료 (일괄: …)') | 코워크 목록에 없던 항목 · 감사 흔적이라 되돌려도 남긴다 |

  중도금·잔금 개별 확인(`70_journey.gs:1310`·`1385`·`1404`)은 **단계 전이가 없고** 상태·확인일시(+잔금 스냅샷)만 바꾼다.

  **(2) 차단 조건 — 코워크 A~E 중 4건 그대로, C는 범위 축소, F 1건 추가.**
  | | 조건 | 판정 근거 | 코워크안 대비 |
  |---|---|---|---|
  | A | 카드로 확정된 마일스톤 | `동의기록.결제수단[원장키] === '카드'`(`_payMarkCard` 98_pay_card.gs:76) — 처리이력 '·카드' 문자열 파싱보다 확실한 구조화 신호. 키는 시그 계약금='계약금' · 스냅 계약금='예약금' · 중도금·잔금 동명 | **판정 소스 변경**(처리이력 → 동의기록.결제수단) |
  | B | 현금영수증 발행됨 | `동의기록.영수증발행[키]` · **콤보 키 '중도금잔금'도 함께 확인**(중도금·잔금을 따로 되돌릴 때 콤보 발행 기록을 놓치지 않게) | 콤보 키 보완 |
  | C | 다음 단계로 전진함 | 현재단계가 '입금완료'가 아니면 차단. **단, 계약금(·계약금 번들)에만 적용** | ★**축소**: 중도금·잔금은 원래 제작중·예식완료에서 확인하는 마일스톤이라(`_bStages`) 단계 전진을 이유로 막으면 정상 건이 전부 막힌다 |
  | D | 종료 고객(취소·노쇼·미계약) | `STAGE_EXCEPTIONS` · 확인 함수와 동일 가드 | 그대로 |
  | E | 24시간 경과 | `UNDO_WINDOW_HOURS = 24`(정책값 · 결정 대기함 등재). 기준 시각 = 계약금 `영수증기준일.예약금` / 중도금·잔금 `확인일시`. **시각을 알 수 없는 구데이터는 경과와 같게 차단**(별도 메시지) | 기준 시각 출처 확정 |
  | F | 환불 완료 처리된 건 | `동의기록.환불완료` 존재 시 차단. 환불액은 기수령액(=확인된 입금)으로 계산되므로 정산이 끝난 뒤 입금을 되돌리면 장부가 어긋난다 | **추가** |

  **(3) 되돌린 뒤 상태** — ①③은 `입금신호` 컬럼이 있으면 '완료신호', 없으면 빈값(대기)으로. ②는 계약금 되돌릴 때 제거 · ④는 잔금 되돌릴 때 제거 · ⑤는 '계약완료'로 원복(C 가드 덕에 항상 '입금완료'에서 출발) · ⑥은 되돌릴 수 없어 모달에 사실대로 적는다 · ⑦은 취소 이력을 한 줄 더 남긴다. `수납묶음` 스냅샷은 남긴다(다음 확인 때 같은 구성이 다시 쓰임). `ROLLBACK_KEEP_PAID`(admin.gs:1754~1759)는 강제변경 롤백의 안전장치라 손대지 않고, AC1은 별도 전용 액션으로 만든다.

- **3차 · 1차 스프린트 반영(클로드 코드) · 2026-07-26** — 배치 AA 10건 + AB 4건 전량 반영. 6개 PR로 쪼개 순서대로 병합(각 PR 사이 main 리베이스 · Merge Guard 초록 확인 후 병합). GAS 변경 0 · 재배포 불필요.
  - PR① #261 AA1·AA3·AA9 / PR② #263 AA2·AA4 / PR③ #264 AA5·AA6·AA7 / PR④ #266 AA8·AA10 / PR⑤ #268 AB1·AB2·AB4 / PR⑥ #272 AB3.
  - 판단 기록 3건: ①**파괴 vs 가역 경계** — 취소·노쇼·미계약은 danger, 임시고정 거절·해제는 되돌릴 수 있어 ghost 유지(AA3·AA9 같은 원칙). ②**AA10은 프론트 재그룹으로 충족** — 서버 정렬(단계순 → 대기순)을 건드리지 않고 `_wait` 7일+만 걸러 위로. `_wait` 없는 응답이면 종전과 동일하게 한 묶음. ③**AB1 금액 출처 분리** — 큐에서 열면 서버 요약 문장을 통째로, 상세에서 열면 기록된 값에서 조립. 앞단에서 요약 문장을 쪼개 해석하지 않는다(형식이 바뀌면 조용히 틀리기 때문).
  - 신규 코드에 H1 토큰 계단·H2 피드백 매핑 적용(붙여넣기 실패=인라인+focus · 처리 결과=토스트 · 파괴=모달). 기존 20종 font-size·96종 hex는 손대지 않음(AD4 원칙).
  - 사고 1건: 한 줄 함수 끝에 `//` 마커 주석을 붙여 뒤 코드가 주석에 삼켜진 구문 오류 2회(AA9·AA7). 이후 마커는 **항상 앞줄**에 두고, admin.html 편집마다 인라인 스크립트 구문 검사 스크립트를 돌리는 것으로 고정.
