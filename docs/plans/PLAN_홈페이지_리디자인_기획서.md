# 홈페이지 완성도 향상 기획서 (Redesign Spec)

> 목적: momentedit.kr을 2026년 럭셔리 기준으로 끌어올린다. 두 리서치(클로드 코드 외부 리서치 + 클로드 인컨텍스트 코드 감사)의 채택 결론을 하나의 실행 계획으로 통합한다.
> 작성 기준일: 2026-07-01 · 대상 파일: `index.html`(정적·Vercel 자동배포)

---

## 0. North Star (설계 원칙)

1. **절제(restraint)가 프리미엄을 만든다.** 화려한 효과가 아니라 여백·타이포·사진의 완성도.
2. **진사(#6B2A24)는 히어로의 시그니처 한 순간.** 나머지 섹션은 웜뉴트럴로 조용하게. (코디의 "크림슨 축소" 조언과 사용자의 "진사 히어로" 결정을 이렇게 화해시킨다.)
3. **성능·접근성은 럭셔리의 전제조건**이지 옵션이 아니다. 느리거나 튀면 고급이 아니다.
4. **트렌드는 도구지 정체성이 아니다.** 다 넣지 않고 브랜드에 맞는 2~3개만 장인정신으로.
5. **고객 노출 문구에 전각 줄표(—) 금지** (기존 규칙 준수).

---

## 1. 현황 진단 (실측 · 2026-07-01)

| 항목 | 현재 상태 | 판정 |
|---|---|---|
| CSS 구조 | 339번 줄 1개 = **33,586자**(minify 1줄) | 🔴 최우선 병목 |
| 진사 사용 범위 | 히어로 모노그램에 한정 | 🟢 이미 좋음 |
| 스크롤 리빌 | IntersectionObserver 5개(JS 구동) | 🟡 CSS 전환 여지 |
| `prefers-reduced-motion` | 히어로 스크립트에 반영 | 🟢 있음(전역 확대 필요) |
| 반응형 타이포 `clamp()` | 6곳 사용 | 🟡 부분 |
| 폰트 로딩 | 구글폰트 CDN + `size-adjust` 폴백 @font-face 3종 + preload + swap | 🟢 양호 / 🟡 **stylesheet 링크 중복 로드** |
| 이미지 | `<img>` 20개 중 **16개 picsum 플레이스홀더**, `<picture>`·AVIF·WebP 전무 | 🔴 실사+포맷 필요 |
| `aspect-ratio`(CLS 방지) | 4곳만 | 🟡 전 이미지 확대 필요 |
| SEO 기본 | title·meta description 존재 | 🟢 있음 |

**핵심 통찰**: 코디 리포트의 "지금 당장" 제안 다수(진사 축소·reduced-motion·clamp·히어로 fetchpriority)는 **이미 구현돼 있다**. 코디는 사이트 소스를 못 봐서 몰랐음. 실제로 남은 큰 레버는 **①CSS 구조화 ②실사 사진 ③이미지 포맷/CLS ④폰트 마감**이다.

---

## 2. 방향성 — 두 리포트에서 채택 / 보류

### ✅ 채택 (근거 충분·온브리프)
- **웜뉴트럴 팔레트 통일**: 배경 크림 계열(예: #FAF8F1)·텍스트 웜차콜(예: #1C1B1A). 순백 대신 따뜻한 오프화이트 (OBSESD 레퍼런스).
- **풀블리드 사진 + 여백 넉넉한 갤러리 그리드** 아트디렉션 (Aesop·Thyme).
- **조용한 텍스트 링크 CTA** + 비강압적 문의 카피 (OBSESD "non-binding 제안서").
- **AVIF/WebP `<picture>` 캐스케이드**, 전 이미지 `aspect-ratio`로 CLS 제거.
- **CWV 목표치**: LCP ≤ 2.5s · INP ≤ 200ms · CLS ≤ 0.1 (모바일 75퍼센타일).
- **Cormorant Garamond는 디스플레이 전용**, 본문은 Noto Serif/Sans KR.
- **`clamp()`에 반드시 rem 성분**(줌 접근성), line-height 단위 없는 값.

### ⏸ 보류 / 조건부
- **"크림슨을 액센트로 축소"** → 히어로는 진사 유지, **나머지 섹션만** 웜뉴트럴로. (원칙 #2)
- **Pantone 2026 'Cloud Dancer'** 인용 → 팔레트 방향의 참고로만, 마케팅 문구에 직접 인용 전 재확인.
- **페이지 전환·프리로더(OBSESD식)** → 성능 예산 통과 시에만 장기 과제.
- **WebGL/Three.js(Aristide Benoist)** → 오프브리프, 미채택.

### ❌ 미채택
- 과한 패럴랙스·대형 이동(전정기관 이슈·럭셔리 톤과 배치).

---

## 3. 벤치마크 (코디 발굴 · 5선)

| 사이트 | 무엇을 배우나 |
|---|---|
| **OBSESD** (obsesd.dk) | 크림/차콜 팔레트, 3단 모션(프리로더→전환→hover 리빌), 비강압적 문의 카피·단계형 프로세스. **1차 레퍼런스** |
| **Aesop** (aesop.com) | 극단적 여백, 시네마틱 풀블리드 히어로, 조용한 텍스트 링크 CTA, 신뢰 배지 |
| **Thyme** (thyme.co.uk) | 무거운 모션 없이 사진·여백·에디토리얼 톤만으로 럭셔리, 카드 그리드, 매거진형 스토리 |
| **siteinspire /luxury** | 밝은 뉴트럴·풀블리드·타이포 레이아웃의 큐레이션 감각 |
| **Avagyan Wedding** | 웨딩 포토그래퍼의 Clean/Typography 방향 |

---

## 4. 작업 항목 (임팩트 × 노력 · Phase별)

각 항목: 현황 → 목표 → 방법 → 담당.

### Phase 0 — 기반 (반드시 먼저)
- **P0-1. CSS 구조화** 🔴
  - 현황: 1줄 33,586자 → 모든 디자인 반복이 느리고 실수 잦음.
  - 목표: 읽기 쉬운 소스로 분리(섹션 주석·토큰 상단화). 빌드 없이도 유지보수 가능한 형태.
  - 방법: `<style>` 내부를 논리 섹션(토큰/베이스/레이아웃/컴포넌트/모션/미디어쿼리)으로 재배치. 필요 시 배포 시 minify는 선택.
  - 담당: **나**. (기능·픽셀 무변경 리팩터 후 스크린샷 회귀 확인)

### Phase 1 — 지금 당장 (High Impact / Low Effort)
- **P1-1. 폰트 링크 중복 제거** + preload 1개로 정리. 담당: 나.
- **P1-2. 전 이미지 `aspect-ratio` 부여**(CLS 0 목표). 담당: 나.
- **P1-3. `<picture>` + AVIF/WebP 캐스케이드** 뼈대 적용(플레이스홀더도 포맷만 먼저). 담당: 나.
- **P1-4. `prefers-reduced-motion` 전역 확대**(히어로 밖 섹션까지). 담당: 나.
- **P1-5. 섹션 외 잔여 하드코딩 폰트사이즈 → `clamp()`(rem 포함)**. 담당: 나.
- **P1-6. 웜뉴트럴 팔레트 토큰 정리**(배경/텍스트 계열 통일, 진사는 히어로 한정 유지). 담당: 나 → **너 승인**.

### Phase 2 — 중기 (High Impact / Medium Effort)
- **P2-1. 실사 사진 교체** 🔴 (가장 큰 지렛대). 담당: **너(촬영컷)** → 나(슬롯·보정 톤·그리드).
- **P2-2. 갤러리 아트디렉션**: 풀블리드 히어로 + 여백 그리드, 디테일컷 중심. 담당: 나.
- **P2-3. 스크롤 리빌을 CSS `animation-timeline: view()`로 이전**(+`@supports` 폴백, JS 축소). 담당: 나.
- **P2-4. 문의 퍼널 개선**: 단일 컬럼·즉시 확인 메시지·응답시간 명시·`tel:` 링크. 담당: 나 → 너 카피 승인.
- **P2-5. 신뢰 섹션**: 실명·촬영유형 후기, 프로세스 단계 시각화. 담당: 너(후기 자료) → 나.

### Phase 3 — 장기 (조건부)
- **P3-1. 페이지 전환·프리로더**(성능 예산 통과 전제). 담당: 나.
- **P3-2. 가변폰트 opsz축 + `font-optical-sizing: auto`**. 담당: 나.
- **P3-3. CWV 정기 모니터링 체계**(PageSpeed/CrUX). 담당: 나 안내 → 너 실행.

---

## 5. 디자인 시스템 명세 (Phase 0에서 확정)

- **컬러**: `--seal:#6B2A24`(히어로 한정) · 배경 크림 · 텍스트 웜차콜 · 힘줄색(hairline) · 골드 액센트 최소. 크림슨 위 크림 반전 텍스트는 **WCAG AA 4.5:1 검증 필수**.
- **타이포 스케일**: 디스플레이=Cormorant(라이트/이탤릭·대사이즈·자간 0.16~0.18em) / 본문=Noto Serif·Sans KR / 행장 45~75자 / 본문 line-height 1.5~1.8 · 헤딩 1.15~1.3.
- **간격 스케일**: 4/8 기반 토큰화, 섹션 간 여백 넉넉히.
- **모션 토큰**: 이징·지속시간·리빌 range를 변수로. 원칙 = opacity 페이드 + 미세 이동, 짧게.

---

## 6. 완료 기준 (Definition of Done)

- [ ] LCP ≤ 2.5s · INP ≤ 200ms · CLS ≤ 0.1 (모바일)
- [ ] 전 이미지 치수/aspect-ratio 지정, 히어로만 eager+high, 나머지 lazy
- [ ] AVIF·WebP 폴백 제공
- [ ] `prefers-reduced-motion`에서 모든 모션 안전 대체
- [ ] 크림슨 반전 텍스트 대비 AA 통과
- [ ] 고객 문구 전각 줄표 0
- [ ] 픽셀 회귀: 리팩터 전후 스크린샷 diff 확인

---

## 7. 열린 결정사항 (너가 정할 것)

1. **실사 사진 방향**: 얼굴 노출 가능 여부 → 디테일컷(반지·부케·손·테이블) 중심으로 갈지.
2. **팔레트 확정**: 웜뉴트럴 크림/차콜 톤을 P1-6 시안에서 A/B로 볼지.
3. **범위**: Phase 0~1만 먼저 갈지, 2까지 한 번에 갈지.

---

## 8. 출처 (핵심)

- OBSESD Awwwards 프로필(팔레트 hex) · Aesop/Thyme 라이브 · siteinspire /luxury
- Google web.dev Core Web Vitals(LCP/INP/CLS 임계값) · MDN(AVIF·scroll-driven animation)
- Josh W. Comeau(scroll-driven animation 지원현황) · Typewolf(Cormorant 디스플레이 용도)
- W3C WCAG 2.3.3(모션)·1.4.4(줌) · CXL/Stanford(첫인상·신뢰도)

> 상세 근거·수치는 코디 리포트 원본(`momentedit_kr_Luxury_Web_Design_Benchmarking...md`) 참조. 본 기획서는 그 결론 + 실제 코드 감사 결과를 통합한 실행판이다.

---

# Part B. 섹션별 실무 검토 (덜어내기 · 보완 · 수정 · 추가)

> 실제 페이지 구조/카피 감사 기반. 렌즈: 연속성 · 완성도 · 퀄리티 · 브랜딩 · 마케팅.

## B-0. 현재 페이지 흐름 (실측)

`hero → #reality(A Long Day) → #about(A Quiet Record) → #archive(Visual Archive) → #service(The 140 Signature · 투명한 올인원 패키지) → #live(The Extended Venue) → #journey(My Page) → #director(혼주라는 이름 · 신혼여행 첫날) → #faq(Common Questions) → #rsvp → #meAdvPanel(AI 상담)`

서사 아크(문제공감 → 철학 → 아카이브 → 서비스/패키지 → 확장경험 → 마이페이지 → 디렉터 노트 → FAQ → 예약)는 **논리적으로 탄탄**하다. 문제는 흐름이 아니라 **일관성·마감·전환장치**다.

## B-1. 연속성 (Continuity)

- 🔴 **수정 — 타이틀 언어 규칙 통일**: 영문 제목(A Long Day, The 140 Signature, The Extended Venue, My Page, Common Questions)과 한글 제목(투명한 올인원 패키지, 왜 평일이 더 자연스러운가, 혼주라는 이름)이 뒤섞임. → **1패턴 확정**: `작은 영문 kicker + 큰 제목(택1 언어)` 또는 `영문 제목 + 한글 보조 1줄`. 전 섹션 동일 컴포넌트로.
- 🟡 **수정 — 헤딩 계층**: `#archive "Visual Archive"`가 h3, `#director`는 h2 없이 h3 2개. → 각 섹션 대표 제목을 h2로 승격, 하위는 h3. (완성도+SEO)
- 🟡 **보완 — 모션 언어 통일**: `.reveal` 33곳을 히어로에서 만든 이징·리빌 토큰과 동일하게(현재 IntersectionObserver 5개 혼재). Phase2에서 `animation-timeline: view()`로.
- 🟢 **유지 — CTA 반복**: "Start Your Narrative" 3회는 브랜딩 일관성. 단 시각 강도는 차등(주 CTA만 강조).

## B-2. 완성도 (Completeness)

- 🔴 **보완 — 가격 투명성 모순**: 섹션명은 "투명한 올인원 패키지"인데 **실제 금액 표기 없음**. → 최소 **시작가/범위**(예: "OOO만원부터 · VAT 포함") 또는 문구를 "맞춤 견적"으로 정직하게 조정. 프라이빗 고가 서비스는 '범위 공개'가 문의 전환·신뢰에 유리.
- 🔴 **추가 — 디렉터 실명·인물**: `#director`에 감성 카피만 있고 **사람(이름·약력·포트레이트) 부재**(추정). → 실명 + 사진 + 한 줄 경력. (신뢰의 핵심 레버)
- 🟡 **보완 — 이미지 마감**: 16개 placeholder → 실사, 전 이미지 aspect-ratio(Phase1/2).

## B-3. 퀄리티 (Quality)

- 웜뉴트럴 팔레트·타이포 스케일·간격 토큰(Phase0/1). Cormorant 디스플레이 한정.
- 🟡 **추가 — 갤러리 아트디렉션**: `#archive`를 풀블리드+여백 그리드로, 디테일컷 중심. 지금은 제목 계층도 낮아(h3) 존재감 약함 → 대표 비주얼 섹션으로 승격.
- 🟡 **보완 — 스캔성**: 영문 장식 제목이 많아 훑기 어려움. **핵심 3~4 섹션만 강한 제목**, 나머지는 라벨로 낮춰 정보 위계 부여.

## B-4. 브랜딩 (Branding)

- 🟡 **보완 — "140" 모티프 정의**: `The 140 Signature` / `The 140 Sequence`의 140이 무엇인지(분? ) **첫 등장에서 1회 명확히** 정의. 브랜드 자산이니 살리되 의미를 각인.
- 🟢 **원칙 — 진사=히어로 시그니처**, 나머지 조용히(반복).
- 🟡 **보완 — 톤 균형**: 에디토리얼 영문 vs 실제 의사결정자(혼주·부모 세대) 이해도. 영문 제목엔 한글 보조 1줄 병기.
- 🟡 **추가 — 브랜드 키트 일관성**: favicon · OG(공유) 이미지 · 워드마크가 히어로 진사 로고와 일관되는지 점검(공유 시 첫인상).

## B-5. 마케팅 / 전환 (Marketing)

- 🔴 **수정 — CTA 이중언어**: `Start Your Narrative` 아래 한글 보조("상담 신청 · 24시간 내 회신") 병기. 브랜딩(영문) + 전환(한글) 둘 다.
- 🔴 **추가 — 후기/신뢰 섹션**: 실명·촬영유형 포함 후기 블록(별점보다 서술형). 근거: 첫인상 약 50ms, 신뢰 판단의 큰 비중이 시각·디자인.
- 🟡 **추가 — FAQ 스키마**: `#faq`에 `FAQPage` JSON-LD → 검색 리치결과(무비용 SEO).
- 🟡 **수정 — 차별화 메시지 전진배치**: '평일 결혼식 · 단독 하우스웨딩 · 양가 직계가족'이 핵심 강점 → 히어로 직후에 한 번 더 또렷이(현재 hero h2에 있으나 스캔 강화).
- 🟡 **보완 — 문의 퍼널**: 단일 컬럼 · 즉시 확인 메시지 · 응답시간 명시 · `tel:` 링크(Phase2).

## B-6. 덜어내기 (Remove / Reduce)

- 🟡 **중복 감사**: `#reality(A Long Day)` 문제공감 ↔ `#director` 감성 노트가 정서적으로 겹칠 수 있음 → 역할 분리 명확화(문제제기 vs 신뢰형성) 또는 하나로 압축.
- 🟡 **장식 제목 과다**: 영문 제목 남발 → 위 B-3처럼 라벨로 강등해 페이지 무게 감소.
- 🟢 **원칙**: 섹션 10개는 서사상 필요. 삭제보다 **위계 조정**으로 체감 길이를 줄인다.

## B-7. 실무 우선순위 (이번 사이클)

| 순위 | 항목 | 렌즈 | 담당 |
|---|---|---|---|
| 1 | 헤딩 계층 정리(h2/h3) + 타이틀 언어 1패턴 | 연속성·완성도·SEO | 나 |
| 2 | 가격 표기 결정(시작가/범위 or 문구 조정) | 완성도·마케팅 | 나 시안 → **너 결정** |
| 3 | CTA 이중언어 + 주 CTA 시각 강조 | 마케팅 | 나 → 너 카피 승인 |
| 4 | "140" 모티프 정의 문장 1개 | 브랜딩 | 나 → 너 확인 |
| 5 | 디렉터 실명·포트레이트·약력 | 완성도·신뢰 | **너(자료)** → 나 |
| 6 | 후기 섹션 신설 | 마케팅 | **너(후기)** → 나 |
| 7 | FAQ 스키마(JSON-LD) | 마케팅·SEO | 나 |

→ 1·3·4·7은 **내가 자료 없이 바로 착수 가능**. 2는 너 결정, 5·6은 너 자료 필요.

---

# Part C. 디자인 최우선 재검토 (Design-Led)

> 관점 전환: **비주얼 디자인을 기반**으로 다시 봄. 콘텐츠·마케팅 편집보다 **디자인 시스템(타입·간격·그리드·모션)**을 먼저 세우고, 나머지가 그 위에 얹히게 한다.

## C-0. 실측 정정 (중요)

현재 `:root` 토큰을 실제로 뜯어보니, **팔레트는 이미 럭셔리 웜뉴트럴로 잘 잡혀 있다**:
`--bg #FAFAF8 · --bg2 #F5F3EF · --bg3 #EDEBE6`(크림 3단) · `--text #1C1B19`(OBSESD #1C1B1A와 사실상 동일) · `--sub #5A554C · --gold #B89A75 · --seal #6B2A24`.
→ **Part A/B의 "웜뉴트럴로 교체" 우선순위는 하향**. 팔레트는 미세조정만. **진짜 디자인 레버는 타입 스케일 · 수직 리듬 · 그리드/폭 · 모션 토큰**이다.

## C-1. 타이포 스케일 (🔴 최우선 디자인 레버)

- **문제(실측)**: 사용된 clamp가 `9-11 / 11-14 / 13-15`(초소형 3개가 거의 겹침) → 큰 공백 → `24-32 / 28-40 / 34-50`. **① 15px와 24px 사이 중간 사이즈 부재 ② 최대 디스플레이 50px로 럭셔리 히어로치고 작음 ③ 모듈러 비율 불일치.**
- **개선 — 모듈러 스케일 확정**(rem 기반·줌 접근성). 예시:

  | 역할 | 토큰 | 값(예시) |
  |---|---|---|
  | eyebrow/label | `--fs-label` | `clamp(0.7rem, 0.62rem+0.35vw, 0.8rem)` + 대문자·tracking 0.16em |
  | caption | `--fs-cap` | `clamp(0.8rem, 0.74rem+0.3vw, 0.9rem)` |
  | body | `--fs-body` | `clamp(1rem, 0.95rem+0.3vw, 1.125rem)` |
  | lead | `--fs-lead` | `clamp(1.15rem, 1rem+0.8vw, 1.5rem)` |
  | h3 | `--fs-h3` | `clamp(1.35rem, 1.1rem+1.3vw, 2rem)` |
  | h2 | `--fs-h2` | `clamp(1.9rem, 1.2rem+3vw, 3.25rem)` |
  | h1/display | `--fs-display` | `clamp(3rem, 1.5rem+7vw, 6.5rem)` |

  → **15↔24 공백을 lead/h3로 메우고, 디스플레이를 키워** 시그니처 순간에 무게를 준다. line-height는 헤딩 1.1~1.2 / 본문 1.6~1.75 단위 없는 값.

## C-2. 수직 리듬 · 간격 스케일 (🔴)

- **문제**: `--gap:104px`(모바일 80px) **단 하나**로 전 섹션 → 리듬이 평평. 럭셔리 에디토리얼은 **시그니처 섹션에 더 큰 숨**을 준다.
- **개선 — 간격 스케일 + 섹션 리듬 토큰**:

  ```
  --space-1:8px; --space-2:16px; --space-3:24px; --space-4:40px;
  --space-5:64px; --space-6:104px; --space-7:160px;
  --rhythm-section:var(--space-6);     /* 일반 */
  --rhythm-feature:var(--space-7);     /* 히어로·아카이브 등 시그니처 */
  ```
  → 일반 섹션은 기존감, **아카이브/히어로 전후만 넓게** 줘 완급.

## C-3. 그리드 · 폭 시스템 (🟡)

- **문제**: 폭 토큰이 `--max:820px` 하나 → 본문엔 좋지만 **풀블리드 갤러리·와이드 구성 불가**.
- **개선 — 폭 3단**:
  ```
  --w-text:680px;   /* 읽기(본문) */
  --w-content:1080px; /* 일반 콘텐츠 */
  --w-full:100vw;   /* 풀블리드 이미지 */
  ```
  → `#archive`를 풀블리드 갤러리로, 텍스트는 `--w-text`로 좁혀 가독성↑(현재 820은 본문엔 살짝 넓음).

## C-4. 모션 토큰 (🟡)

- **문제**: `--ease`와 `--ease-essence`가 **동일값 중복**. 지속시간 토큰 없음(각 애니에 하드코딩).
- **개선**: 중복 제거 + 지속시간 스케일.
  ```
  --dur-fast:.3s; --dur:.6s; --dur-slow:1.1s;
  --ease:cubic-bezier(0.16,1,0.3,1);   /* 표준 */
  --ease-seal:cubic-bezier(0.65,0,0.35,1); /* 강조(진사) */
  ```
  → `.reveal` 33곳과 히어로 리빌을 **같은 토큰**으로 통일(연속성).

## C-5. 컴포넌트 원자화 (🟡)

- 모든 섹션이 같은 **`section-header` 원자**(eyebrow + 제목 + 리드)를 쓰게 표준화 → 섹션마다 제각각인 제목 스타일 제거(연속성·완성도).
- 카드(서비스·갤러리)도 공통 규격(이미지 비율·패딩·hover)으로.

## C-6. 디자인 관점 덜어내기 / 보완 / 수정 / 추가

- **덜어내기**: 중복 이징(`--ease`≡`--ease-essence`), 초소형 3중 사이즈(9/11/13) → 2개로, 균일 간격 단조로움.
- **보완**: 중간 타입 사이즈(lead/h3), 폭 토큰, 간격 스케일, 모션 지속시간 토큰.
- **수정**: 디스플레이 사이즈 확대, 모듈러 스케일화, 섹션 리듬 완급.
- **추가**: 그리드/폭 시스템, `section-header`·`card` 공통 컴포넌트.

## C-7. 디자인 최우선 실행 순서 (재정렬)

| 순위 | 항목 | 비고 |
|---|---|---|
| **0** | CSS 구조화(Part A P0-1) | 토큰 편집의 전제 |
| **0.5** | **디자인 토큰 시스템 확정**(C-1~C-4: 타입·간격·폭·모션) | 픽셀 변화 있음 → **너 승인 후 적용**. 스크린샷 A/B로 확인 |
| 1 | 컴포넌트 원자화(C-5) | 토큰 위에서 |
| 2 | 이후 Part B(헤딩·CTA·140·후기·FAQ 등)가 새 시스템 위에 얹힘 | 시스템 상속 |

**결론**: 팔레트는 손대지 말고(이미 좋음), **타입 스케일 + 수직 리듬 + 폭/그리드 + 모션 토큰**을 먼저 세우는 게 디자인 완성도의 8할. 이게 서면 Part B 편집들이 자동으로 정돈된다. 토큰은 픽셀이 바뀌니 **시안(A/B 스크린샷)으로 먼저 보여주고 적용**하는 걸 권장.


