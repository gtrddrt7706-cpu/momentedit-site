# 오픈 직전 목록 — 저장소 비공개 전환 때 한꺼번에 [LAUNCH_LIST]

사장님 결정(9/25): momentedit-site 는 **오픈 때까지 공개**로 둔다(코워크가 직접 읽는 통로).
아래는 오픈 직전에 한 번에 하는 일이다. 순서대로 한다. 끝낸 줄은 `[x]`.

## 1. 전환 전에 (코드)
- [ ] **A3 액션 분 줄이기** — 비공개가 되면 무료 한도가 월 2,000분이다(30일 실측 약 1,215분 = 61%)
  - merge-guard 의 `push` 트리거를 뺀다(PR 실행과 겹침 · 30일 229분)
  - gas-deploy 는 CLASPRC_JSON 을 넣기 전까지 `workflow_dispatch` 만 남긴다(지금 푸시마다 실패 · 41분)
  - nightly-screen 을 이틀에 한 번으로(30일 520분) — 한도를 넘을 때만
- [ ] **개인 연락처 정리** — 운영자 개인 휴대폰 · 개인 gmail 이 코드와 문서에 들어 있다
  - 위치 목록: momentedit-docs `operations/오픈직전_개인연락처.md` (공개 저장소에 지도를 두지 않으려고 그쪽에 둔다)
  - 코드(.gs)에 든 것은 스크립트 속성으로 옮기고 GAS 재배포가 함께 필요하다

## 2. 전환 (사장님)
- [ ] **레터 GAS 한 줄 교체** — 부부폼 프로젝트(form-to-couple)에서 `CFG.RAW` 한 줄만
  `'https://www.momentedit.kr/assets/preview/'` 로 바꾸고 배포 관리에서 **«새 버전»**으로 재배포 [RAW_OFF_GITHUB]
  (파일 통째 붙여넣기 금지 — 저장소와 배포본이 다를 수 있다)
- [ ] 깃허브 → 저장소 Settings → General → 맨 아래 Danger Zone → Change repository visibility → **Private**

## 3. 전환 뒤 확인 (A5)
- [ ] 베르셀 main 배포가 도는지(베르셀 깃허브 앱이 비공개 저장소를 읽는지) — 아무 커밋 하나 병합 뒤 배포 완료 확인
- [ ] 레터 메일 이미지가 뜨는지 — 테스트 메일 1통
- [ ] 코드 세션 복제 · 푸시가 되는지 / 코워크가 가지를 읽던 통로가 닫히니 전달 방식을 다시 정한다
- [ ] 첫 주 액션 분이 한도 안인지

## 참고
- 공개였던 동안의 **이력**에는 옮긴 자료(국가지원금 · 설명자료 · 결제 기획 등)의 옛 판이 남아 있다.
  이력 다시 쓰기는 하지 않는다 — 비공개 전환이 그것을 가린다.
- 민감 자료는 9/25 에 momentedit-docs 로 옮겼다([GRANT_MOVED_0925] · 대응표 momentedit-docs `site-moved.md`).
