# 합동 점검 회신 — 게이트를 뚫은 돌연변이 넷 [ADV_AUDIT]

> ★마커를 파일 첫 줄에 박는다(지적 두 번 받고 굳힌 관행).
> 2026-08-15 · main `230c66e` 기준. 숫자는 전부 실행 결과다.
> **기준선**: `data-roundtrip` rc=0 · `merge-guard` rc=0 · `gap-profile` rc=0 (전부 초록에서 시작)

---

## 0. 한눈에

| | 결과 |
|---|---|
| ① 적대적 검증 | **게이트를 뚫은 돌연변이 4건** (전부 rc 0/0 유지) |
| ② 390px 재보행 | **🐛 0 · 🟡 0** — 이번 주 화면 6건 전부 실측 통과 |
| ③ 모의 세계 현실성 | **어긋난 곳 2건** (①의 성공 원인이기도 하다) |
| ④ 소리 사전 점검 | rc=0 · 기존 82클립 기준선 재확인 |

★그리고 **내 자가 또 틀렸다(세 번째)** — ②에서 처음 나온 🐛 1·🟡 1이 둘 다 오탐이었다. §2-2에 적었다.

---

## 1. ① 적대적 검증 — 게이트가 초록인 채 제품이 깨진 사례 넷

방법: `main` 깨끗한 상태에서 제품 파일만 훼손 → 두 게이트 실행 → 원복.
(★훼손은 커밋된 상태에서만 했다 — 미커밋 고침이 `git checkout` 에 지워지는 사고를 두 세션 다 밟았다.)

### A. 다른 트랙의 문자열이 전부 8자로 잘려도 초록

`automation/platform/80_production.gs` · `handleSaveProductionTrack` 진입부

```js
// [MUTANT_A] guideinfo 가 아닌 트랙의 문자열을 8자로 자른다
if (track !== 'guideinfo' && body && body.draft) {
  for (var _mk in body.draft) if (typeof body.draft[_mk] === 'string') body.draft[_mk] = body.draft[_mk].slice(0, 8);
}
```

**결과: roundtrip 0 · guard 0.** 게이트 ①은 `track: 'guideinfo'` **한 트랙만** 저장해 본다.
나머지 여섯(`dining`·`ritual`·`final`·`seat`·`snap`·`confirm`)은 한 번도 안 지난다.
실제로 이 꼴이 나면 청첩장 문구·다이닝 요청사항·최종 확정 메모가 조용히 잘린다.

### B. 후기(review)가 5자로 잘려도 초록

`handleSubmitSurvey` 진입부에 `body.review = String(body.review).slice(0, 5);`

**결과: roundtrip 0 · guard 0.** 게이트 ③은 `answers.overall === 'very'` 만 본다.
`review`(고객이 쓴 후기 본문)는 셀에서도 관리자 상세에서도 **한 번도 대조되지 않는다.**
③의 관리자 확인은 `'very' || '매우' || '후기'` OR 조건이라 셋 중 하나만 살아도 통과한다.

### C. 되읽기에서 청첩장·최종확정을 통째로 지워도 초록

`_prodLoad` 가 `invitation`·`final` 키를 `delete` 하도록 변경

**결과: roundtrip 0 · guard 0.** 고객이 저장한 청첩장·최종 확정이 화면에서 사라지는데
게이트는 `guideinfo` 만 되읽어 확인한다.

### D. 설문 제출이 동의기록을 통째로 비워도 초록

`handleSubmitSurvey` 가 `row.set('동의기록', '{}')` 를 실행

**결과: roundtrip 0 · guard 0.** ★넷 중 가장 나쁘다 —
동의기록에는 `계약금확인`·`결과물전달일` 같은 **되돌릴 수 없는 정산 기록**이 들어 있다.
게이트 ②는 **제작 저장 경로 하나**로만 키 보존을 시험한다. 다른 핸들러가 같은 셀을 건드리는 경우는 안 본다.

### 공통 원인 — 게이트가 「대표 하나」로 짜여 있다

넷 다 같은 모양이다: **검사가 지나간 길만 안전하고, 안 지나간 길은 무방비**다.
그물 자체는 잘 짜였다(진짜 핸들러를 부르고 특수문자·유니코드까지 본다).
문제는 **표본이 하나**라는 것뿐이다.

**메우는 방향(합의 후에)**: 트랙 이름을 하드코딩하지 말고 `_prodCols()` 에서 **동적으로 뽑아 전수**로 돈다.
지금 `guideinfo` 에 쓰는 그 왕복을 트랙 수만큼 반복하면 A·C가 함께 막힌다.
B는 `review` 원문 대조 한 줄, D는 「동의기록을 건드리는 핸들러 전수」로 넓히면 된다.

---

## 2. ② 390px 재보행 — 이번 주 화면 6건

### 2-1. 전부 통과 (실측)

| 자리 | 실측 | 판정 |
|---|---|---|
| 나가기 판 「취소하고 계속 만들기」 | 290x44 · 누르면 판 닫힘 · `idx 8 → 8` 유지 · 적은 글 보존 | ✅ |
| 목차 칩 `.mtl-done` | 34x**40** · 45x**40** · 58x**40** (전 40px) | ✅ (전 23px 였다) |
| 목차 줄 `.mtl-rail` 마스크 | `linear-gradient(90deg, rgba(0,0,0,0) 0px, rgb(0,0,0) 20…)` | ✅ |
| mypage 설문 칩 | **42개 전부 44px 이상**(최소 44) | ✅ |
| mypage 빈 제출 인라인 안내 | `.srv-miss-note` = 「이 문항도 골라 주세요」 | ✅ |
| mypage 가로 넘침 · JS 오류 | 0 · 0 | ✅ |

**🐛 0 · 🟡 0 · ⚡ 0.** 내 1차 지적 셋이 그대로 살아 있고 실제로 작동한다.

### 2-2. ★내 자가 세 번째로 틀렸다 — 오탐 둘

처음 돌린 판정은 「🐛 취소 버튼이 없다」·「🟡 목차 칩 0x0」이었다. **둘 다 내 잘못이다.**

| 오탐 | 진짜 이유 |
|---|---|
| 「취소 버튼 없음」 | 판을 찾는 선택자가 **안쪽 div** 를 집었다. 실제 버튼 목록을 통째로 찍어 보니 `그냥 나가기 · 저장하고 나가기 · 취소하고 계속 만들기` 셋이 다 있었다 |
| 「목차 칩 0x0」 | **완성 화면에서 쟀다.** 거기선 목차 줄이 안 그려져 0x0 이 나온다. 걷는 중(ring 단계)에서 재니 40px 이다 |

★**새 규칙 하나**: 칩·레일처럼 **화면마다 있고 없고가 갈리는 것은 「있는 화면」에서 잰다.**
0x0 은 「작다」가 아니라 「없다」이고, 그 둘을 같은 칸에 넣으면 멀쩡한 고침이 결함으로 보고된다.
(앞선 둘 — `::after` 히트영역 · 스크린샷 잘린 가장자리 — 과 같은 종류다. 세 번 다 자가 문제였다.)

---

## 3. ③ 모의 세계 현실성 — 어긋난 곳 둘

`scripts/audit/_gasworld.mjs` 와 진짜 `writeCell`(`automation/form-to-couple.gs:375`)을 대조했다.

### 3-1. ★모의 세계에는 「헤더가 없는 컬럼」이 존재할 수 없다

```js
// 진짜 writeCell
var c = colOf[header];
if (!c) { Logger.log('  (헤더 없음, 건너뜀: ' + header + ')'); return; }   // ← 조용히 사라진다
```

모의 세계의 `HEADERS` 는 `CUSTOMER_HEADERS + _prodCols() + 확장목록` 으로 조립된다 —
**항상 모든 컬럼이 있는 세계**다. 그래서 이 저장소가 문서로 못 박아 둔 그 사고
(`addProdTrackColumns` 주석: *"컬럼이 없는 상태로 배포되면 writeCell 이 조용히 건너뛰어
저장이 통째로 사라진다 · 화면엔 저장됐어요"*)를 **구조적으로 재현할 수 없다.**

가장 무서운 실패 모드가 모의 세계에서는 일어날 수 없는 일이다.

### 3-2. 빈 값 스킵(`force`) 규칙이 없다

진짜 `writeCell` 은 `value === '' && !force` 면 **안 쓴다**(점진 입력 보존).
모의 세계의 `setValue` 는 무엇이든 그대로 쓴다.
→ 「지우려고 빈 값을 보냈는데 안 지워지는」 부류를 모의 세계에서는 못 잡는다.

★3-1이 곧 §1-C 가 통과한 이유이기도 하다 — 컬럼이 사라지는 세계가 없으니
컬럼이 사라져서 나는 사고도 시험되지 않는다.

**메우는 방향(합의 후)**: 모의 `sheetC.getRange` 를 진짜 `writeCell` 규칙에 맞춘다 —
`COL_OF` 에 없는 헤더면 쓰지 않고 이벤트만 남기고, 빈 값은 `force` 없이는 무시.
그러면 「컬럼 없이 배포」와 「빈 값 삭제」 두 부류가 그 순간부터 시험 대상이 된다.

---

## 4. ④ 소리 사전 점검 — rc 0 · 기준선 그대로

`node scripts/audit/gap-profile.mjs` → **rc 0** (이 컨테이너에 ffprobe 6.1.1 있음)

| 나레이션 82클립 | 실측 |
|---|---|
| 앞 무음 | 최소 0.21 · **중앙 0.23** · 최대 0.48 |
| 뒤 무음 | 최소 0.45 · **중앙 0.50** · 최대 4.55 |
| 안쪽 문장쉼 | 중앙 0.52 · 최대 1.10 |
| 무음 비율 | 20.8% · 총 15.3분 |

**어조 60벌이 도착하면 이 값과 대조한다**(앞 0.23 / 뒤 0.50 · 안쪽 1.10 이하).
★뒤 무음 최대 4.55초 3곳(`toast-toast`·`toast-both-b`·`narr-close`)은 **의도된 것**이다 —
함성과 잔 소리가 지나갈 자리다. 새 클립을 이 최댓값에 맞추면 안 된다.

---

## 5. 요약 (종료 코드)

```
data-roundtrip  rc 0   (기준선)
merge-guard     rc 0   (기준선)
gap-profile     rc 0
돌연변이 4건 전부 → roundtrip 0 · guard 0 로 살아남음
390px 재보행    🐛 0 · 🟡 0 · ⚡ 0   (내 오탐 2건은 자 문제 · §2-2)
모의 세계 어긋남 2건 (§3)
```

★구멍은 **보고만** 했다. 메우지 않았고 검사도 안 고쳤다(지시대로).
제품 파일은 전부 원복했다 — `git status` 깨끗(0줄) 확인.

---

## 6. 코드 회신 — 넷 다 메웠고, 그 돌연변이로 증명했다 (2026-08-15) [ADV_CLOSED]

### 6-1. 게이트 전수화 (data-roundtrip v2)
§1의 방향 그대로: 제작 저장을 **전 트랙 전수**(ritual·dining·seat·guideinfo·snap·final + invitation
별도 핸들러 · 트랙별 실제 필드에 15자+ 특수문자 프로브) [RT_ALLTRACK] · 각 저장 뒤 **되읽기(_prodLoad)**
대조 · **동의기록 키 보존을 저장 8경로 + 설문 전부에서** [RT_CONSENT_ALL] · **review 원문 전문 대조**
[RT_REVIEW]. 인수 시험 = 네 돌연변이 그대로 재현:

| 돌연변이 | v1 | v2 |
|---|---|---|
| A 다른 트랙 8자 절단 | 초록(뚫림) | **rc 1** |
| B review 5자 절단 | 초록(뚫림) | **rc 1** |
| C 되읽기 invitation·final delete | 초록(뚫림) | **rc 1** |
| D 설문이 동의기록 '{}' 덮어씀 | 초록(뚫림) | **rc 1** |

넷 다 원복 후 rc 0. ★v2 를 만들며 나도 함정 하나를 밟았다 — JSON 문자열 안 따옴표·줄바꿈은
\" \n 으로 이스케이프돼, 원문 그대로 찾으면 9건이 가짜 실패로 붉었다(hit() 이 두 꼴 다 본다).

### 6-2. 모의 세계 §3-1 = 진짜 → 반영
_gasworld 에 「헤더 없는 컬럼」 옵션(world 3번째 인자 dropHeaders) [WORLD_DROPCOL].
게이트 ⑤: 제작 컬럼이 전부 없는 세계에서 저장 → 제품 가드(_prodColsMissing)가 **거부**하고
셀에 부분 기록도 없음 확인 [RT_DROPCOL]. 문서로만 존재하던 최악의 사고가 이제 매 푸시마다 시험된다.
기존 소비자 7종(admin-ac-check 등) 회귀 0.

### 6-3. §3-2 판정 = 오탐 (비교 대상이 다른 프로젝트)
빈 값 스킵·force 규칙은 **form-to-couple.gs(부부폼 · 별도 GAS 프로젝트)의 writeCell**이다.
R3n9Mr 의 writeCell(consultation-booking.gs:1526)엔 그 규칙이 없고 — 모의 세계도 없다. 즉 일치한다.
실측: 샌드박스가 어느 판을 쓰는지 확인 → **R3n9Mr 판**(로드 순서상 뒤에 정의돼 이김). 어긋남 아님.
★남는 교훈: 같은 저장소에 두 프로젝트의 동명 함수가 산다 — 대조할 땐 프로젝트 소속부터 확인.

### 6-4. §2·§4 접수
390px 재보행 0건 = 이번 주 화면 6건 종결. 소리 기준선(0.23/0.50 · 뒤 4.55 셋은 의도)은
어조 60벌 도착 시 그 값으로 대조한다.

---

## 7. 2바퀴 [ADV_ROUND2 2026-08-15] — v2 를 다시 깨 봤다

**돌연변이는 0건이다.** v2 는 밟는 길 안에서는 뚫리지 않았다. 대신 **밟지 않는 길에서
진짜 버그 1건**을 실물로 잡았다 — 심어 넣은 것이 아니라 지금 main 에 살아 있는 것이다.

### 7-1. ★컷 제출 상한 두 개가 짝이 안 맞는다 (실버그 · 무증상 유실)
`automation/platform/80_production.gs:1052 / 1063` · `handleSubmitResultSelection`

```js
for (…; _gi < picksRaw.length && _gOut.length < 400; _gi++)   // ① 400개까지 받는다
…
if (picks.length > 8000) picks = picks.slice(0, 8000);        // ② 8000자에서 자른다
//                       ↑ 주석: "파일명(ID) 형식은 길어서 상한 완화(400개 캡과 세트)"
```

「400개 캡과 세트」라고 적혀 있지만 **세트가 아니다.** 한 항목은
`DSC_0163.jpg(1AbCdEfGhIjKlMnOpQrStUvWxYz000163), ` = 약 49자다. 8000 ÷ 49 ≈ **164개**.

실측(모의 세계 · 파일명 12자 + 드라이브 ID 33자):

| 보낸 컷 | 저장된 선택수 | 저장 길이 | 결과 |
|---|---|---|---|
| 100 | 100 | 4,898자 | 무손실 |
| 150 | 150 | 7,348자 | 무손실 |
| **200** | **164** | 8,000자 | **36장 유실** |
| 300 | 164 | 8,000자 | 136장 유실 |
| 400 | 164 | 8,000자 | 236장 유실 |

**왜 나쁜가 — 아무도 모른다.**
1. `ok: true` 를 돌려준다. 고객 화면엔 "제출됐어요".
2. `선택수` 는 **잘린 뒤** 세므로 164 라고 정직하게(?) 적힌다 — 200을 골랐다는 흔적이 없다.
3. 관리자에게 가는 카톡·메일도 164 다. 통장 대조하듯 맞춰 볼 원본 숫자가 어디에도 없다.
4. 8000자는 토큰 한가운데를 자른다 — 꼬리가 `DSC_0163.jpg(` 로 끝난다(**ID 가 잘려 나감**).
   그 조각이 다시 토큰으로 세어져 164 안에 **파일을 못 찾는 항목 1개**가 섞인다.

**도달하나** — 400 캡을 둔 것 자체가 "수백 장을 고를 수 있다"는 전제다. 원본 수백 장에서
마음에 드는 것을 고르는 손님이 164를 넘기는 것은 이상한 일이 아니다.

**게이트는 왜 못 잡나** — `data-roundtrip.mjs` 가 `handleSubmitResultSelection` 를
한 번도 호출하지 않는다. 1바퀴의 진단("표본이 하나")과 같은 병이 **핸들러 단위로** 남아 있다.

**★고치지 않았다** — 합의 전 메움 금지 규칙대로 보고만 한다. 고칠 때의 선택지는 셋이고
어느 쪽이든 "조용히"만 아니면 된다: ⓐ 400 캡을 164로 내리고 넘으면 거부
ⓑ 8000자를 400개가 들어갈 만큼(약 20,000자) 올림 ⓒ 자를 거면 `ok:false` 로 거부.
어느 쪽을 골라도 `선택수` 는 **자르기 전 개수**와 대조해 다르면 관리자에게 알리는 것이 같이 가야 한다.

### 7-2. 동의기록 — 안 밟던 핸들러 실호출
`submitResultSelection` · `requestExtraRetouch` 실행 확인 → **둘 다 보존**.
`signContract`·`paymentSignal`·`saveRefundAccount`·`adminSetResultLinks` 는 시드 전제 부족으로
가드에 막혀(ok:false) **실행되지 않았다** — 「깨끗함」이 아니라 「아직 안 재 봄」이다. 3바퀴 몫.

### 7-3. ★붙여넣기 파일 둘의 순서가 다르다 (버그 아님 · 사람이 틀리는 자리)
`재더빙_붙여넣기.txt` 는 **클립번호 오름차순**(38·63·80), `재더빙_리드보강.txt` 는
**발견순**(63·80·38). 의도된 것이고 근거도 코드에 있다(check-text-audio 의 [PASTE_ONLY]).
다만 **같은 순서라고 가정하면 클립이 통째로 뒤바뀐다** — tribute 소리가 narr-final-warn 이름으로
저장된다. 실제로 [DUB_ONEFILE] 1차 구현이 그 가정을 했고, 대조 검사가 그 자리에서 잡았다.
지금은 순서의 정본을 붙여넣기 쪽으로 못 박고 [PASTE_IS_ORDER] 가드에 올렸다.

### 7-4. 되보고
`data-roundtrip` rc **0** · `merge-guard` rc **0** · 새 돌연변이 **0건** · **실버그 1건**(7-1).
시도한 길: submitResultSelection · requestExtraRetouch · signContract · paymentSignal ·
saveRefundAccount · adminSetResultLinks · 동의기록 6경로 · 컷 제출 상한 5구간(100~400).
