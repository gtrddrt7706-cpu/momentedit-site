# 데이터 흐름 전수 지도 · 현황 (2026-08-15 실측)

코드에서 기계로 뽑은 목록이다(추측 아님). doPost 68액션 · UrlFetchApp 발신 2곳 ·
Vercel 함수 16개 · 웹훅 2계열 · 구글 내부 서비스(메일 9파일·캘린더·드라이브 3파일).
갱신할 때도 같은 방식으로 다시 뽑을 것 — 손으로 늘리면 이 문서가 거짓말을 시작한다.

## 1. 고객 브라우저 ↔ GAS `/exec` (본선 · doPost 68액션)

| 방향 | 무엇 | 보호 장치 | 검증(2026-08-15) |
|---|---|---|---|
| 읽기 | getMyState·결과물 갤러리·하객 안내(guideView)·계약서 | 토큰(resolveSession) · 갤러리 10분 캐시 | render-check·mypage-shot 0 |
| 쓰기(저장 6종) | 청첩장·제작 7트랙·서명·입금신호·컷 제출·후기 | **전역 잠금 15초** · 행 탐색 잠금 안 · 알림은 잠금 밖(_notifyQ) · 트랙별 화이트리스트 정규화 · 이중클릭 1건(실측) | **data-roundtrip 게이트**(왕복 무손실 · merge-guard 상설) |
| 쓰기(환불 계좌) | saveRefundAccount | 잠금(SCALE_LOCK) · 확인→쓰기 잠금 안 · 알림 밖 | gas-lint·gasworld 0 |
| 쓰기(취소 2종) | cancelReservation·emailCancel | **무잠금 = 판단** — 본문이 캘린더·메일(느린 I/O)이라 잠그면 개악. 멱등 가드(상태=취소면 통과) | 근거 주석 코드에 · 가드 |
| 잠금 초과 시 | 전 경로 28곳 | **lockBusySignal** — 누적 집계 + 관리자 하루 1통 메일(규모 조기경보) | 모의 GAS 실호출 0 |

## 2. 관리자 admin.html ↔ GAS (adminCall)

_requireAdmin · _adminLock(15초·실패 신호) · 멱등 · 처리이력 · 되돌리기용 dry-run 프리뷰
(입금취소·강제단계). 고객 저장과 **같은 전역 잠금 계열**이라 서로 밟지 않는다.
검증: admin-ac-check·admin-forcestage-noop·admin-review-stage·rollback-deliverydate·admin-shot 0.
헤더(컬럼) 드리프트는 header-order + GUARD_MIRROR 가 감시.

## 3. GAS → 밖 (발신 · UrlFetchApp 실측 2도메인 + 구글 내부)

| 상대 | 무엇 | 안전 장치 |
|---|---|---|
| 솔라피(api.solapi.com) | 고객 알림톡·SMS | 야간 보류·잔액 경보(하루 1통)·발송을 잠금 밖에서 |
| 토스(api.tosspayments.com) | 카드결제 confirm | 서버 금액 대조(클라 위변조 차단)·이중청구 방지·잠금 |
| 메일(MailApp·9파일) | 관리자 알림 전부 + 고객 메일 | 관리자 = 메일 전용(_noEmoji 자동 세척) |
| 캘린더(CalendarApp) | 상담·가예약 동기화 | 취소 시 삭제 · 백필 트리거 |
| 드라이브(DriveApp·3파일) | 계약서 서명본·결과물 갤러리·주간 백업 | weeklyBackup 8주 보관 |
| 카카오맵(dapi.kakao) | 장소 검증·수집(월간 트리거) | 실패 시 관리자 알림 |

## 4. 프런트 → Vercel 함수(/api/ 16개) → AI·GAS

- **AI 5종**(advisor·ritual-advisor·schedule-advisor·after-concierge·faq-draft): Anthropic 호출.
  지식 = `_kb.js`(가격 240/330 — **check-price-sync 게이트**) + aiFacts(시트 라이브 주입) ·
  레이트리밋(_ratelimit) · 비용은 costlog → GAS 적재.
- **적재 4종**(handoff·lead·lead-click·costlog → GAS): 공유키(AI_HANDOFF_SECRET) ·
  이제 전부 **_lockedAppend**(동시 기록 행 충돌 차단 · 3초 못 잡으면 그냥 쓴다).
- og-inv: 청첩장 OG 이미지 생성(읽기 전용).

## 5. 밖 → 우리 (웹훅 수신)

- **솔라피 전달결과 리포트** → doPost 가 형태 감지 → 실패 시 그 고객에게 메일(카톡 미수신 커버).
  ★콘솔에 웹훅 URL(/exec) 등록이 전제 — 사람 몫 1회.
- **하객 편지 웹훅**(guest-letter-webhook.gs) · **부부 폼**(form-to-couple.gs · 별도 GAS 프로젝트 —
  나레이션 녹음 업로드 · vimeoGuard D-3 감시). 잠금 실패 시 로그 남기고 비잠금 진행(로그 성격).

## 6. 파일 (드라이브가 나른다 · 우리 서버 무경유)

원본·보정본·영상 = 링크 전달(카톡+메일) · 썸네일은 고객 브라우저가 구글에서 직접 ·
전달일 기산 6개월 보관 → 만료 7일 전 메일 → 자동 정리. 링크 공유 방식의 트레이드오프
(주소를 알면 열람 = 가족 공유 쉬움 ↔ 유출 시 회수 어려움)는 업계 관행으로 수용(2026-08-15 판단).

## 7. 브라우저 로컬(localStorage)

위저드·식순 임시본은 기기에 늘 남는다(서버 저장과 이중) — "저장 안 해도 이 기기에는 남아요"의 근거.
`?S=` 미리듣기 주소에는 고객이 쓴 글(서약문·편지·자유칸)을 싣지 않는다.

## 8. 수명 관리(자동 트리거)

주간 백업(8주) · 미계약 6개월 익명화(법정보관 제외·행 삭제 없음) · 로그 90/35일 ·
알림톡 추적 7일 · AI 인계 30일 만료 · 결과물 6개월 만료 통지.

## 남아 있는 알려진 트레이드오프 (문제 아님 · 결정된 것)

1. 같은 고객 두 기기 동시 편집 = 마지막 저장 승리(트랙별 칸이라 다른 위저드끼리는 무충돌).
2. 취소 2경로 무잠금(위 §1) — 잠그려면 actCancel I/O 큐 분리가 먼저(체크리스트).
3. 전역 잠금의 규모 상한 — lockBusySignal 메일이 잦아지면 DB 이관 검토 시작(그 전엔 현행이 최선).
