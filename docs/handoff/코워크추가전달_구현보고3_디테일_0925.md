[코워크 → 코드 · 9/25 · 추가 전달 — 구현 보고 3 디테일 점검(ed5fc1c)]
★앞 회신(«코드에_회신_구현보고3_0925.md»)에 더하는 것입니다. 앞 회신 2장의 세 가지는 그대로 두고, 병합 전에 셋을 더합니다(1장).
★코워크가 한 것:
- ed5fc1c(INAPP_UNLOCK · 재배포 확인 포함)를 받아 390 · 360 · 320 · 768 · 1280 · 가로 844×390에서 ①→④와 크게 보기를 직접 봤습니다.
- 마이페이지 준비 칸은 가짜 값으로 _ritPrepFold만 불러 그려 봤습니다(서버 연결 없음). parents.html 시각 카드도 봤습니다.
- 독립 점검 둘(처음 온 커플의 휴대폰 걸음 · 키보드/화면 읽기/넓은 화면)을 따로 돌렸습니다. 여기 적은 것은 코워크가 다시 재현했습니다. 재현하지 않고 그 점검의 숫자를 옮긴 곳은 «(독립 점검 실측)»으로 표시했습니다.
★INAPP_UNLOCK · INAPP_SIM — 좋습니다. 앞 회신 2-1이 들어가면 한동안 글 줄뿐이라 당장은 덜 쓰이지만, 재녹음 뒤에는 꼭 필요합니다. GAS 재배포 끝난 것(누락 0 · 173항목)도 확인했습니다.

━━ 1. 병합 전에 더할 셋 — 1-1 · 1-2는 지금 main(운영)에도 있습니다 ━━
■ 1-1. ④ «파일로 저장»이 아무 일도 안 한다 — #815부터 main에 있음
- saveScriptTxt 끝 줄에 달린 주석이 클릭 세 문장을 삼켰습니다:
  `a.download=…;   // [OPEN_COURSE] document.body.appendChild(a); a.click(); document.body.removeChild(a);`
- 옛 코스도 같습니다(함수 하나).
- 고침: 주석을 줄 끝으로 옮기고 세 문장을 살립니다.
- 검사: 다운로드 이벤트 1 · 파일 이름 · 첫 줄.
- «한 줄 안에서 // 주석 뒤에 코드가 이어지는 꼴»이 더 있는지 한 번 훑어 주세요. 코워크도 시안에서 같은 실수를 두 번 했습니다.

■ 1-2. «하객 박수로 답하기» 선언 — ④가 안 열리고, 새로고침하면 «엄숙하게»로 바뀐다 — main에도 있음
- ④:
  - rowVal 'declare'가 DECLARE[S.declare].d를 읽는데, 'clap'은 DECLARE에 없습니다.
  - 그래서 TypeError(reading 'd')가 나고, 머리는 «완성»인데 본문은 ③ 그대로 남습니다.
  - ‹기록› 예시가 이 값을 기본으로 씁니다. 예시 넷 중 하나로 시작한 고객이 ④를 못 봅니다.
- 새로고침:
  - _applySaved의 `if(!DECLARE[S.declare])S.declare='1'`이 'clap'을 '1'로 바꿉니다.
  - 실측: ‹기록› → ② → 새로고침 → S.declare 'clap' → '1'. 고객이 고른 것이 말없이 바뀌고, 그 값으로 다시 저장됩니다.
  - ② 칩 46개(와인 셋 포함)를 하나씩 누르고 새로고침해 봤습니다. 바뀌는 것은 이것 하나입니다.
- 고침: rowVal과 _applySaved가 'clap'을 알게 합니다. 새 코스 값의 판정은 RitualOpen 한 곳에 둡니다.
- 검사:
  - 예시 넷 모두에서 ④가 열린다.
  - 칩 전부 «누르고 새로고침해도 그대로».

■ 1-3. 마이페이지 안(?embed=1)에서 크게 보기 중 Esc — 빌더까지 닫힌다 — 이번 PR에서 생김
- 2977행(크게 보기 Esc → lsCloseBig)과 4575행(임베드 Esc → _obExit)이 같은 키를 둘 다 받습니다.
- 임베드 쪽의 예외는 미리듣기 창(ob_rpViewer) 하나뿐입니다. 그래서 크게 보기 · «처음부터 다시 만들기» 판 · 상담 창에서 Esc를 누르면 _obExit가 불립니다.
  - 저장된 상태면 식순 창이 통째로 닫힙니다.
  - 저장 전이면 나가기 확인 판이 뜹니다(독립 점검 실측 · 코워크 코드 확인).
- 고침: 임베드 Esc 첫 줄에서 LP.big이거나 열린 판 · 상담 창이 있으면 return.

━━ 2. 다음 가지 — 고객이 겪는 것 ━━
■ 2-1. ② «담지 않은 순간» — «들어 보고 담을 수 있어요»가 되지 않는다
- 담지 않은 순간의 ▶(lsp:letter 등)는 눌러도 아무 일이 없습니다(재생 목록 0 · 작은 플레이어 없음).
- 작은 그림(lst:k)은 크게 보기를 곧장 «이 순간을 다 보셨어요» 끝 화면으로 엽니다.
- 고침: 담지 않은 순간도 기본 칩 값으로 들려줍니다(담지 않은 채로). 그게 어렵다면 ▶ · 작은 그림과 «들어 보고» 문구를 뺍니다.

■ 2-2. 안내 2/2 «두 분이 하실 일»이 새 코스와 다른 말을 한다
- 지금 화면:
  - «고르고, 들어보고, 익히면 끝이에요»에 번호가 둘뿐입니다(둘째가 «완성본을 미리듣기로 이어들으며…»).
  - «준비는 글만 — … 무엇을 언제까지인지는 완성 화면에 정리돼요»
  - «D-14까지 수정»
- 새 코스에서 바꿀 글(옛 코스는 그대로):
  - 번호 넷: ① 고르기 · ② 보고 듣기 · ③ 준비하기 · ④ 완성. 하나마다 한 줄씩.
  - «준비는 ③ 한 곳에 — 쓸 글 · 보낼 것 · 챙길 것 · 부모님께 부탁드릴 것»
  - «순서는 예식 2주 전까지, 글은 예식 7일 전까지 고칠 수 있어요»
- 안내 1/2의 «인사 사진» → «사진과 인사»(띠 · 알림과 같은 이름).

■ 2-3. «이전»이 방문 기록을 쌓는다
- 실측: ①→②→③에서 «이전» 두 번으로 ①에 옵니다(history.length 6 → 8).
  - 여기서 휴대폰 뒤로를 누르면 ② → ③으로, 앞으로 갑니다.
  - 나가려는 고객이 앞 걸음을 계속 만납니다.
- 고침: 바로 앞 걸음으로 가는 «이전»은 history.back() 길로 갑니다(«← 고르기로»가 쓰는 _opGo와 같게).

■ 2-4. «파일 올리기» — 올라가지 않는데 올렸다고 믿게 된다(옛 코스도 같음)
- 파일을 고르면 이름만 저장합니다(S.up[k]=f.name).
- 2초 알림 «파일 이름을 담아 뒀어요 · 파일은 D-7까지 카톡·메일로 저희에게 보내 주세요»가 사라지면 «✓ 파일이름 · 바꾸기»만 남습니다. 안내 글은 «파일 올리기로 올려 주세요»입니다.
- 결과: 올렸다고 믿고 따로 보내지 않으면, 당일 두 분 목소리가 빠질 수 있습니다.
- 고침:
  - 실제로 올리는 길이 있으면 그 길로 올립니다.
  - 없으면 알림에 이미 적힌 길(카톡 · 메일)을 칸 안에 늘 보이게 합니다. 사이트에 있는 카톡 채널과 메일 주소로 바로 가는 단추도 둡니다.
  - 단추와 상태 글은 사실대로 씁니다(예: «보낼 파일 이름 적어 두기» / «보냈어요» 표시).
  - 어느 쪽으로 했는지 되보고에 적어 주세요.

■ 2-5. 서약 · 편지 — 분량 · 읽는 법 · 경고색이 서로 어긋난다
- 서약 칸은 하나입니다. 그 칸에 세 가지 글이 붙어 있습니다:
  - «서로에게 쓴 약속 · 당일까지 비밀로 둬도 돼요»
  - «한 사람 기준 약 300자»
  - «서로 한 줄씩 번갈아 읽고, 마지막 한 문장은 함께»
- 번갈아 읽는 글은 비밀일 수 없습니다. 둘이 300자씩 쓰면(583자) 글자 수가 경고색(--seal)이 됩니다. ① 카드는 «(한 분 1분쯤)»입니다.
- 편지:
  - ① · ②는 «편지 400자씩»입니다.
  - ③은 «마음 하나만 골라 두 줄이면 충분해요»와 «한 사람 기준 1분 안팎»을 함께 씁니다.
  - 400자를 적으면 경고색이 됩니다.
- 원인: wcCnt 경고가 모든 칸에 360자 하나입니다.
- 고침:
  - 서약에서 «비밀로 둬도 돼요»를 뺍니다. 읽는 법은 «번갈아 · 끝 문장은 함께»로 이미 정해져 있습니다.
  - 분량은 한 곳의 숫자를 ①②③이 같이 씁니다.
    - 서약: «두 분이 함께 쓰는 글 · 한 분 300자쯤(모두 600자쯤)»
    - 편지: «한 분 400자쯤»
    - «두 줄이면 충분해요»는 뺍니다.
  - 경고색은 칸마다 권장량의 약 1.2배에서 켭니다(서약 약 720 · 편지 한 분 약 480 · 편지 칸 하나에 두 분이 쓰면 그 두 배).
  - 숫자는 엔진의 예상 초(partsOf)와 맞춰 코드가 정합니다.

■ 2-6. ② 이름표 · 칩 이름 — 앞 회신 3-3 표에 더할 것

| 지금 | 바꿈 |
|---|---|
| 하객이 박수로 답하는(선언 · 박수 첫 줄 — 문장이 끊김) | 박수를 청하는 말 |
| 영상 · 3분(준비한 순서 첫 줄) | 여는 말 |
| 부모님께 / 서로에게(편지 첫 줄) | 여는 말 |
| 입장 직후(하객 맞이 첫 줄 · 크게 보기 · ③ 녹음 참고) | 하객 입장 때 |
| 칩 «성우 · 엄숙하게» · «성우 · 따뜻하게» | «나레이션 · 엄숙하게» · «나레이션 · 따뜻하게» — 흐름 · 요약 · ④와 같게. 목소리가 AI 나레이션이라 «성우»는 사실과도 다릅니다 |
| 칩 묶음 «도착 멘트» | «맺는 말» — 흐름에서 그 줄의 이름 |
| «… · 미리 보기에서는 넘겨요» | «… · 여기서는 넘겨요» |
| ① 카드 자세히 «부모님께 여쭐 것» | «부모님께 부탁드릴 것» |

- 입장 «짧고 강하게» + 맺는 말 «입장과 같이»:
  - «짧고 강하게»에는 제 맺는 말이 없어서 기본 맺는 말이 나옵니다.
  - 그 조합에서는 칩 이름을 «기본»으로 보이게 합니다. 한 줄을 새로 만들지는 사장님 대본 점검 때 정합니다(대본은 사장님 몫).
- «케이크와 축배 + 붓지 않음»이면 ② 줄 요약이 비고, ①의 «지금 ·» 줄도 사라집니다 → 요약은 «케이크와 축배».

■ 2-7. ① 카드 «이 자리인 까닭» — 이웃 순간을 안 담아도 그 이름을 말한다
- 고정 글이라 이웃을 안 담아도 그대로 나옵니다:
  - 준비한 순서: «편지 앞이에요. 편지가 마지막 큰 순간이 되도록.»(편지를 안 담아도)
  - 덕담: «서약 바로 앞이에요»
  - 부모님께 인사: «선언 다음이에요»
- 고침: 이웃을 담았을 때만 그 이름을 말하고, 없으면 까닭만 말합니다.
- ① 빈 상자: «입장과 닫는 인사는 늘 있어요» → «식전 영상 · 입장 · 닫는 인사는 늘 있어요»(PREVIDEO_ALWAYS).

■ 2-8. ③ 보낼 것
- 준비한 순서가 영상이면, «식전 영상 링크» 아래에 «영상 파일(가로 · 3분 안)»이 이름 없이 붙습니다 → «준비한 순서 영상(가로 · 3분 안)».
- «사진 30~40장을 보내 주세요»에 보낼 곳이 없습니다 → 2-4와 같은 길로 보냅니다.
- 어려운 말 풀기: «가로 mp4 3분 이내» → «휴대폰으로 가로로 찍은 영상 · 3분 안» · «mp3 · m4a» → «휴대폰 녹음 파일(음성 메모)».
- 축사 «원고(분당 300자 안팎)(큰 글씨로 돌려드려요)»에서 괄호가 겹칩니다.
  - 앞 회신 3-4(a)대로 마감을 데이터로 하면 치환이 없어집니다.
  - 글은 «원고는 분당 300자 안팎 · 받으면 큰 글씨로 돌려드려요»로.

■ 2-9. ② 알림도 ①처럼 접히게
- ①은 스크롤하면 알림이 한 줄로 접힙니다. ②는 늘 펼쳐진 채 위에 붙어 있습니다.
- 360×740에서 위 173px + 아래 84px가 붙어서, 목록이 쓰는 자리는 65%입니다(독립 점검 실측).

■ 2-10. 크게 보기
- 가로 휴대폰(844×390):
  - 장면 상자가 화면을 다 씁니다. 순간 이름 · 자막은 화면 아래(367 · 437px~ · 독립 점검 실측)에 있어 조작 띠에 가립니다.
  - 고침: 높이 500 아래에서는 장면을 45vh 안으로 줄이고, 글을 옆이나 바로 아래에 둡니다.
- 끝 화면:
  - 지금은 [다시 보기] [목록으로]와 큰 ↻가 겹칩니다. ↻는 다시 보기와 같고, 목록으로는 위의 ⌄와 같습니다.
  - 고침: [다시 보기] [다음 · 준비하기] 둘만 둡니다.
- 칩이 많은 입장 화면에서 아래로 밀면 ⌄와 «n / N»이 함께 사라집니다 → 머리 줄을 고정합니다.

■ 2-11. 작게 다듬기
- «이 순간 빼기»가 칩과 같은 모양입니다 → 글 단추(밑줄)로 바꿔 칩 묶음과 떨어지게.
- ① 띠 흐름 칩이 오른쪽에서 표시 없이 잘립니다(«닫…» · 빈 상태 «…담아 보세요»).
  - 끝에 흐림을 둡니다.
  - 빈 상태 안내는 띠 밖 한 줄로 뺍니다.
- 걸음 표시: 지난 걸음(단추)이 지금 걸음보다 몇 px 낮게 앉습니다 → 글줄을 맞춥니다.
- 숫자 기준: ①은 «담지 않은 순간 2가지»(준비한 순서 포함), ②는 «1»입니다 → 기준을 같게. ②에는 «준비한 순서는 ①에서»가 이미 있습니다.
- 목록 구분점 «·»와 이름 안의 «·»(케이크 · 축배)가 같아서 개수가 많아 보입니다 → 목록 구분은 «›»나 줄바꿈으로.
- 한글 줄바꿈: body에 word-break: keep-all(«완성됐어 / 요» · «사진과 인 / 사» 같은 끊김).

━━ 3. 다음 가지 — 키보드 · 화면 읽기 · 글자 크기 · 넓은 화면 ━━
- 크게 보기 포커스 가둠이 샙니다.
  - Tab 순서: … lfnext → BODY → meAdvFab(밖) → lfmin.
  - 까닭: #meAdvStack이 .wrap 밖이라 inert가 안 걸립니다.
  - 고침: 크게 보기 동안 #meAdvStack에도 inert를 겁니다.
- 누른 단추가 다시 그리기로 사라지면 포커스가 BODY로 떨어집니다.
  - 어디서: ② 빼기(lsr:k) · 되돌리기(lsundo) · 담기(lsa:k) · ① «예시 전으로 되돌리기» · 알림 단추.
  - 고침: 빼기 뒤에는 «되돌리기»로, 되돌리기 · 담기 뒤에는 그 줄의 lsm:k로, 판을 닫으면 연 단추로 보냅니다.
- ② 처음 들어올 때 포커스가 제목에 남지 않고 BODY로 갑니다. 엔진 · 녹음표를 불러온 뒤 다시 그리기 때문입니다.
  - 고침: 다시 그리기 전에 포커스가 #stepHead였으면 새 제목으로 되돌립니다.
- ① «고르기 어려우시면 … ↓»(opJump)와 예시(opEx)는 화면만 옮기고 포커스는 제자리입니다.
  - 고침: #opEx · #opMine에 focus()(둘 다 이미 tabindex=-1).
- ① 단추 이름이 «+ 담기»×10 · «자세히 ▾»×13뿐입니다.
  - 고침: aria-label «화촉 담기» · «화촉 자세히». 카드 이름을 제목(h4)으로.
- ② 칩 radiogroup이 방향키에 반응하지 않습니다(lsc:entry:E에서 →를 눌러도 그대로).
  - 고침: 고른 칩만 tabindex 0으로 두고 방향키로 옮기며 고르게. 또는 radio를 빼고 aria-pressed로.
- ② 줄 ▶는 재생 중에도 «… 듣기»이고, 다시 누르면 처음부터 다시 틉니다.
  - 고침: 재생 중에는 «멈춤» 토글로.
- 상태 알림(.ls-msg · .op-note)이 누를 때마다 새 노드로 바뀝니다.
  - 고침: #lsLive처럼 고정 칸에 글만 바꿔 넣습니다.
- ③ 링크 칸 오류가 화면 읽기에 전해지지 않습니다.
  - 고침: aria-invalid · aria-describedby · role=status.
- 이름 정리:
  - ② 큰 단추(lsall) 이름에 «장면 영상 준비 중»이 섞입니다 → aria-label «처음부터 보고 듣기 · 약 5분».
  - ① 선 그림 SVG는 aria-hidden.
  - 작은 플레이어 첫 단추 이름 → «크게 보기 · (순간 이름)».
- 작은 플레이어 포커스 윤곽: #7A5F37 on #3A2D22 = 2.23:1 → 밝은 윤곽으로.
- 44px 미만 둘:
  - .op-undo(예시를 바꾼 뒤) 136×30
  - «음악까지 미리듣기» 창 닫기 ✕ 34×34(독립 점검 실측)
- 글자 크기:
  - 11px 이하 글(.ls-no · .prog-pct 11 · .wr-cat 11 · .wc-stat 10 · .dday 10 · .sr-empty 9.5)을 12px 이상으로 올립니다.
  - 크기가 px로 고정이라 브라우저 글꼴 키우기가 먹지 않습니다(루트 125%에서 보이는 글 변화 0). rem으로 바꿀지는 코드가 판단합니다(범위가 크면 뒤로).
- 넓은 화면 · 가로(선택):
  - 1280에서도 크게 보기가 484px 그대로라, 영상이 들어오면 PC에서 작게 보입니다 → min(90vw, 960px)쯤.
  - 가로 휴대폰 · 확대에서는 붙는 띠와 작은 플레이어가 화면 절반을 덮습니다(844×390에서 58% · 독립 점검 실측). 높이 500 아래에서는 띠를 붙이지 않는 쪽으로.

━━ 4. 앞 회신 바로잡기와 더하기 ━━
- P10 글: 마이페이지 줄 이름이 «좌석 · 음료»라서 그 이름을 씁니다.
  - «자리마다 축배 음료 알려 주기 · 마이페이지 «좌석 · 음료»에서»
  - 마이페이지 준비 칸에도 같은 글을 씁니다.
- 마이페이지 준비 칸(가짜 값으로 그려 봄):
  - ③과 같은 마감 문제가 그대로 보입니다: «반지 두 개 · 10월 9일까지(예식 7일 전)» · «양가에서 와인 한 병씩 · 10월 9일까지».
  - 앞 회신 3-4(a)의 마감 데이터로 함께 풀립니다.
  - 마감을 날짜로 바꿔 보여 주는 것은 좋습니다.
- parents.html «예식 날 부모님 시간»:
  - 표와 폭은 문제없습니다(320에서도).
  - 첫 줄 «편지와 따로 적어 둡니다.»는 부모님께 뜻이 없는 말입니다 → «예식 날 움직이실 시각이에요. 모든 시각은 본식 시작에서 셉니다.»

━━ 5. 되보고 ━━
- 1장 셋, 고치기 전 → 뒤:
  - 파일로 저장 다운로드: 0 → 1
  - 예시 넷 중 ④가 열리는 것: 3 → 4
  - 칩을 누르고 새로고침해도 그대로인 것: 45 → 46
  - 임베드에서 크게 보기 Esc 뒤: 빌더가 닫힘 → 남음
- 2 · 3장: 항목별 ✓ 또는 «안 함 · 까닭»
