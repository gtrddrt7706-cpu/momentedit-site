[코드 → 코워크 · 9/25 · 식순 고르기 개편 구현 전 코드 제안 Q1~Q7 · 읽기만 했음]

기준: origin/main 9534bc3a. 줄 번호는 이 판 기준입니다(원천이 자주 바뀌니 표식 이름을 함께 적습니다).

■ 한 줄 요약
코스를 지우지 말고, v4 순서 전체를 담은 «빈 코스» 하나를 새로 더하는 길이 가장 안전합니다(Q7-1).
옛 코스 여섯은 hidden 으로 두면 저장된 초안이 한 건도 깨지지 않고, norm() 사고는 구조적으로 생기지 않습니다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Q1. 저장된 초안 호환 → 바꾸자(방법만 · 설계는 그대로)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 초안은 두 곳에 있습니다
  - 기기: localStorage me_order (order-preview.html) = {v:2, S, idx, sk, cs, sv, who}
  - 서버: 시트 제작_ritual 칸 (automation/platform/80_production.gs) = {_v:3, S, summary:{course,count,min,flow}}
  - 순서와 담긴 순간은 S 안의 네 값이 정합니다: course · extra · off · ord
- norm() 사고의 정체 — assets/ritual-cue.js 192행 한 줄
    if (!D.COURSES[s.course]) s.course = 'damback';
  코스 키가 사라지면 소리 없이 «약속»으로 바뀌고, seqOf(369행)가 약속 코스에 없는 순간을 걸러 냅니다.
  당일 콘솔이 고객이 고르지 않은 예식을 틉니다. [COURSE_HIDDEN] 주석(ritual-data.js 198~202행)이 이것입니다.
  ★반대로 빌더는 모르는 코스면 복원을 거부합니다(order-preview.html 3351행). 한 초안에 두 화면이 다른 답을 냅니다.
- 초안을 읽는 곳이 여럿입니다: admin 식순지 · mypage 요약(«○○ 코스») · PHOTO_SCENE · GAS 확인서 스냅샷 · Admin.html
- 제안
  ① 코스 키를 지우지 않는다. 새 방식은 «새 코스 키 하나»로 넣는다(Q7-1). 옛 초안은 옛 키로 그대로 돈다.
  ② 서버 초안을 일괄 변환하지 않는다. 다시 저장하는 순간 _prodConfirmVoid(80_production.gs)가
     예식 확인서를 풀어 버린다. 옮기려면 «읽을 때 바꾸기»(ORD_FIXPOS 가 이미 이렇게 한다).
  ③ norm() 의 조용한 떨어짐을 «큰 소리로 실패»로 바꾼다 — 모르는 코스면 콘솔에 경고 띠 + 운영자 확인.
     (기술 판단 · 사장님 결정 불필요)
- 위험: 옛 초안 중 ord 로 순서를 바꿔 둔 것을 v4 로 옮기면 당일 순서가 조용히 바뀐다 → 옮기지 말고 옛 키로 둔다.
- 모름: 실제로 저장된 초안이 몇 건인지(시트를 못 본다). 0건이면 ②는 걱정할 필요가 없다 — 사장님이 시트에서 확인 가능.
- 사장님 결정: 아니오

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Q2. 순서를 하나로 고정 → 그대로 가도 됨(엔진은 거의 안 바뀐다)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 지금 순서는 다섯 겹입니다
  - 코스별 seq 와 opt(at) (ritual-data.js)
  - RANK 두 벌: ritual-cue.js 349행 · order-preview.html 1639행 — 지금은 편지 → 부모님 인사 → 준비한 순서라 v4 와 다르다
  - ↑↓ 버튼 momMove + FIXPOS (order-preview.html)
  - 엔진 seqOf(ritual-cue.js 369행) → build(763행)가 seq 를 차례로 돌며 순간별 큐를 이어 붙인다
- ★녹음 이음에 미치는 영향 없음. 코드에는 «이웃 둘을 묶은 이음 클립»이 없습니다.
  순간마다 자기 여는 말 · 닫는 말만 있고, 닫는 말은 다음 순서를 지목하지 않습니다([LEAD_OUT]).
  그래서 순서를 고정해도 녹음은 다시 받지 않습니다. 설계의 «이음 72»가 인접 조합 수라면 코드에는 해당 구조가 없습니다.
- 제안: 새 코스 하나의 seq 를 v4 순서로 적고 ↑↓와 ord 를 그 코스에서만 끈다. RANK 두 벌은 새 코스에서 안 쓴다.
- 함께 고칠 검사: merge-guard ORD_FIXPOS · ritual-order-sim.mjs · check-ritual-cue.js 스냅샷 ·
  check-ritual-mirror.js 의 «COURSES === 6» · build-course-story · check-echo-inrun (구현 때 코드가 한다)
- 사장님 결정: 아니오

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Q3. 와인 → 축배 판 → 바꾸자(녹음 두 줄이 새로 필요)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- [WINE_RETIRED](ritual-cue.js)는 그대로 유효. 폐지 클립 17(valley-wine) · 54(valley-out)는 되살리지 않는다.
- 지금 축배 클립: 40 toast-toast · 41 toast-cake · 42 toast-both · 76 toast-both-b · 56 narr-toast-out · 77 cake-out · 87 narr-toast-none
  ★87 은 mp3 가 아직 없다(assets/audio/narration/ 에 없음 · 녹음 대기). 판 셋 모두 그 앞에 나간다.
- 판별로 필요한 것
  - 잔만 들기: 지금 both 판 그대로. 새 녹음 없음
  - 두 와인을 한 잔에: «붓는» 말이 새로 필요. 76 은 «두 사람에게 잔이 가는 동안»이라 붓는 장면과 어긋날 수 있다
    → 새 슬러그로 맨 끝(89번 뒤)에 붙인다. 17 을 되살리면 WINE_RETIRED 와 부딪친다
  - 양가가 한 병씩: 새 여는 말 1줄 필요. «어느 집 와인인지 사연 한 줄»은 예식마다 바뀌어 녹음으로 못 넣는다
    → 디렉터가 말하거나 빼는 것으로 설계에서 정해 주세요
- 모름: 케이크만 · 잔만 «단독» 판을 남기는지(설계에 없음)
- 사장님 결정: 예(양가 사연을 누가 말하나 · 새 녹음 두 줄)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Q4. 새로 들어오는 것 → 항목마다 다름
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 화촉: 코드에 없다. 입장 앞에 두면 두 곳과 부딪친다
  - FIXPOS 규칙 «입장 앞에 본식 순서 없음»(order-preview.html 1669~1672행)
  - 식전 안내 «곧 문이 열리고…»가 화촉 앞에 나가게 된다 → 그 안내를 화촉 뒤로 옮길지 설계 결정 필요
- 식전 영상: 반만 있다. 빌더에 S.growth 켜기와 링크 칸만 있고 엔진은 이 값을 안 읽는다. 큐 · 녹음 없음
- 준비한 순서: free(«자유 한 칸», 클립 58 · 59 녹음됨)로 이미 있다
  ★다만 «노래는 받지 않음»은 기존 약속을 뒤집는다. SONG_RETIRED 주석 · advisor-kb.js · api/_ritual-kb.js 가
    «노래는 자유 한 칸으로»라고 안내하고 있다 → AI 상담 답도 함께 바뀌어야 한다
- 편지 판: parent / each / both 로 있고 27 · 28 · 29 녹음이 있다. 새 녹음 없음
- 문안 원천 다섯 곳: ritual-data.js · ritual-cue.js(EXTRA · FILES) · order-preview.html 사본 · 배역_예시_대사.txt · build-dubbing-script.mjs
- 새 순간 하나당 파급: FILES 맨 끝에만 붙이고 N_FILES 를 올린다 · COURSES 사본은 build-course-mirror --write 로만 다시 뽑는다 · 생성기 연쇄는 코드가 돌린다
- 사장님 결정: 예(화촉 앞 식전 안내 위치 · 노래 받지 않음이 AI 답까지 바꾸는 것)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Q5. 시간 띠 → 바꾸자(원천을 하나로)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 지금 길이 값이 네 곳에 흩어져 있다
  - DAY(ritual-data.js 784행): 하루 140분 뼈대
  - MIN.base(787행): 코스 단위 예산 — 기록 17 · 약속 23 · 가족 25
  - [EVENT_BUDGET] 주석: 순간당 4분
  - 빌더 사본 estMin(order-preview.html) · 엔진 PARAM.spm 300 · live.est
  ★인사 사진 라운드 예산이 MIN.base[코스]에 묶여 있다(ritual-cue.js) → 코스를 없애면 이것부터 풀어야 한다
- 실측하면 서로 맞지 않는다: 엔진이 대본 글자로 잰 본식 합은 기록 5.3분 · 약속 9.2분 · 가족 10.3분인데
  MIN.base 는 17 · 23 · 25. 차이는 걷기 · 반지 · 사진 같은 «말 없는 시간»이다
- 제안: 순간마다 «대본 시간(엔진이 잰다) + 말 없는 시간(표에 적는다)»을 ritual-data.js 표 한 곳에 두고,
  시간 띠 · 빌더 · 엔진이 모두 그 표를 읽게 한다. 55분(본식 + 사진과 인사)은 DAY 에서 온다
- 시안 고르는화면_시안2_0925.html 의 parts() 값은 그 표의 첫 판으로 옮긴다
- 사장님 결정: 아니오(값 자체는 코워크 설계)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Q6. 바꿔야 할 고객 문구 (파일 · 줄)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- index.html
  - 6086행 «식순과 이벤트 배치가 자유롭습니다»
  - 6058행 «두 분이 설계한 식순으로 자유롭게… 고르신 코스에 따라»
  - 236 · 6692행 «주례 · 혼인 서약 같은 의례를…»
  - 164 · 5633 · 6640행 «16~25 / 30~39»
- order-preview.html(빌더)
  - 1939 · 1952 · 1958 · 1971행 코스 카드와 16~25 안내
  - 1605 · 2164행 «축가 · 영상 · 깜짝 순서»
  - 2324 · 2615 · 2630행 «추가 비용은 미리 안내»
  - ★1149행 «주례 없이, 하객분들이 함께 답하거나 가족이 낭독해요» — 하객 응답은 [ASK_RETIRED]로 이미 폐지됐다.
    지금도 사실과 다른 문장이다(개편과 별개로 먼저 고칠 수 있다)
- mypage.html: 식순 요약 «○○ 코스» · PHOTO_SCENE
- 그 밖: sequence-modal.js · advisor-kb.js · api/_kb.js · api/_ritual-kb.js · 스마트스토어 원본
- ★contract/v1-1.html 328행 «The Ceremony (16~25분)» — 계약서라 새 판을 내고 GAS 재배포가 필요하다
- 사장님 결정: 예(계약서 문구)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Q7. 더 단순한 길 · 설계의 빈틈
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7-1. 무엇을: 코스 여섯을 지우지 말고 «v4 전체 순서를 담은 새 코스 하나»(예: open)를 더한다.
     모든 순간을 off 로 담았다 뺐다 하고, ↑↓와 ord 는 그 코스에서 없앤다. 옛 코스 여섯은 hidden.
     예시 넷(기록 · 약속 · 가족 · 전부)은 «새 코스에 off 값을 미리 채운 것»으로 만든다.
   왜: 엔진이 거의 그대로 쓰이고, norm() 떨어짐이 생기지 않고, 시트 스키마를 안 바꾼다.
   대안: 코스 개념을 엔진에서 걷어 낸다 — 확인서 · 식순지 · PHOTO_SCENE · 인사 사진 예산까지 번져 크다.
   위험: 작다. 옛 코스 여섯을 오래 끌고 가는 비용 정도.
   사장님 결정: 아니오(기술 방법)
7-2. 빈틈: «빈 채 시작»이면 엔진 warnOf 가 «성혼 선언이 빠졌어요» 같은 경고를 띄운다.
     설계는 막는 말을 두지 않기로 했으니, 경고를 둘지(안내만) 없앨지 정해 주세요.
   사장님 결정: 예
7-3. 빈틈: 순간 이름을 바꾸면 PHOTO_SCENE · 식순지 · ritual-story ALIAS 가 따라 바뀌어야 한다.
     이름은 화면 라벨만 바꾸고 내부 키(k)는 그대로 두는 것을 권한다. 사장님 결정: 아니오
7-4. 진행 순서: 고객 화면을 크게 바꾸고 초안 호환이 걸린 작업이라 [COWORK_SPLIT_0925]에 따라
     claude/preview-… 가지 + PR 까지만 올리고, 미리보기 주소로 코워크 검토를 받은 뒤 병합한다.
7-5. 따로 먼저 해도 되는 것: Q6 의 1149행(폐지된 하객 응답 문장) — 개편과 무관한 사실 오류.
     코워크가 «먼저 고쳐라»라고 하면 바로 고친다(되돌리기 싼 변경).

■ 모름
- 저장된 초안 수(시트) · PR #352 상태(사장님이 닫을지 정하는 중)
